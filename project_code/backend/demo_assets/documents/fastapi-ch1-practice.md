# FastAPI实战 第一章 实践讲义

## 本节目标

- 创建第一个 FastAPI 应用
- 添加基础健康检查接口
- 体验自动文档页面

## 实践步骤

1. 安装 `fastapi` 和运行依赖
2. 定义 `app = FastAPI()`
3. 添加一个返回 JSON 的简单路由

## 完成标准

- 服务可以启动
- 路由返回 200
- `/docs` 页面可以正常打开
