# 前端页面详细设计说明书 - 6.8 反馈提交页 / 反馈弹窗

> 文档版本：v1.0 | 生成时间：2026-03-24 | 技术栈：Vue 3 + TypeScript + Element Plus + Pinia

---

## 1. 页面概述

| 属性 | 说明 |
|------|------|
| 组件名称 | 反馈提交弹窗（非独立页面，嵌入其他页面） |
| 组件文件 | `src/components/feedback/FeedbackDialog.vue` |
| 访问权限 | 需登录（学员 / 讲师 / 管理员） |
| 嵌入位置 | 课程详情页（反馈 Tab 内联）、个人中心（顶部操作按钮触发弹窗） |
| 设计形态 | 课程详情页 → 内联表单；个人中心 → `el-dialog` 弹窗 |

---

## 2. UI 组件结构图

```
FeedbackDialog.vue                          ← 反馈弹窗（el-dialog 模式）
└── el-dialog (v-model="visible", title="提交反馈", width="560px")
    └── FeedbackForm.vue                    ← 反馈表单（可内联使用或弹窗使用）
        └── el-form (ref="feedbackFormRef", label-position="top")
            ├── el-form-item[反馈类型]
            │   └── el-select (v-model="form.feedbackType", :disabled="typeDisabled")
            │       ├── el-option[系统问题, value="system"]
            │       └── el-option[课程问题, value="course"]
            │
            ├── [课程问题] el-form-item[关联课程]
            │   └── el-input[readonly] → 自动填充课程名称（课程详情页场景）
            │
            ├── el-form-item[反馈内容]
            │   └── el-input[type=textarea, :rows="5", :maxlength="500", show-word-limit]
            │       └── placeholder="请详细描述您遇到的问题（10-500 字符）"
            │
            ├── el-form-item[截图上传]
            │   └── el-upload
            │       ├── list-type="picture-card"
            │       ├── :limit="8"
            │       ├── accept=".jpg,.jpeg,.png"
            │       ├── :before-upload="validateImage"
            │       └── tip: "支持 JPG/PNG，单张最大 5MB，最多 8 张"
            │
            └── el-form-item
                └── el-button[type=primary, :loading="submitting"] → "提交反馈"
```

---

## 3. 数据模型 (TypeScript)

```typescript
/** 反馈表单数据 */
interface FeedbackFormData {
  feedbackType: 'system' | 'course'
  courseId: number | null
  courseName: string
  content: string
  images: string[]          // 已上传的图片 URL
}

/** 反馈表单 Props */
interface FeedbackFormProps {
  /** 嵌入模式：inline（内联） | dialog（弹窗） */
  mode: 'inline' | 'dialog'
  /** 预设反馈类型（课程详情页传入 'course'） */
  defaultType?: 'system' | 'course'
  /** 是否锁定反馈类型（课程详情页锁定为 course） */
  typeLocked?: boolean
  /** 关联课程 ID（课程详情页自动传入） */
  courseId?: number
  /** 关联课程名称 */
  courseName?: string
}

/** 反馈弹窗 Props */
interface FeedbackDialogProps {
  visible: boolean
}
```

---

## 4. 状态管理

### Pinia：无全局状态依赖

### Local State

```typescript
interface FeedbackFormState {
  form: FeedbackFormData
  submitting: boolean
  uploadingCount: number      // 当前正在上传的图片数量
  uploadedFiles: UploadFile[] // el-upload 文件列表
}
```

---

## 5. 核心业务逻辑

### 5.1 初始化

```
// 课程详情页场景
<FeedbackForm mode="inline" defaultType="course" :typeLocked="true"
  :courseId="courseId" :courseName="courseTitle" />

// 个人中心场景
<FeedbackDialog v-model:visible="showFeedback" />
  → <FeedbackForm mode="dialog" defaultType="system" />
```

### 5.2 图片上传校验

```typescript
function validateImage(file: File): boolean | Promise<boolean> {
  const validTypes = ['image/jpeg', 'image/png']
  if (!validTypes.includes(file.type)) {
    ElMessage.warning('仅支持 JPG/PNG 格式')
    return false
  }
  if (file.size > 5 * 1024 * 1024) {
    ElMessage.warning('单张图片最大 5MB')
    return false
  }
  if (form.images.length >= 8) {
    ElMessage.warning('最多上传 8 张截图')
    return false
  }
  return true
}
```

### 5.3 提交流程

```
handleSubmit()
 ├─ 表单校验
 │   ├─ content 长度 10-500
 │   ├─ feedbackType 非空
 │   └─ courseId（课程问题时必须）
 ├─ submitting = true
 ├─ POST /api/v1/feedbacks
 │   └─ { feedback_type, course_id?, content, images }
 ├─ 成功 → ElMessage.success('反馈已提交') → 清空表单 → 关闭弹窗
 ├─ code=429 → ElMessage.warning('提交过于频繁，请稍后再试')
 └─ submitting = false
```

---

## 6. API 接口清单

| 接口 | 方法 | 路径 | 函数名 | 入参 |
|------|------|------|--------|------|
| 提交反馈 | POST | `/api/v1/feedbacks` | `submitFeedback(data)` | `SubmitFeedbackRequest` |
| 上传截图 | POST | `/api/v1/upload/file` | `uploadFile(file)` | FormData |
