# 前端页面详细设计说明书 - 6.7 个人中心

> 文档版本：v1.0 | 生成时间：2026-03-24 | 技术栈：Vue 3 + TypeScript + Element Plus + Pinia
> 本文档覆盖个人中心 5 个子页面：基本信息、修改密码、学习记录、消息中心、我的反馈

---

## 1. 页面概述（总览）

| 属性 | 说明 |
|------|------|
| 模块名称 | 个人中心 |
| 布局组件 | `src/views/profile/ProfileLayout.vue`（左侧菜单 + 右侧内容区） |
| 访问权限 | 需登录（学员 / 讲师 / 管理员） |

| 子页面 | 路由地址 | 路由名称 | 文件路径 |
|--------|----------|----------|----------|
| 个人基本信息 | `/profile` | `Profile` | `src/views/profile/ProfileInfoPage.vue` |
| 修改密码 | `/profile/password` | `ProfilePassword` | `src/views/profile/ChangePasswordPage.vue` |
| 我的学习记录 | `/profile/records` | `ProfileRecords` | `src/views/profile/LearningRecordsPage.vue` |
| 消息中心 | `/profile/messages` | `ProfileMessages` | `src/views/profile/MessagesPage.vue` |
| 我的反馈 | `/profile/feedbacks` | `ProfileFeedbacks` | `src/views/profile/MyFeedbacksPage.vue` |

---

## 2. 布局组件结构

```
ProfileLayout.vue                          ← 个人中心布局
├── AppHeader.vue                          ← 全局导航栏
├── el-container
│   ├── el-aside (width="220px")           ← 左侧菜单
│   │   └── ProfileSideMenu.vue
│   │       └── el-menu (router, :default-active="currentRoute")
│   │           ├── el-menu-item[个人信息, icon=User]     → /profile
│   │           ├── el-menu-item[修改密码, icon=Lock]     → /profile/password
│   │           ├── el-menu-item[学习记录, icon=Reading]   → /profile/records
│   │           ├── el-menu-item[消息中心, icon=Bell]     → /profile/messages
│   │           │   └── el-badge[:value="unreadCount", :hidden="!unreadCount"]
│   │           └── el-menu-item[我的反馈, icon=ChatDotRound] → /profile/feedbacks
│   └── el-main                            ← 右侧内容区
│       └── <router-view />                ← 子路由渲染
└── AppFooter.vue
```

---

## 3. 数据模型（TypeScript，全模块共享）

```typescript
/** 用户详细信息 */
interface UserProfile {
  user_id: number
  username: string
  email: string
  phone: string
  nickname: string
  avatar_url: string
  role: 'student' | 'teacher' | 'admin'
  status: 'active' | 'disabled' | 'pending'
  created_at: string
  last_login_at: string
}

/** 更新个人信息请求 */
interface UpdateProfileRequest {
  nickname?: string
  email?: string
  phone?: string
  email_code?: string
}

/** 修改密码请求 */
interface ChangePasswordRequest {
  old_password: string
  new_password: string
  confirm_password: string
}

/** 学习记录项 */
interface LearningRecordItem {
  course_id: number
  course_title: string
  course_cover: string
  last_section_id: number
  last_section_title: string
  last_learn_at: string
  course_status: 'published' | 'archived'
}

/** 学习记录请求参数 */
interface LearningRecordsParams {
  time_range: 'recent_7' | 'recent_30' | 'all'
  page: number
  page_size: number
}

/** 消息项 */
interface MessageItem {
  message_id: number
  message_type: 'announcement' | 'notification'
  title: string
  content: string
  is_read: boolean
  created_at: string
}

/** 消息列表请求参数 */
interface MessagesParams {
  message_type?: 'all' | 'announcement' | 'notification'
  is_read?: boolean
  page: number
  page_size: number
}

/** 消息列表响应（含未读数） */
interface MessagesListData extends PaginatedData<MessageItem> {
  unread_count: number
}

/** 消息详情 */
interface MessageDetail extends MessageItem {
  read_at: string | null
}

/** 反馈项 */
interface FeedbackItem {
  feedback_id: number
  feedback_type: 'system' | 'course'
  content: string
  images: string[]
  status: 'pending' | 'processed'
  course_id?: number
  course_title?: string
  created_at: string
}
```

---

## 4. 子页面详细设计

---

### 4.1 个人基本信息页 (`/profile`)

#### UI 组件结构

```
ProfileInfoPage.vue
├── PageHeader                              ← 页面标题 "个人信息"
├── AvatarSection                           ← 头像区域
│   ├── el-avatar[:src, :size="80"]
│   └── UploadAvatarBtn (el-upload)         ← 更换头像按钮
├── InfoDisplaySection                      ← 信息展示（只读）
│   ├── InfoRow[用户名] → {{ username }}（不可修改）
│   ├── InfoRow[角色] → el-tag（学员/讲师/管理员）
│   ├── InfoRow[审核状态] → el-tag[type=warning]（讲师 pending 时展示）
│   └── InfoRow[注册时间] → {{ created_at }}
├── EditableInfoForm                        ← 可编辑表单
│   └── el-form
│       ├── el-form-item[昵称] → el-input
│       ├── el-form-item[邮箱] → el-input + 验证码按钮
│       ├── el-form-item[手机号] → el-input
│       └── [修改邮箱时] el-form-item[验证码] → el-input + 发送按钮
└── el-button[type=primary] → "保存修改"
```

#### Local State

```typescript
interface ProfileInfoState {
  profile: UserProfile | null
  isLoading: boolean
  editForm: { nickname: string; email: string; phone: string; emailCode: string }
  isEditing: boolean
  isSaving: boolean
  emailChanged: boolean       // 邮箱是否被修改（决定是否需要验证码）
  emailCodeCountdown: number
}
```

#### 核心交互

| 操作 | 处理逻辑 |
|------|----------|
| onMounted | `GET /api/v1/users/me` → 填充展示和表单 |
| 修改昵称 | 直接编辑，保存时提交 |
| 修改邮箱 | 检测邮箱变化 → 展示验证码输入框 → 发送验证码 → 保存时携带 email_code |
| 修改手机号 | 直接编辑，保存时提交 |
| 保存修改 | `POST /api/v1/users/me` → 成功后更新 userStore 和本地展示 |

#### API 清单

| 接口 | 方法 | 路径 | 函数名 |
|------|------|------|--------|
| 获取个人信息 | GET | `/api/v1/users/me` | `fetchProfile()` |
| 更新个人信息 | POST | `/api/v1/users/me` | `updateProfile(data)` |
| 发送邮箱验证码 | POST | `/api/v1/auth/send-email-code` | `sendEmailCode(data)` |
| 上传头像 | POST | `/api/v1/upload/file` | `uploadFile(file)` |

---

### 4.2 修改密码页 (`/profile/password`)

#### UI 组件结构

```
ChangePasswordPage.vue
├── PageHeader → "修改密码"
└── el-form (label-width="120px")
    ├── el-form-item[原密码] → el-input[type=password, show-password]
    ├── el-form-item[新密码] → el-input[type=password, show-password]
    │   └── PasswordStrengthBar (复用)
    ├── el-form-item[确认新密码] → el-input[type=password, show-password]
    └── el-button[type=primary, :loading] → "确认修改"
```

#### 核心交互

```
handleChangePassword()
 ├─ 表单校验（原密码非空、新密码格式、确认一致、新旧不同）
 ├─ POST /api/v1/users/me/change-password
 ├─ 成功 →
 │   ├─ ElMessage.success('密码修改成功，请重新登录')
 │   ├─ userStore.clearLoginInfo()  // 清除所有登录态
 │   └─ router.replace('/login')    // 强制跳转登录页
 └─ 失败 →
     ├─ 原密码错误 → '原密码错误，请重新输入'
     ├─ 新旧相同 → '新密码不能与原密码相同'
     └─ 其他 → response.message
```

#### API 清单

| 接口 | 方法 | 路径 | 函数名 |
|------|------|------|--------|
| 修改密码 | POST | `/api/v1/users/me/change-password` | `changePassword(data)` |

---

### 4.3 我的学习记录页 (`/profile/records`)

#### UI 组件结构

```
LearningRecordsPage.vue
├── PageHeader → "学习记录"
├── FilterBar
│   └── el-radio-group (v-model="timeRange")
│       ├── el-radio-button[近 7 天]   value="recent_7"
│       ├── el-radio-button[近 30 天]  value="recent_30"
│       └── el-radio-button[全部]      value="all"
├── [加载中] el-skeleton × 3
├── [空状态] el-empty → "暂无学习记录，快去学习课程吧"
├── [正常] RecordList
│   └── RecordCard × N
│       ├── el-image[课程封面, 左侧]
│       ├── CourseTitle → 课程名称
│       ├── LastSection → "上次学习：{{ last_section_title }}"
│       ├── LastTime → "{{ last_learn_at }}"
│       ├── [已下架] el-tag[type=danger] → "已下架"
│       └── ContinueBtn
│           ├── [已发布] el-button → "继续学习"
│           └── [已下架] el-button[disabled] → "课程已下架"
└── el-pagination
```

#### 核心交互

| 操作 | 处理逻辑 |
|------|----------|
| onMounted | `GET /api/v1/users/me/learning-records?time_range=all&page=1` |
| 切换时间范围 | 重置 page=1 → 重新请求 |
| 点击"继续学习" | `router.push(\`/learn/${courseId}\`)` |
| 课程已下架 | 按钮禁用，提示"该课程已下架或删除，无法继续学习" |

#### API 清单

| 接口 | 方法 | 路径 | 函数名 |
|------|------|------|--------|
| 学习记录列表 | GET | `/api/v1/users/me/learning-records` | `fetchLearningRecords(params)` |

---

### 4.4 消息中心页 (`/profile/messages`)

#### UI 组件结构

```
MessagesPage.vue
├── PageHeader
│   ├── "消息中心"
│   └── el-button[批量已读] → "全部标为已读"
├── FilterBar
│   ├── TypeFilter → el-radio-group
│   │   ├── el-radio-button[全部]     value="all"
│   │   ├── el-radio-button[系统通知] value="notification"
│   │   └── el-radio-button[公告]     value="announcement"
│   └── StatusFilter → el-radio-group
│       ├── el-radio-button[全部]   value=undefined
│       ├── el-radio-button[未读]   value=false
│       └── el-radio-button[已读]   value=true
├── [空状态] el-empty → "暂无消息"
├── [正常] MessageList
│   └── MessageCard × N
│       ├── UnreadDot (v-show="!is_read") ← 蓝色未读圆点
│       ├── TypeTag → el-tag[消息类型]
│       ├── Title → 消息标题（未读加粗）
│       ├── Summary → 消息摘要（单行省略）
│       ├── Time → 发布时间
│       └── Actions
│           ├── el-button[查看] → 展开详情 / 跳转
│           └── el-button[删除, type=danger] → 二次确认删除
├── el-pagination
│
└── MessageDetailDrawer.vue               ← 消息详情抽屉
    └── el-drawer
        ├── DrawerHeader → 消息标题 + 类型 + 时间
        └── DrawerBody → 消息完整内容
```

#### 核心交互

| 操作 | 处理逻辑 |
|------|----------|
| onMounted | `GET /api/v1/messages?page=1` |
| 点击消息 | 1. `POST /api/v1/messages/{id}/read`（标记已读）；2. 打开详情 Drawer；3. 更新列表中该条的 is_read |
| 全部标为已读 | `POST /api/v1/messages/mark-all-read` → 刷新列表 → 更新 userStore.unreadMessageCount |
| 删除消息 | ElMessageBox.confirm → `POST /api/v1/messages/{id}` → 从列表移除 |
| 筛选切换 | 重置 page=1 → 重新请求 |

#### API 清单

| 接口 | 方法 | 路径 | 函数名 |
|------|------|------|--------|
| 消息列表 | GET | `/api/v1/messages` | `fetchMessages(params)` |
| 消息详情 | GET | `/api/v1/messages/{id}` | `fetchMessageDetail(id)` |
| 标记已读 | POST | `/api/v1/messages/{id}/read` | `markAsRead(id)` |
| 批量已读 | POST | `/api/v1/messages/mark-all-read` | `markAllRead()` |
| 删除消息 | POST | `/api/v1/messages/{id}` | `deleteMessage(id)` |
| 未读数 | GET | `/api/v1/messages/unread-count` | `fetchUnreadCount()` |

---

### 4.5 我的反馈页 (`/profile/feedbacks`)

#### UI 组件结构

```
MyFeedbacksPage.vue
├── PageHeader → "我的反馈"
├── [空状态] el-empty → "暂无反馈记录"
├── [正常] FeedbackList
│   └── FeedbackCard × N
│       ├── TypeTag → el-tag[反馈类型: 系统问题/课程问题]
│       ├── Content → 反馈内容摘要（两行省略）
│       ├── Images → 缩略图列表 (el-image × N, 可预览)
│       ├── Time → 提交时间
│       ├── StatusTag
│       │   ├── [pending] el-tag[type=warning] → "处理中"
│       │   └── [processed] el-tag[type=success] → "已处理"
│       └── CourseName → [课程问题时] 关联课程名称
└── el-pagination
```

#### 核心交互

| 操作 | 处理逻辑 |
|------|----------|
| onMounted | 请求反馈列表（后端需提供"我的反馈"接口，或复用管理端接口+用户过滤） |
| 点击图片 | el-image 预览模式展示大图 |

> **接口备注**：当前接口文档仅提供管理员端反馈列表 `GET /api/v1/feedbacks`，用户端"我的反馈"需后端新增 `GET /api/v1/users/me/feedbacks` 接口，或前端复用反馈提交接口的返回数据做本地缓存。

---

## 5. 异常场景汇总

| 异常场景 | 页面 | 处理策略 |
|----------|------|----------|
| 未登录 | 所有子页面 | 路由守卫拦截 → 跳转登录页 |
| 个人信息加载失败 | 基本信息 | 展示错误状态 + 重试 |
| 昵称格式不合法 | 基本信息 | 字段下方提示"昵称包含违规字符或长度超出限制" |
| 邮箱/手机号已占用 (409) | 基本信息 | 提示"该邮箱/手机号已被其他账号绑定" |
| 原密码错误 | 修改密码 | 提示"原密码错误，请重新输入" |
| 课程已下架 | 学习记录 | 禁用"继续学习"按钮，展示已下架标签 |
| 消息不存在 | 消息中心 | 提示"消息不存在或已删除" |
| 网络异常 | 所有子页面 | ElMessage.error + 重试按钮 |
