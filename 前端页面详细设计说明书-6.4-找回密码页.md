# 前端页面详细设计说明书 - 6.4 找回密码页

> 文档版本：v1.0 | 生成时间：2026-03-24 | 技术栈：Vue 3 + TypeScript + Element Plus + Pinia

---

## 1. 页面概述

| 属性 | 说明 |
|------|------|
| 页面名称 | 找回密码页 |
| 路由地址 | `/forgot-password` |
| 路由名称 | `ForgotPassword` |
| 路由文件 | `src/views/auth/ForgotPasswordPage.vue` |
| 访问权限 | 公开（仅未登录用户访问；已登录用户重定向至首页） |
| 页面目标 | 支持未登录用户通过邮箱或手机号完成密码重置，采用分步骤引导流程 |

---

## 2. UI 组件结构图

```
ForgotPasswordPage.vue
├── AuthLayout.vue                              ← 认证页通用布局（与登录/注册页共用）
│   ├── BrandSide                               ← 左侧：平台 Logo + 标语 + 背景插画
│   └── FormSide                                ← 右侧：表单容器
│
├── ResetPasswordStepper.vue                    ← 步骤条容器
│   └── el-steps (:active="currentStep" align-center)
│       ├── el-step[title="验证身份"]            ← 步骤 1
│       ├── el-step[title="输入验证码"]           ← 步骤 2
│       └── el-step[title="设置新密码"]           ← 步骤 3
│
├── [步骤 1] IdentityVerifyStep.vue             ← 验证身份
│   └── el-form (ref="step1FormRef")
│       ├── MethodSelector                      ← 找回方式选择
│       │   └── el-radio-group (v-model="resetMethod")
│       │       ├── el-radio-button[邮箱找回]
│       │       └── el-radio-button[手机号找回]
│       │
│       ├── AccountField                        ← 账号输入
│       │   └── el-form-item + el-input
│       │       ├── [邮箱模式] placeholder="请输入注册邮箱"
│       │       │   └── prefix-icon: Message
│       │       └── [手机号模式] placeholder="请输入注册手机号"
│       │           └── prefix-icon: Phone
│       │
│       ├── [手机号模式] BoundEmailHint          ← 绑定邮箱脱敏展示
│       │   └── el-alert[type=info]
│       │       └── "验证码将发送至绑定邮箱 z***n@example.com"
│       │
│       └── NextStepButton                      ← 下一步按钮
│           └── el-button[type=primary, :loading="isVerifying"]
│
├── [步骤 2] EmailCodeVerifyStep.vue            ← 输入验证码
│   └── el-form (ref="step2FormRef")
│       ├── EmailHint                           ← 验证码发送提示
│       │   └── el-alert[type=success]
│       │       └── "验证码已发送至 z***n@example.com，请查收"
│       │
│       ├── EmailCodeField                      ← 邮箱验证码输入
│       │   └── el-form-item
│       │       ├── el-input (6位验证码, maxlength=6)
│       │       └── el-button (重新发送 / 倒计时)
│       │
│       ├── PrevStepButton                      ← 上一步
│       │   └── el-button[plain]
│       │
│       └── NextStepButton                      ← 下一步（验证码校验）
│           └── el-button[type=primary, :loading="isVerifyingCode"]
│
├── [步骤 3] NewPasswordStep.vue                ← 设置新密码
│   └── el-form (ref="step3FormRef")
│       ├── NewPasswordField                    ← 新密码
│       │   └── el-form-item + el-input[type=password, show-password]
│       │       └── prefix-icon: Lock
│       │
│       ├── ConfirmPasswordField                ← 确认新密码
│       │   └── el-form-item + el-input[type=password, show-password]
│       │       └── prefix-icon: Lock
│       │
│       ├── PasswordStrengthBar                 ← 密码强度指示（可选）
│       │   └── 三段颜色条：弱(红) / 中(橙) / 强(绿)
│       │
│       ├── PrevStepButton                      ← 上一步
│       │   └── el-button[plain]
│       │
│       └── SubmitButton                        ← 确认重置
│           └── el-button[type=primary, :loading="isResetting"]
│
└── [成功] ResetSuccessResult.vue               ← 重置成功结果页
    └── el-result[icon=success]
        ├── title: "密码重置成功"
        ├── sub-title: "请使用新密码登录"
        └── el-button[type=primary] → "去登录" → router.push('/login')
```

---

## 3. 数据模型 (TypeScript)

### 3.1 API 请求 / 响应类型

```typescript
/** 密码重置请求参数 */
interface ResetPasswordRequest {
  login_id: string          // 邮箱或手机号
  email_code: string        // 邮箱验证码
  new_password: string      // 新密码
  confirm_password: string  // 确认新密码
}

/** 密码重置响应 */
interface ResetPasswordResponse {
  // data: null（成功时无业务数据）
}

/** 发送邮箱验证码请求（复用注册页类型） */
interface SendEmailCodeRequest {
  email: string
  purpose: 'register' | 'reset_password'
}

/** 发送邮箱验证码响应 */
interface SendEmailCodeResponse {
  expires_in: number
}

/** 账号查找响应（前端辅助类型，基于接口推断） */
interface AccountLookupInfo {
  exists: boolean
  maskedEmail: string  // 脱敏邮箱，如 "z***n@example.com"
}
```

### 3.2 表单数据类型

```typescript
/** 找回密码表单数据（跨步骤共享） */
interface ForgotPasswordFormData {
  // 步骤 1：验证身份
  resetMethod: 'email' | 'phone'
  loginId: string             // 邮箱或手机号

  // 步骤 2：验证码
  emailCode: string
  targetEmail: string         // 实际接收验证码的邮箱（邮箱模式=loginId，手机号模式=绑定邮箱）
  maskedEmail: string         // 脱敏展示的邮箱

  // 步骤 3：新密码
  newPassword: string
  confirmPassword: string
}

/** 步骤枚举 */
type StepIndex = 0 | 1 | 2 | 3  // 0=验证身份, 1=输入验证码, 2=设置新密码, 3=成功
```

---

## 4. 状态管理逻辑

### 4.1 Pinia State（全局状态）

找回密码页**不写入任何全局状态**。整个流程完全在组件内部完成，成功后跳转登录页。

唯一的全局依赖：
- 读取 `useUserStore().isLoggedIn` → 路由守卫拦截已登录用户

### 4.2 Local State（组件私有状态）

```typescript
interface ForgotPasswordPageState {
  // 步骤控制
  currentStep: StepIndex           // 当前步骤索引

  // 表单数据
  formData: ForgotPasswordFormData

  // UI 状态
  isVerifying: boolean             // 步骤 1 下一步 loading
  isVerifyingCode: boolean         // 步骤 2 验证码校验 loading（前端仅做格式校验）
  isResetting: boolean             // 步骤 3 提交 loading
  isSendingCode: boolean           // 发送验证码 loading
  emailCodeCountdown: number       // 验证码倒计时（60s）
  isResetSuccess: boolean          // 重置成功标记

  // 错误状态
  step1Error: string               // 步骤 1 错误信息
  step2Error: string               // 步骤 2 错误信息
  step3Error: string               // 步骤 3 错误信息
}
```

---

## 5. 核心业务逻辑与交互

### 5.1 初始化流程 (onMounted)

```
onMounted
 ├─ 1. 检查登录态：若已登录 → router.replace('/')
 ├─ 2. 初始化表单数据
 │    └─ formData.resetMethod = 'email'（默认邮箱找回）
 └─ 3. currentStep = 0（从步骤 1 开始）
```

### 5.2 步骤 1：验证身份

#### 5.2.1 找回方式切换

| 操作 | 处理逻辑 |
|------|----------|
| 切换到邮箱模式 | 1. `formData.resetMethod = 'email'`；2. 清空 `formData.loginId`；3. 清除校验状态；4. 更新 placeholder 和校验规则 |
| 切换到手机号模式 | 1. `formData.resetMethod = 'phone'`；2. 清空 `formData.loginId`；3. 清除校验状态；4. 更新 placeholder 和校验规则 |

#### 5.2.2 校验规则

```typescript
const step1Rules = computed<FormRules>(() => ({
  loginId: [
    {
      required: true,
      message: formData.resetMethod === 'email' ? '请输入注册邮箱' : '请输入注册手机号',
      trigger: 'blur'
    },
    formData.resetMethod === 'email'
      ? { type: 'email', message: '请输入正确的邮箱地址', trigger: 'blur' }
      : { pattern: /^1[3-9]\d{9}$/, message: '请输入正确的 11 位手机号', trigger: 'blur' }
  ]
}))
```

#### 5.2.3 点击"下一步"

```
handleStep1Next()
 ├─ 1. step1FormRef.validate() → 校验通过
 ├─ 2. isVerifying = true
 ├─ 3. 发送验证码
 │    ├─ 邮箱模式：
 │    │   └─ POST /api/v1/auth/send-email-code { email: formData.loginId, purpose: 'reset_password' }
 │    │   └─ 成功 → formData.targetEmail = formData.loginId
 │    │           → formData.maskedEmail = maskEmail(formData.loginId)
 │    └─ 手机号模式：
 │        └─ POST /api/v1/auth/send-email-code { email: 由后端通过手机号查找绑定邮箱, purpose: 'reset_password' }
 │        └─ 注意：实际流程中，后端需要先根据手机号查找用户的绑定邮箱再发送
 │        └─ 成功 → 后端返回脱敏邮箱信息
 │                → formData.maskedEmail = response 中脱敏邮箱
 ├─ 4. 响应处理：
 │    ├─ 成功 (code=200)
 │    │   ├─ 启动 60s 倒计时
 │    │   ├─ currentStep = 1（进入步骤 2）
 │    │   └─ ElMessage.success('验证码已发送')
 │    └─ 失败
 │        ├─ code=404 → step1Error = '该邮箱/手机号未注册账号'
 │        ├─ code=429 → step1Error = '发送过于频繁，请 60 秒后重试'
 │        └─ 其他 → step1Error = response.message
 └─ 5. isVerifying = false
```

### 5.3 步骤 2：输入验证码

#### 5.3.1 校验规则

```typescript
const step2Rules: FormRules = {
  emailCode: [
    { required: true, message: '请输入邮箱验证码', trigger: 'blur' },
    { len: 6, message: '验证码为 6 位数字', trigger: 'blur' },
    { pattern: /^\d{6}$/, message: '验证码为 6 位数字', trigger: 'blur' }
  ]
}
```

#### 5.3.2 交互操作

| 操作 | 处理逻辑 |
|------|----------|
| 点击"重新发送" | 1. 检查倒计时（>0 则忽略）；2. 调用 `POST /api/v1/auth/send-email-code`；3. 重启 60s 倒计时 |
| 点击"上一步" | `currentStep = 0`；保留 `formData.loginId`（不清空） |
| 点击"下一步" | 1. 前端校验格式；2. `currentStep = 2`（验证码最终在步骤 3 提交时由后端校验） |

> **设计决策说明**：验证码的有效性由最终的 `POST /api/v1/auth/reset-password` 接口统一校验。步骤 2 的"下一步"仅做前端格式校验（6 位数字），不额外调用校验接口，减少一次 API 调用。若后端提供独立的验证码校验接口，可在此步骤增加服务端校验。

### 5.4 步骤 3：设置新密码

#### 5.4.1 校验规则

```typescript
const step3Rules: FormRules = {
  newPassword: [
    { required: true, message: '请输入新密码', trigger: 'blur' },
    { min: 8, max: 20, message: '密码长度为 8-20 位', trigger: 'blur' },
    {
      pattern: /^(?=.*[a-zA-Z])(?=.*\d).+$/,
      message: '密码需包含字母和数字',
      trigger: 'blur'
    }
  ],
  confirmPassword: [
    { required: true, message: '请确认新密码', trigger: 'blur' },
    {
      validator: (_rule, value, callback) => {
        if (value !== formData.newPassword) {
          callback(new Error('两次输入的新密码不一致'))
        } else {
          callback()
        }
      },
      trigger: 'blur'
    }
  ]
}
```

#### 5.4.2 点击"确认重置"

```
handleResetPassword()
 ├─ 1. step3FormRef.validate() → 校验通过
 ├─ 2. isResetting = true
 ├─ 3. POST /api/v1/auth/reset-password
 │    └─ {
 │         login_id: formData.loginId,
 │         email_code: formData.emailCode,
 │         new_password: formData.newPassword,
 │         confirm_password: formData.confirmPassword
 │       }
 ├─ 4. 响应处理：
 │    ├─ 成功 (code=200)
 │    │   ├─ isResetSuccess = true
 │    │   ├─ currentStep = 3（展示成功页）
 │    │   └─ ElMessage.success('密码重置成功')
 │    └─ 失败
 │        ├─ code=422 + message含"验证码"
 │        │   └─ step3Error = '验证码错误或已过期'
 │        │   └─ 建议：回退到步骤 2 重新获取验证码
 │        ├─ code=422 + message含"密码"
 │        │   └─ step3Error = '密码需为 8-20 位字母 + 数字组合'
 │        ├─ code=404
 │        │   └─ step3Error = '该账号不存在'
 │        └─ 其他 → step3Error = response.message
 └─ 5. isResetting = false
```

#### 5.4.3 重置成功

| 操作 | 处理逻辑 |
|------|----------|
| 点击"去登录" | `router.push('/login')` |
| 自动倒计时跳转（可选增强） | 5s 后自动跳转登录页，展示倒计时文案"5 秒后自动跳转至登录页" |

### 5.5 异常场景处理

| 异常场景 | 触发步骤 | 前端判断条件 | 处理策略 |
|----------|----------|-------------|----------|
| **账号未注册** | 步骤 1 | code=404 | 输入框下方红色提示"该邮箱/手机号未注册账号" |
| **验证码发送频率限制** | 步骤 1/2 | code=429 | ElMessage.warning('发送过于频繁，请稍后重试')；按钮保持倒计时 |
| **验证码错误或过期** | 步骤 3 | code=422 + message 含"验证码" | ElMessageBox.confirm 弹窗："验证码已失效，是否返回重新获取？" → 确认则回退到步骤 1 |
| **新密码格式错误** | 步骤 3 | code=422 + message 含"密码" | 字段下方提示"密码需为 8-20 位字母 + 数字组合" |
| **两次密码不一致** | 步骤 3 | 前端校验 | 字段下方提示"两次输入的新密码不一致" |
| **网络断开** | 任意步骤 | axios catch | ElMessage.error('网络连接失败，请检查网络后重试') |
| **服务端 500** | 任意步骤 | code=500 | ElMessage.error('服务暂时不可用，请稍后重试') |
| **已登录用户访问** | 进入页面 | 路由守卫 | `router.replace('/')` |
| **用户直接输入步骤 2/3 的 URL** | 进入页面 | 无步骤 1 数据 | 强制从步骤 1 开始，不允许跳步 |

---

## 6. API 接口清单

| 序号 | 接口用途 | HTTP 方法 | 路径 | 调用函数名 | 入参 | 调用时机 |
|------|----------|-----------|------|------------|------|----------|
| 1 | 发送邮箱验证码 | POST | `/api/v1/auth/send-email-code` | `sendEmailCode(data: SendEmailCodeRequest)` | `{ email, purpose: 'reset_password' }` | 步骤 1 下一步 / 步骤 2 重新发送 |
| 2 | 密码重置 | POST | `/api/v1/auth/reset-password` | `resetPassword(data: ResetPasswordRequest)` | `{ login_id, email_code, new_password, confirm_password }` | 步骤 3 确认重置 |

---

## 7. 补充设计要点

### 7.1 邮箱脱敏工具函数

```typescript
/**
 * 邮箱地址脱敏展示
 * 输入：zhangsan@example.com → 输出：z***n@example.com
 * 输入：ab@example.com → 输出：a***b@example.com
 */
function maskEmail(email: string): string {
  const [local, domain] = email.split('@')
  if (local.length <= 2) {
    return `${local[0]}***@${domain}`
  }
  return `${local[0]}***${local[local.length - 1]}@${domain}`
}
```

### 7.2 手机号模式的邮箱查找

根据接口文档，`POST /api/v1/auth/send-email-code` 的入参是 `email`，不是手机号。因此手机号模式下的流程需要特殊处理：

**方案**：手机号模式下，前端先调用一个账号查找接口获取绑定邮箱的脱敏信息，再用实际邮箱发送验证码。

如果后端未提供独立的账号查找接口，可通过 `POST /api/v1/auth/reset-password` 接口的 `login_id` 字段传递手机号，由后端内部处理邮箱查找和验证码发送。此时前端仅展示"验证码已发送至您的绑定邮箱"作为通用提示。

### 7.3 步骤数据保护

```typescript
// 步骤跳转保护：不允许跳过前置步骤
watch(currentStep, (newStep) => {
  // 步骤 2 需要步骤 1 的数据
  if (newStep >= 1 && !formData.loginId) {
    currentStep.value = 0
    return
  }
  // 步骤 3 需要步骤 2 的数据
  if (newStep >= 2 && !formData.emailCode) {
    currentStep.value = 1
    return
  }
})

// 页面离开提示（表单已填写时）
onBeforeRouteLeave((to, from, next) => {
  if (formData.loginId && !isResetSuccess.value) {
    ElMessageBox.confirm('确定要离开吗？当前填写的信息将丢失', '提示', {
      confirmButtonText: '确定离开',
      cancelButtonText: '继续填写',
      type: 'warning'
    }).then(() => next()).catch(() => next(false))
  } else {
    next()
  }
})
```

### 7.4 倒计时管理（与注册页共用逻辑）

```typescript
// 可抽取为 composable: useCountdown.ts
function useCountdown(initialSeconds: number = 60) {
  const countdown = ref(0)
  let timer: ReturnType<typeof setInterval> | null = null

  function start() {
    countdown.value = initialSeconds
    timer = setInterval(() => {
      countdown.value--
      if (countdown.value <= 0) {
        stop()
      }
    }, 1000)
  }

  function stop() {
    if (timer) {
      clearInterval(timer)
      timer = null
    }
    countdown.value = 0
  }

  const isActive = computed(() => countdown.value > 0)

  onUnmounted(stop)

  return { countdown, isActive, start, stop }
}
```

### 7.5 响应式布局

| 屏幕宽度 | 布局方案 |
|----------|----------|
| ≥1280px | 左右分栏：左侧品牌区（40%）+ 右侧表单区（60%），步骤条水平排列 |
| <1280px | 全屏表单居中，品牌区隐藏，步骤条紧凑排列 |

### 7.6 可复用组件提取

本页面与注册页共享以下逻辑，建议抽取为 composable 或组件：

| 可复用单元 | 类型 | 文件路径 | 说明 |
|------------|------|----------|------|
| `AuthLayout.vue` | 组件 | `src/layouts/AuthLayout.vue` | 认证页通用布局（登录/注册/找回密码三页共用） |
| `useCountdown` | Composable | `src/composables/useCountdown.ts` | 倒计时逻辑（注册页 + 找回密码页共用） |
| `maskEmail` | 工具函数 | `src/utils/format.ts` | 邮箱脱敏（找回密码页 + 个人中心页可能复用） |
| 密码校验规则 | 常量 | `src/utils/validators.ts` | 密码格式正则和校验规则（注册 / 找回密码 / 修改密码三处共用） |
| `PasswordStrengthBar.vue` | 组件 | `src/components/PasswordStrengthBar.vue` | 密码强度指示器（注册 + 找回密码共用） |
