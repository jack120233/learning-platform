# 教师端课程管理：后端接口开发参考文档

本文档定义了前端目前已实现的接口调用逻辑，请后端根据此规范补齐或扩展接口。

## 一、 文件上传类接口 (Base URL: `/api/v1`)

### 1.1 通用小文件上传 (已存在，需扩展)
前端用于上传封面图、小体积视频或文档。
- **路径**: `POST /upload/file`
- **请求体**: `multipart/form-data`
  - `file`: 二进制文件
- **前端已适配的文件类型**: 
  - 封面: `.jpg, .png`
  - 资料/课件: `.pdf, .doc, .docx, .ppt, .pptx, .xls, .xlsx, .csv, .md, .zip`
- **期望返回**:
  ```json
  {
    "file_url": "https://cdn.example.com/path/to/file.pdf",
    "file_name": "教学大纲.pdf",
    "file_size": 102400
  }
  ```

### 1.2 大文件分片上传 (待实现)
用于上传高清视频或其他大体积资源。

#### A. 初始化上传
- **路径**: `POST /upload/init`
- **请求参数**: `{ "file_name": string, "file_size": number, "chunk_size": number }`
- **期望返回**:
  ```json
  { "upload_id": "uuid-string", "chunk_size": 10485760, "total_chunks": 5 }
  ```

#### B. 上传分片
- **路径**: `POST /upload/chunk`
- **请求体**: `multipart/form-data` (包含 `upload_id`, `chunk_index`, `chunk` 二进制)

#### C. 完成上传 (合并)
- **路径**: `POST /upload/complete`
- **请求参数**: `{ "upload_id": string, "file_name": string, "total_chunks": number }`
- **期望返回**: 与 1.1 相同，包含 `file_url`, `file_name`, `file_size`。

---

## 二、 业务绑定类接口 (Base URL: `/api/v1`)

### 2.1 课程全局资料管理
- **上传并绑定**: `POST /courses/{course_id}/materials`
  - 前端目前直接传 `multipart/form-data`。后端需保存文件并返回 `material_id`。
- **删除绑定**: `POST /courses/{course_id}/materials/{material_id}/delete`

### 2.2 章节/小节学习资源管理
前端在上传完文件后，会调用此接口将 `file_url` 转化为业务资源。

- **上传到章节**: `POST /courses/{course_id}/chapters/{chapter_id}/resources`
- **上传到小节**: `POST /courses/{course_id}/sections/{section_id}/resources`
- **请求参数 (`ResourceUploadItem`)**:
  ```json
  {
    "resource_type": "video" | "audio" | "document" | "image",
    "title": "文件名.mp4",
    "file_name": "文件名.mp4",
    "file_url": "http://...",
    "file_size": 123456,
    "sort_order": 0,
    "is_free": false,
    "duration": 600 // 视频可选
  }
  ```
- **删除资源**: 
  - 章节: `POST /courses/{course_id}/chapters/{chapter_id}/resources/{resource_id}/delete`
  - 小节: `POST /courses/{course_id}/sections/{section_id}/resources/{resource_id}/delete`

---

## 三、 前端页面逻辑对照说明
1. **课程全局资料**: 页面顶部区域，支持各种文档。
2. **章节层级 (Chapter)**: 提供“整体管理”入口，对话框宽度 **900px**，显示双栏（左视频/右课件）。
3. **小节层级 (Section)**: 
   - “上传视频”按钮：打开 **500px** 宽对话框，仅过滤显示视频区。
   - “课件资源”按钮：打开 **500px** 宽对话框，仅过滤显示文档/压缩包区。
4. **旧数据处理**: 非 `video` 类型的旧数据（如图片）会自动归类到右侧预览区。
