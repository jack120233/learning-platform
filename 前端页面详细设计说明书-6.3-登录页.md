# 前端页面详细设计说明书 - 6.3 登录页

> 文档版本：v1.0 | 生成时间：2026-03-24 | 技术栈：Vue 3 + TypeScript + Element Plus + Pinia

---

## 1. 页面概述

| 属性 | 说明 |
|------|------|
| 页面名称 | 登录页 |
| 路由地址 | `/login` |
| 路由名称 | `Login` |
| 路由文件 | `src/views/auth/LoginPage.vue` |
| 访问权限 | 公开（仅未登录用户访问；已登录用户重定向至首页） |
| 页面目标 | 支持邮箱/手机号登录，处理记住我、账号锁定、Token 管理等场景 |

---

## 2. UI 组件结构图

```
LoginPage.vue
├── AuthLayout.vue                          ← 认证页通用布局（与注册页共用）
│   ├── BrandSide                           ← 左侧：平台 Logo + 标语 + 背景插画
│   └── FormSide                            ← 右侧：登录表单容器
│
├── LoginForm.vue                           ← 登录表单核心组件
│   └── el-form (ref="loginFormRef")
│       ├── LoginMethodTabs                 ← 登录方式切换
│       │   └── el-tabs (v-model="loginMethod")
│       │       ├── el-tab-pane[邮箱登录]
│       │       └── el-tab-pane[手机号登录]
│       │
│       ├── LoginIdField                    ← 账号输入
│       │   └── el-form-item + el-input
│       │       ├── [邮箱模式] placeholder="请输入邮箱"
│       │       │   └── prefix-icon: Message
│       │       └── [手机号模式] placeholder="请输入手机号"
│       │           └── prefix-icon: Phone
│       │
│       ├── PasswordField                   ← 密码输入
│       │   └── el-form-item + el-input[type=password, show-password]
│       │       └── prefix-icon: Lock
│       │
│       ├── OptionsRow                      ← 辅助操作行
│       │   ├── RememberMe                  ← 记住我
│       │   │   └── el-checkbox (v-model="rememberMe")
│       │   └── ForgotPassword              ← 忘记密码
│       │       └── router-link[to="/forgot-password"]
│       │
│       ├── SubmitButton                    ← 登录按钮
│       │   └── el-button[type=primary, :loading="isSubmitting"]
│       │       └── 文案："登录" / "登录中..."
│       │
│       └── RegisterLink                    ← 还没有账号？去注册
│           └── router-link[to="/register"]
│
└── TeacherPendingNotice.vue               ← 讲师审核中顶部通知条（条件渲染）
    └── el-alert[type=warning, :closable="false"]
        └── "您的讲师申请正在审核中，当前可以学员身份浏览和学习课程"
```

---

## 3. 数据模型 (TypeScript)

### 3.1 API 请求 / 响应类型

```typescript
/** 登录请求参数 */
interface LoginRequest {
  login_id: string       // 邮箱或手机号
  password: string
  remember_me: boolean
}

/** 登录成功响应 */
interface LoginResponse {
  user_id: number
  username: string
  email: string
  nickname: string
  avatar_url: string
  role: 'student' | 'teacher' | 'admin'
  status: 'active' | 'pending' | 'disabled'
  access_token: string
  refresh_token: string
  token_type: string
  expires_in: number
}

/** 刷新令牌请求 */
interface RefreshTokenRequest {
  refresh_token: string
}

/** 刷新令牌响应 */
interface RefreshTokenResponse {
  access_token: string
  token_type: string
  expires_in: number
}
```

### 3.2 表单数据类型

```typescript
/** 登录表单字段 */
interface LoginFormData {
  loginId: string         // 邮箱或手机号
  password: string
  rememberMe: boolean
}
```

---

## 4. 状态管理逻辑

### 4.1 Pinia State（全局状态）

登录成功后写入 `useUserStore`：

```typescript
// 登录成功 → userStore.setLoginInfo(response)
// 存储位置：
//   - accessToken → 内存 (Pinia state)
//   - refreshToken → localStorage (持久化)
//   - 用户基本信息 → Pinia state + localStorage

interface UserStoreActions {
  setLoginInfo(data: LoginResponse): void
  clearLoginInfo(): void
  refreshAccessToken(): Promise<boolean>
  // Token 刷新逻辑：
  //   1. 读取 localStorage 中的 refreshToken
  //   2. POST /api/v1/auth/refresh
  //   3. 成功 → 更新 accessToken
  //   4. 失败 → clearLoginInfo() → router.push('/login')
}
```

### 4.2 Local State（组件私有状态）

```typescript
interface LoginPageState {
  // 表单数据
  formData: LoginFormData

  // UI 状态
  loginMethod: 'email' | 'phone'   // 当前登录方式
  isSubmitting: boolean             // 登录按钮 loading
  loginErrorCount: number           // 当前会话登录失败次数（前端计数，仅做提示用）

  // 重定向
  redirectUrl: string | null        // 登录前目标页（从 route.query.redirect 获取）
}
```

---

## 5. 核心业务逻辑与交互

### 5.1 初始化流程 (onMounted)

```
onMounted
 ├─ 1. 检查登录态：若已登录 → router.replace('/')
 ├─ 2. 解析重定向 URL
 │    └─ redirectUrl = route.query.redirect as string || null
 ├─ 3. 恢复"记住我"状态
 │    └─ 若 localStorage 中存有 'edu_remember_login_id'
 │       ├─ formData.loginId = localStorage.getItem('edu_remember_login_id')
 │       ├─ formData.rememberMe = true
 │       └─ 自动判断并设置 loginMethod（邮箱/手机号格式检测）
 └─ 4. 聚焦到第一个空输入框
```

### 5.2 表单校验规则

```typescript
const loginRules = computed<FormRules>(() => ({
  loginId: [
    { required: true, message: loginMethod.value === 'email' ? '请输入邮箱' : '请输入手机号', trigger: 'blur' },
    loginMethod.value === 'email'
      ? { type: 'email', message: '请输入正确的邮箱地址', trigger: 'blur' }
      : { pattern: /^1[3-9]\d{9}$/, message: '请输入正确的 11 位手机号', trigger: 'blur' }
  ],
  password: [
    { required: true, message: '请输入密码', trigger: 'blur' },
    { min: 8, max: 20, message: '密码长度为 8-20 位', trigger: 'blur' }
  ]
}))
```

### 5.3 用户交互处理

#### 5.3.1 登录方式切换

| 操作 | 处理逻辑 |
|------|----------|
| 切换 Tab | 1. 更新 `loginMethod`；2. 清空 `formData.loginId`；3. 清除该字段校验状态 `loginFormRef.clearValidate('loginId')`；4. 聚焦到账号输入框 |

#### 5.3.2 提交登录

```
handleLogin()
 ├─ 1. el-form.validate() → 校验通过
 ├─ 2. isSubmitting = true
 ├─ 3. POST /api/v1/auth/login
 │    └─ { login_id: formData.loginId, password: formData.password, remember_me: formData.rememberMe }
 ├─ 4. 响应处理：
 │    ├─ 成功 (code=200)
 │    │   ├─ userStore.setLoginInfo(response.data)
 │    │   ├─ 处理"记住我"
 │    │   │   ├─ 勾选 → localStorage.setItem('edu_remember_login_id', formData.loginId)
 │    │   │   └─ 未勾选 → localStorage.removeItem('edu_remember_login_id')
 │    │   ├─ 角色跳转路由
 │    │   │   ├─ redirectUrl 存在 → router.replace(redirectUrl)
 │    │   │   ├─ role='admin' → router.replace('/admin/users')
 │    │   │   ├─ role='teacher' && status='active' → router.replace('/')
 │    │   │   ├─ role='teacher' && status='pending' → router.replace('/') + 展示审核通知
 │    │   │   └─ role='student' → router.replace('/')
 │    │   └─ ElMessage.success('登录成功')
 │    └─ 失败
 │        ├─ code=401 message含"不存在或密码错误" → ElMessage.error('账号不存在或密码错误')
 │        ├─ code=403 message含"锁定" → ElMessage.error('账号已锁定，请 30 分钟后重试')
 │        ├─ code=403 message含"禁用" → ElMessage.error('账号已被禁用，请联系管理员')
 │        └─ 其他 → ElMessage.error(response.message)
 └─ 5. isSubmitting = false
```

#### 5.3.3 记住我

| 操作 | 处理逻辑 |
|------|----------|
| 勾选 | `formData.rememberMe = true`；登录成功后将 loginId 存入 localStorage |
| 取消勾选 | `formData.rememberMe = false`；登录成功后清除 localStorage 中的 loginId |

**Token 有效期影响**：
- 勾选"记住我" → `remember_me: true` → 后端设置 refresh_token 有效期 30 天
- 未勾选 → `remember_me: false` → refresh_token 有效期 7 天
- access_token 始终 24 小时

#### 5.3.4 键盘交互

| 操作 | 处理逻辑 |
|------|----------|
| 账号输入框按 Enter | 聚焦到密码输入框 |
| 密码输入框按 Enter | 触发 `handleLogin()` |

### 5.4 异常场景处理

| 异常场景 | 前端判断条件 | 处理策略 |
|----------|-------------|----------|
| **账号不存在或密码错误** | code=401 | ElMessage.error('账号不存在或密码错误')；`loginErrorCount++`；当 `loginErrorCount >= 3` 时提示"密码连续错误 5 次将锁定账号" |
| **账号锁定** | code=403 + message 含"锁定" | ElMessage.error('账号已锁定，请 30 分钟后重试')；禁用登录按钮 30 分钟（可选） |
| **账号禁用** | code=403 + message 含"禁用" | ElMessage.error('账号已被禁用，请联系管理员') |
| **待审核讲师登录** | 登录成功 + status='pending' | 正常登录 → 首页顶部展示审核中通知条 |
| **网络断开** | axios catch | ElMessage.error('网络连接失败，请检查网络后重试') |
| **服务端 500** | code=500 | ElMessage.error('服务暂时不可用，请稍后重试') |
| **已登录重复访问** | 路由守卫 | `router.replace('/')` |

---

## 6. API 接口清单

| 序号 | 接口用途 | HTTP 方法 | 路径 | 调用函数名 | 入参 | 调用时机 |
|------|----------|-----------|------|------------|------|----------|
| 1 | 用户登录 | POST | `/api/v1/auth/login` | `login(data: LoginRequest)` | `{ login_id, password, remember_me }` | 点击登录按钮 / Enter |
| 2 | 刷新令牌 | POST | `/api/v1/auth/refresh` | `refreshToken(data: RefreshTokenRequest)` | `{ refresh_token }` | axios 拦截器 401 自动触发 |
| 3 | 退出登录 | POST | `/api/v1/auth/logout` | `logout()` | 无 | 导航栏点击退出（非本页，但逻辑关联） |

---

## 7. 补充设计要点

### 7.1 Token 管理策略

```typescript
// axios 请求拦截器
axios.interceptors.request.use((config) => {
  const token = userStore.accessToken
  if (token) {
    config.headers.Authorization = `Bearer ${token}`
  }
  return config
})

// axios 响应拦截器 - Token 自动刷新
let isRefreshing = false
let pendingRequests: Array<(token: string) => void> = []

axios.interceptors.response.use(
  (response) => response,
  async (error) => {
    const originalRequest = error.config
    if (error.response?.status === 401 && !originalRequest._retry) {
      if (isRefreshing) {
        // 排队等待刷新完成
        return new Promise((resolve) => {
          pendingRequests.push((token) => {
            originalRequest.headers.Authorization = `Bearer ${token}`
            resolve(axios(originalRequest))
          })
        })
      }
      originalRequest._retry = true
      isRefreshing = true
      try {
        const success = await userStore.refreshAccessToken()
        if (success) {
          const newToken = userStore.accessToken
          pendingRequests.forEach(cb => cb(newToken))
          pendingRequests = []
          originalRequest.headers.Authorization = `Bearer ${newToken}`
          return axios(originalRequest)
        }
      } finally {
        isRefreshing = false
      }
      // 刷新失败 → 清除登录态 → 跳转登录页
      userStore.clearLoginInfo()
      router.push({ path: '/login', query: { redirect: router.currentRoute.value.fullPath } })
    }
    return Promise.reject(error)
  }
)
```

### 7.2 路由守卫设计

```typescript
// router/guards.ts
router.beforeEach((to, from, next) => {
  const userStore = useUserStore()

  // 已登录用户不可访问登录/注册页
  if (userStore.isLoggedIn && ['Login', 'Register', 'ForgotPassword'].includes(to.name as string)) {
    return next({ path: '/' })
  }

  // 需要登录的页面
  if (to.meta.requiresAuth && !userStore.isLoggedIn) {
    return next({ path: '/login', query: { redirect: to.fullPath } })
  }

  // 角色权限检查
  if (to.meta.roles && !to.meta.roles.includes(userStore.role)) {
    ElMessage.error('无权限访问')
    return next({ path: '/' })
  }

  next()
})
```

### 7.3 响应式布局

| 屏幕宽度 | 布局方案 |
|----------|----------|
| ≥1280px | 左右分栏：左侧品牌区（40%）+ 右侧表单区（60%） |
| <1280px | 全屏表单居中，品牌区隐藏 |

### 7.4 防重复提交

```typescript
// 登录按钮防重复
// 1. isSubmitting 控制 el-button :loading 和 :disabled
// 2. 表单提交后立即 isSubmitting = true
// 3. 响应返回后 isSubmitting = false
// 4. 无论成功失败都在 finally 中恢复
```
