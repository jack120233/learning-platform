## 项目上下文摘要（前端对照速查表）
生成时间：2026-04-10

### 1. 相似实现分析
- **实现 1**: `docs/api-endpoint-inventory.md`
  - 模式：完整事实清单，按模块分节，逐接口列出方法、路径、登录、参数、请求体、返回体。
  - 可复用：12 个模块分组、80 个接口基线、认证判定、非标准 `POST` 备注、响应包装说明。
  - 需注意：该文档字段较多，包含处理函数、代码位置、测试文件，不适合前端速查。

- **实现 2**: `docs/api-testing-guide.md:1-486`
  - 模式：面向人工联调的轻量展示，偏向“接口路径 + 请求头 + 示例流程”。
  - 可复用：轻量章节组织方式、认证说明、适合快速扫读的展示节奏。
  - 需注意：它是手测指南，不是事实清单，不能反向覆盖路由代码事实。

- **实现 3**: `backend/app/schemas/common.py:15-31`
  - 模式：统一响应外层包装 `ApiResponse[T]`。
  - 可复用：前端速查表可统一说明响应外层固定为 `code/message/data`，分页接口统一补充 `PageData` 结构。
  - 需注意：部分接口 `data` 为空，少数接口 `data` 为 `dict`，需单独提示。

### 2. 项目约定
- **路径约定**: 统一前缀为 `/api/v1`。
- **认证约定**: `CurrentUserId` = 需要 Bearer Token；当前已挂载业务路由未发现 `OptionalUserId`。
- **响应约定**: 外层统一 `ApiResponse[T]`；分页为 `ApiResponse[PageData[T]]`。
- **文档约定**: 完整清单保留在 `docs/api-endpoint-inventory.md`；速查表应独立成文，避免污染原始盘点文档和手测指南。

### 3. 可复用组件清单
- `docs/api-endpoint-inventory.md`：唯一事实来源。
- `backend/app/api/v1/router.py:17-27`：11 个业务模块分组依据。
- `backend/app/core/dependencies.py:61-163`：登录要求判定依据。
- `backend/app/schemas/common.py:15-31`：统一响应结构说明。

### 4. 测试/验证策略
- 本次为文档压缩任务，不新增代码测试。
- 验证方式：
  - 速查表模块数应仍为 12。
  - 接口总数应仍为 80。
  - 公开/需登录统计应仍为 24/56。
  - 特殊说明需保留：非标准 `POST`、文案权限不等于显式 RBAC、`dict`/空 `data` 返回。

### 5. 依赖和集成点
- 路由注册链：`backend/app/main.py` → `backend/app/api/v1/router.py` → 各模块路由文件。
- 前端对照主字段：方法、路径、登录、核心请求、核心返回、备注。
- 应删除字段：处理函数、代码位置、测试文件。

### 6. 技术选型理由
- 采用独立速查表而非改写 `docs/api-endpoint-inventory.md`，避免破坏“大而全”的事实清单定位。
- 采用“模块总览 + 模块速查表 + 通用说明”结构，兼顾扫读效率和联调所需最少信息。

### 7. 关键风险点
- 多个接口文案写“管理员权限/讲师权限”，但路由层未见显式 RBAC。
- 存在大量 `POST` 承担更新/状态流转/删除语义的接口。
- 学习模块个别接口返回 `dict`，不适合前端依赖固定字段假设。
- `content.py` 无统一 `prefix`，但最终路径仍归入 `/api/v1/courses/...`。
