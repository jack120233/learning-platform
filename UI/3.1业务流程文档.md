# 在线视频学习网站业务流程文档

## 一、业务流程总览

本在线视频学习平台的核心业务围绕**用户注册登录**、**课程生产（讲师端）**、**课程学习（学员端）**、**后台管理（管理员端）**、**反馈与消息交互**五大模块展开，所有业务流程均基于平台角色权限（学员 / 讲师 / 管理员）划分，以下是各模块的详细业务流程说明。

---

## 二、用户注册与登录流程

### 2.1 学员注册流程

**适用角色**：未注册用户
**流程步骤**：

1. 未注册用户进入平台注册页面，填写以下信息：

    - 用户名（4-20 位，支持字母、数字、下划线）

    - 密码（8-20 位，包含字母 + 数字）

    - 确认密码（与密码一致）

    - 11 位中国大陆手机号

    - 页面生成的 4 位图形验证码

    - 邮箱账号（符合格式规范）

    - 邮箱收到的 6 位验证码

    - 选择「学员」角色

2. 系统自动校验：

    - 用户名、手机号、邮箱是否全局唯一
- 密码格式是否符合要求
    - 图形验证码是否正确
- 邮箱验证码是否正确且在10分钟内有效期中
    
3. 校验通过后，系统自动创建学员账号，返回用户 ID、用户名、角色信息，自动登录并跳转至平台首页。
**异常分支**：

- 用户名 / 手机号 / 邮箱已存在：提示「该用户名 / 手机号 / 邮箱已被使用」

- 密码格式不符：提示「密码需为 8-20 位字母 + 数字组合」

- 验证码错误 / 过期：提示「验证码错误或已过期，请重新获取」

- 网络异常：提示「网络连接失败，请稍后重试」

### 2.2 讲师注册与审核流程

**适用角色**：未注册用户
**流程步骤**：

1. 未注册用户进入注册页面，填写与学员注册一致的基础信息，选择「讲师」角色。

2. 系统校验基础信息通过后，创建**待审核讲师账号**，账号临时权限为学员，返回用户 ID、用户名及「待审核」状态提示,随后自动登录并跳转至普通学员的首页（此时仅拥有学员权限）。

3. 系统自动向管理员发送讲师审核提醒，管理员需在 7 天内完成审核：

    - 审核通过：账号权限升级为讲师，系统发送站内消息通知用户。

    - 审核驳回：系统发送站内消息告知驳回原因，用户可修改资料重新提交审核。

4. 7 天未审核时，系统自动提醒管理员处理。
**异常分支**：

- 审核驳回后用户重新提交：需重新填写资料，再次进入审核流程。

- 注册信息校验不通过：同学员注册异常提示。

### 2.3 管理员注册流程

**适用角色**：未注册用户
**流程步骤**：

1. 未注册用户进入注册页面，填写基础信息，选择「管理员」角色，额外填写**已存在的管理员邮箱**作为推荐凭证。

2. 系统校验基础信息通过后，校验推荐的管理员邮箱是否为有效管理员账号。

3. 校验通过后，系统发送审核请求给推荐的管理员，待其审核通过后，创建管理员账号。
**异常分支**：

- 推荐管理员邮箱无效：提示「推荐管理员账号不存在，请确认后重新输入」

- 审核被驳回：提示「管理员注册审核未通过，无法创建账号」

### 2.4 用户登录流程

**适用角色**：已注册用户
**流程步骤**：

1. 已注册用户进入登录页面，选择登录方式（邮箱 / 手机号二选一），填写密码，可选「记住我」选项。

2. 系统校验账号是否存在、密码是否正确，校验密码连续错误次数（连续 5 次错误则锁定账号 30 分钟）。

3. 校验通过后，系统生成 JWT 访问令牌（有效期 24 小时）和刷新令牌（勾选「记住我」则有效期 30 天，否则 7 天），记录登录时间和 IP 地址，跳转至对应角色首页。
**异常分支**：

- 账号不存在 / 密码错误：提示「账号不存在或密码错误」

- 账号被锁定：提示「账号已锁定，请 30 分钟后重试」

- 账号被禁用：提示「账号已被禁用，请联系管理员」

- 令牌过期：自动使用刷新令牌获取新令牌，失败则跳转至登录页。

### 2.5 密码找回与修改流程

#### 2.5.1 密码找回流程

**适用角色**：已注册且未登录用户
**流程步骤**：

1. 用户进入密码找回页面，选择通过邮箱或手机号找回：

    - 选择邮箱：输入绑定邮箱，系统发送验证码至该邮箱。

    - 选择手机号：系统脱敏显示绑定邮箱，发送验证码至该邮箱。

2. 用户输入收到的验证码，校验通过后，进入新密码设置页面，输入两次新密码。

3. 系统校验新密码格式符合要求且两次输入一致后，更新密码，跳转至登录页。
**异常分支**：

- 邮箱 / 手机号未注册：提示「该邮箱 / 手机号未注册账号」

- 验证码错误 / 过期：提示「验证码错误或已过期」

- 新密码格式不符：提示「密码需为 8-20 位字母 + 数字组合」

#### 2.5.2 密码修改流程

**适用角色**：已登录用户
**流程步骤**：

1. 已登录用户进入个人中心 - 密码修改页面，输入原密码、新密码、确认新密码。

2. 系统校验原密码正确、新密码格式符合要求、新密码与原密码不同且两次输入一致。

3. 校验通过后，系统更新密码，清除当前登录凭证，强制用户重新登录。
**异常分支**：

- 原密码错误：提示「原密码错误，请重新输入」

- 新密码与原密码一致：提示「新密码不能与原密码相同」

- 两次新密码输入不一致：提示「两次输入的新密码不一致」

---

## 三、讲师课程管理流程

### 3.1 课程创建流程

**适用角色**：已审核通过的讲师、管理员
**流程步骤**：

1. 讲师 / 管理员登录后，进入课程管理页面，点击「创建课程」。

2. 填写课程基础信息：

    - 课程标题（2-100 字符）

    - 课程封面（JPG/PNG 格式，≤10MB，建议 16:9 尺寸）

    - 课程简介（10-500 字符）

    - 课程描述（可选，富文本，可上传 PDF/PPT/Word 文档）

    - 课程分类（选择已有分类或创建新分类）

    - 课程标签（最多 5 个，可选创建新标签）

    - 配套资料（可选，PDF/DOC/ZIP 格式，≤1GB）

3. 提交后，系统自动生成课程 ID，课程状态为「草稿」，跳转至课程编辑页面。
**异常分支**：

- 封面上传失败：提示「封面上传失败，请重试」

- 必填项未填写：高亮未填写项，提示「请完善必填信息」

- 文件格式 / 大小不符合要求：提示「不支持该文件格式 / 文件大小超出限制」

### 3.2 课程目录管理流程

**适用角色**：课程创建讲师、管理员
**流程步骤**：

1. 讲师 / 管理员进入课程编辑页面，进行目录结构管理：

    - 添加章节：填写章节名称（2-50 字符），设置排序（默认按创建顺序）。

    - 添加小节：选择所属章节，填写小节名称（2-50 字符），设置排序。

    - 调整顺序：拖拽章节 / 小节调整展示顺序。

2. 课程至少包含 1 个章节；学习资源可直接挂在章节下，也可挂在小节下（视频 / 音频 / PPT / 图片）。

3. 删除章节 / 小节时，系统弹出二次确认弹窗，确认后级联删除下属小节 / 资源。
**异常分支**：

- 删除包含资源的章节 / 小节：提示「删除后下属内容将一并删除，是否确认？」

- 未添加章节 / 资源：提示「课程至少需包含 1 个章节和 1 个学习资源」

### 3.3 课程资源上传流程

**适用角色**：课程创建讲师、管理员
**流程步骤**：

1. 讲师 / 管理员进入章节或小节编辑页面，选择资源类型（视频 / 音频 / PPT / 图片），上传对应文件：

    - 视频：MP4 格式，≤5GB，支持分片上传、断点续传。

    - 音频：MP3 格式，≤2GB，支持分片上传、断点续传。

    - PPT/PDF/word：≤200MB；图片：JPG/PNG 格式，≤10MB。

2. 系统实时显示上传进度，上传完成后自动提取文件信息（视频时长、分辨率等），生成资源 ID 和文件 URL。

3. 资源上传后，可进行替换或删除操作（删除需二次确认）。
**异常分支**：

- 文件格式不支持：提示「不支持该文件格式，请上传指定格式文件」

- 文件大小超出限制：提示「文件大小超出限制，最大支持 XX」

- 上传中断：支持断点续传，提示「上传中断，可继续上传」

- 存储空间不足：提示「存储空间不足，请联系管理员」

### 3.4 课程发布流程

**适用角色**：课程创建讲师、管理员
**流程步骤**：

1. 讲师 / 管理员确认课程已满足发布条件：

    - 课程状态为「草稿」

    - 至少包含 1 个章节、1 个学习资源

    - 课程基础信息（标题、封面、简介）完整

2. 点击「发布课程」，系统将课程状态从「草稿」变更为「已发布」，记录发布时间。

3. 发布后课程立即对所有学员可见，讲师仍可编辑课程内容（不影响发布状态）。
**异常分支**：

- 课程内容不完整：提示「课程内容不完整，请至少添加 1 个章节和 1 个学习资源」

- 课程已发布：提示「课程已处于发布状态」

### 3.5 课程下架流程

**适用角色**：课程创建讲师、管理员
**流程步骤**：

1. 讲师 / 管理员进入已发布课程的管理页面，点击「下架课程」或勾选多门课程进行批量下架，填写下架原因（10-200 字符）。

2. 系统校验课程状态为「已发布」，校验下架原因符合长度要求。

3. 校验通过后，将课程状态变更为「已下架」，记录下架时间和下架原因，下架后课程对学员不可见，但讲师仍可编辑。
**异常分支**：

- 课程未发布：提示「课程未处于发布状态，无需下架」

- 下架原因未填写：提示「请填写下架原因」

- 权限不足：提示「无权限执行此操作」

### 3.6 课程编辑流程

**适用角色**：课程创建讲师、管理员
**流程步骤**：

1. 讲师 / 管理员进入课程管理页面，选择状态为「草稿 / 已发布 / 已下架」的课程，点击「编辑课程」。

2. 可修改课程基础信息（标题、简介、封面等）、目录结构（章节 / 小节排序、新增 / 删除）、学习资源（上传 / 替换 / 删除）。

3. 编辑完成后点击「保存」，系统自动更新课程信息，已发布课程编辑后学员端实时同步最新内容。
**异常分支**：

- 课程不存在：提示「该课程不存在或已被删除」

- 权限不足：提示「无权限编辑该课程」

- 网络异常：提示「课程编辑失败，请稍后重试」

### 3.7 课程删除流程

**适用角色**：课程创建讲师、管理员
**流程步骤**：

1. 讲师 / 管理员进入课程管理页面，选择状态为「草稿 / 已下架」的课程，点击「删除课程」。

2. 系统弹出二次确认弹窗，删除状态为已下架课程时，提示「删除后课程及所有内容将永久清除，是否确认？」。删除状态为草稿时，提示「草稿一旦删除不可恢复，是否确认？」

3. 用户确认后，系统删除课程及下属所有章节、小节、学习资源，记录删除人及删除时间。
**异常分支**：

- 课程为已发布状态：提示「已发布课程需先下架，方可删除」

- 权限不足：提示「无权限执行此操作」

---

## 四、学员课程学习流程

### 4.1 课程浏览与搜索流程

**适用角色**：学员、讲师、游客
**流程步骤**：

1. 用户（游客 / 已登录用户）进入平台首页，默认展示「已发布」状态的课程列表，按最新发布时间倒序排列。

2. 用户可进行以下操作：

    - 搜索：输入关键词（课程标题、讲师名称）进行模糊搜索，支持空格分词，搜索结果按相关度排序，关键词高亮显示。
- 筛选：选择课程分类进行筛选。
    - 排序：选择「最新发布 / 最多学习」排序方式。
- 系统**自动记录用户个人的最近10条搜索历史**，并在后台**统计全局热门搜索词**供默认展示/推荐使用。
    
3. 课程列表分页展示，每页 20 条，点击课程卡片可进入课程详情页。
**异常分支**：

- 无课程数据：提示「暂无课程，敬请期待」

- 搜索无结果：提示「未找到相关课程」

- 网络异常：提示「课程列表加载失败，请稍后重试」

### 4.2 课程详情查看流程

**适用角色**：已登录学员
**流程步骤**：

1. 已登录学员点击课程卡片，进入课程详情页。

2. 系统展示课程完整信息：标题、封面、讲师、简介、目录结构（章节 - 小节 - 资源）、配套资料下载入口、反馈入口。

3. 系统自动记录课程访问次数，学员可点击「开始学习」或「继续学习」（如有学习记录）进入课程学习页面。
**异常分支**：

- 课程不存在 / 已下架：提示「该课程不存在或已下架」

- 未登录：提示「请先登录，再查看课程详情」

- 网络异常：提示「课程详情加载失败，请稍后重试」

### 4.3 课程学习流程

#### 4.3.1 视频学习流程

**适用角色**：已登录学员
**流程步骤**：

1. 学员进入课程学习页面，选择包含视频资源的小节，点击播放视频。

2. 系统自动加载视频，支持全屏播放、进度调整，每 30 秒自动保存一次播放进度。

3. 视频播放完毕后，自动跳转至下一小节，标记当前小节为「已完成」。

4. 下次进入该课程时，自动从上次保存的进度开始播放。
**异常分支**：

- 视频文件不存在：提示「视频文件不存在」

- 视频加载失败：提示「视频加载失败，请刷新重试」

- 网络中断：自动暂停视频，提示「网络连接中断」

#### 4.3.2 音频学习流程

**适用角色**：已登录学员
**流程步骤**：

1. 学员进入课程学习页面，选择包含音频资源的小节，点击播放音频。

2. 系统自动加载音频，支持播放 / 暂停，每 30 秒自动保存一次播放进度。

3. 音频播放完毕后，自动跳转至下一小节，标记当前小节为「已完成」。

4. 下次进入该课程时，自动从上次保存的进度开始播放。
**异常分支**：

- 音频文件不存在：提示「音频文件不存在」

- 音频加载失败：提示「音频加载失败，请刷新重试」

- 网络中断：自动暂停音频，提示「网络连接中断」

#### 4.3.3 课件 / 图片学习流程

**适用角色**：已登录学员
**流程步骤**：

1. 学员进入课程学习页面，选择包含课件（PPT/PDF/Word）或图片的小节，点击预览。

2. 课件支持翻页、跳转指定页码、全屏预览、下载；图片支持放大 / 缩小 / 旋转 / 切换（多张）、全屏预览。

3. 打开课件 / 图片后，系统自动标记该小节为「已完成」。
**异常分支**：

- 课件 / 图片不存在：提示「文件不存在」

- 文件格式不支持：提示「不支持该文件格式」

- 加载失败：提示「文件加载失败，请刷新重试」

### 4.4 学习记录查看流程

**适用角色**：已登录学员、讲师、管理员
**流程步骤**：

1. 已登录用户进入个人中心 -「我的学习记录」页面。

2. 系统展示当前用户的所有学习记录，包含课程名称、最近学习时间、学习进度，按最近学习时间倒序排列。

3. 用户可筛选学习时间（近 7 天 / 近 30 天 / 全部），点击「继续学习」可直接跳转至上次学习的小节位置。
**异常分支**：

- 无学习记录：提示「暂无学习记录，快去学习课程吧」

- 网络异常：提示「学习记录加载失败，请稍后重试」

- 课程已下架 / 删除：点击继续学习时提示「该课程已下架或删除，无法继续学习」

---

## 五、管理员后台管理流程

### 5.1 用户管理流程

**适用角色**：管理员
**流程步骤**：

1. 管理员登录后台，进入「用户管理」页面，可进行以下操作：

    - 筛选：按用户类型（学员 / 讲师 / 管理员 / 待审核讲师）、账号状态（正常 / 禁用）筛选。

    - 搜索：按邮箱、手机号搜索用户。

    - 操作：查看用户详情、禁用 / 启用账号、删除账号、审核待审核讲师。

2. 审核待审核讲师时，可选择「通过审核」（升级为讲师权限）或「驳回审核」（发送站内消息告知原因），管理员可操作自动审核开关，开启后新注册讲师将自动通过审核，无需人工干预。

3. 禁用账号后，用户无法登录；启用账号后，用户恢复正常使用；删除账号需二次确认，删除学员则级联清理学习/反馈数据；删除讲师账号时，级联删除其学习/反馈记录，但保留该讲师已发布的课程数据）。
**异常分支**：

- 用户不存在：提示「该用户不存在或已被删除」

- 权限不足：提示「无权限执行此操作」

- 网络异常：提示「用户管理操作失败，请稍后重试」

### 5.2 角色权限管理流程

**适用角色**：管理员
**流程步骤**：

1. 管理员登录后台，进入「角色权限管理」页面，选择角色类型（学员 / 讲师 / 管理员）。

2. 勾选对应权限项（学员：课程学习、反馈提交；讲师：课程管理 + 学员权限；管理员：用户管理、权限管理等）。

3. 提交后，系统更新角色权限配置，权限变更立即生效，记录权限变更日志。
**异常分支**：

- 权限配置冲突：提示「权限配置存在冲突，请重新设置」

- 保存失败：提示「权限配置保存失败，请稍后重试」

### 5.3 公告管理流程

**适用角色**：管理员
**流程步骤**：

1. 管理员登录后台，进入「公告管理」页面，可进行以下操作：

    - 新增公告：填写公告标题、内容，选择发布状态（草稿 / 已发布），提交后系统保存公告。

    - 编辑公告：选择草稿 / 已发布公告，修改标题、内容或发布状态，提交后实时更新前端展示。

    - 删除公告：选择任意状态公告，点击删除，二次确认后永久删除公告，前端同步清除。

2. 公告列表默认按发布时间倒序排列，记录公告操作日志（操作人、时间、类型）。
**异常分支**：

- 公告标题 / 内容为空：提示「公告标题和内容为必填项」

- 公告不存在：提示「该公告不存在或已被删除」

- 网络异常：提示「公告操作失败，请稍后重试」

### 5.4 反馈管理流程

**适用角色**：管理员
**流程步骤**：

1. 管理员登录后台，进入「反馈管理」页面，可进行以下操作：

    - 筛选：按反馈类型（系统问题 / 课程问题）、处理状态（待处理 / 已处理）、时间范围筛选。

    - 搜索：按反馈内容、用户名搜索。

    - 操作：查看反馈详情（内容、截图）、标记为「已处理」、批量操作。

2. 反馈列表默认按提交时间倒序排列，处理后系统自动向用户发送反馈结果通知。
**异常分支**：

- 无反馈记录：提示「暂无反馈记录」

- 反馈不存在：提示「该反馈不存在或已被删除」

- 网络异常：提示「反馈管理操作失败，请稍后重试」

---

## 六、反馈与消息流程

### 6.1 用户反馈提交流程

**适用角色**：已登录学员、讲师
**流程步骤**：

1. 已登录用户在课程详情页或个人中心页，点击「提交反馈」。

2. 选择反馈类型：

    - 课程详情页：默认选择「课程问题」，自动关联当前课程 ID。

    - 个人中心页：默认选择「系统问题」。

3. 填写反馈内容（10-500 字符），可选上传截图（最多 8 张，JPG/PNG 格式，单张≤5MB）。

4. 提交后，系统记录反馈时间、用户信息、课程信息（课程问题），反馈状态为「待处理」，同一用户 24 小时内最多提交 10 条反馈。
**异常分支**：

- 反馈内容为空 / 过短：提示「反馈内容至少 10 个字符」

- 截图格式 / 大小不符：提示「仅支持 JPG/PNG 格式，单张图片最大 5MB」

- 提交过于频繁：提示「提交过于频繁，请稍后再试」

### 6.2 管理员反馈处理流程

**适用角色**：管理员
**流程步骤**：

1. 管理员进入后台反馈管理页面，查看待处理反馈，点击查看详情（内容、截图、关联课程 / 用户信息）。

2. 处理完成后，点击「标记为已处理」，系统更新反馈状态，并向用户发送站内消息告知处理结果。
**异常分支**：

- 反馈已处理：提示「该反馈已处理，无需重复操作」

- 网络异常：提示「反馈处理失败，请稍后重试」

### 6.3 系统消息推送流程

**适用角色**：系统自动执行
**流程步骤**：

1. 系统触发消息推送条件：

    - 系统通知：系统维护、功能更新等。

    - 反馈结果通知：反馈处理完成后。

    - 审核结果通知：讲师审核通过 / 驳回后。

2. 系统自动生成消息标题（≤50 字符）、内容（≤500 字符），选择推送方式（站内消息必选，邮件可选）。

3. 系统向目标用户推送消息，记录发送时间和阅读时间，消息保存 60 天，过期自动删除。
**异常分支**：

- 用户不存在：跳过推送该用户。
- 邮件服务异常：仅推送站内消息。
- 推送频率过高：限制同类消息推送间隔至少 5 分钟。

### 6.4 用户查看与管理消息流程`

**适用角色**：系统自动执行
**流程步骤**：

1.用户登录后进入消息中心：

- 查看消息列表（展示未读数量）

-  支持按类型/状态筛选 
-  点击消息标记已读/批量已读/删除。

**异常分支**：

- 消息不存在或已删除提示。


---

## 七、其他核心流程

### 7.1 个人信息管理流程

**适用角色**：已登录所有用户
**流程步骤**：

1. 已登录用户进入个人中心 -「个人基本信息」页面，可修改昵称、邮箱、手机号。

2. 修改邮箱 / 手机号时，需输入对应验证码进行身份校验。

3. 系统校验昵称格式、邮箱 / 手机号唯一性，校验通过后更新信息，记录操作日志。
**异常分支**：

- 昵称格式违规：提示「昵称包含违规字符或长度超出限制」

- 邮箱 / 手机号已被占用：提示「该邮箱 / 手机号已被其他账号绑定」

- 验证码错误：提示「验证码无效或已过期」

### 7.2 退出登录流程

**适用角色**：已登录所有用户
**流程步骤**：

1. 已登录用户点击页面右上角「退出登录」，系统弹出确认弹窗。

2. 用户确认后，系统清除本地登录凭证（Cookie、Session、Token），跳转至平台首页，记录退出操作日志。
**异常分支**：

- 未登录：提示「您尚未登录，无需退出」

- 网络异常：提示「退出登录失败，请稍后重试」
> （注：文档部分内容可能由 AI 生成）
