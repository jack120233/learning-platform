# 前端页面详细设计说明书 - 6.1 首页 / 课程门户页

> 文档版本：v1.0 | 生成时间：2026-03-24 | 技术栈：Vue 3 + TypeScript + Element Plus + Pinia

---

## 1. 页面概述

| 属性 | 说明 |
|------|------|
| 页面名称 | 首页 / 课程门户页 |
| 路由地址 | `/` |
| 路由名称 | `Home` |
| 路由文件 | `src/views/home/HomePage.vue` |
| 访问权限 | 公开（游客 / 学员 / 讲师 / 管理员 均可访问） |
| 页面目标 | 课程发现入口，支持浏览、搜索、筛选，引导用户进入课程详情 |

---

## 2. UI 组件结构图

```
HomePage.vue
├── AppHeader.vue                          ← 全局导航栏（布局组件）
│   ├── LogoSection                        ← Logo + 平台名称
│   ├── MainNav                            ← 主导航菜单 (el-menu)
│   │   └── el-menu-item × N              ← 首页、职业培训课堂、考培活动...
│   ├── QuickEntries                       ← 学院课程快捷入口
│   ├── SearchTrigger                      ← 搜索图标按钮
│   └── UserArea                           ← 用户区域
│       ├── [未登录] LoginRegisterBtns     ← 登录 / 注册按钮
│       └── [已登录] UserDropdown          ← 头像 + el-dropdown
│           ├── 个人中心
│           ├── 我的学习
│           ├── 消息中心 (Badge 未读数)
│           ├── [讲师] 课程管理
│           ├── [管理员] 后台管理
│           └── 退出登录
│
├── BannerCarousel.vue                     ← Banner 轮播区
│   └── el-carousel
│       └── el-carousel-item × N          ← 焦点图卡片（封面 + 标题 + 链接）
│
├── SearchFilterBar.vue                    ← 搜索 + 筛选区
│   ├── SearchInput                        ← el-input + el-button（搜索框）
│   │   ├── SearchHistory                  ← 搜索历史下拉（最近 10 条）
│   │   └── HotKeywords                   ← 热门搜索词标签
│   ├── CategoryFilter                     ← 分类筛选 (el-radio-group / el-tabs)
│   └── SortSelector                       ← 排序切换：最新发布 / 最多学习
│
├── CourseListSection.vue                  ← 课程列表区
│   ├── [加载中] SkeletonCardGrid         ← 骨架屏 (el-skeleton × 8)
│   ├── [空状态] EmptyState               ← 无课程提示 + 插画
│   ├── [错误] ErrorState                 ← 错误提示 + 重试按钮
│   └── [正常] CourseCardGrid
│       └── CourseCard.vue × N             ← 课程卡片
│           ├── CoverImage                 ← 课程封面 (el-image，懒加载)
│           ├── CourseTitle                ← 课程名称（最多两行省略）
│           ├── TeacherName               ← 讲师名称
│           ├── CourseSummary             ← 课程简介摘要（可选）
│           └── MetaInfo                   ← 发布时间
│
├── PaginationBar.vue                      ← 分页区
│   └── el-pagination                      ← 分页器（total / page-size / current-page）
│
└── AppFooter.vue                          ← 全局页脚
    ├── FooterLinks                        ← 底部导航链接
    ├── ContactInfo                        ← 联系信息
    └── Copyright                          ← 版权声明
```

---

## 3. 数据模型 (TypeScript)

### 3.1 API 响应类型

```typescript
/** 通用 API 响应结构 */
interface ApiResponse<T> {
  code: number
  message: string
  data: T
}

/** 分页响应结构 */
interface PaginatedData<T> {
  items: T[]
  total: number
  page: number
  page_size: number
  total_pages: number
}

/** 首页课程列表项 */
interface HomeCourseItem {
  course_id: number
  title: string
  cover_url: string
  summary: string
  teacher_name: string
  published_at: string
}

/** 课程列表项（通用，含更多字段） */
interface CourseListItem {
  course_id: number
  title: string
  cover_url: string
  thumbnail_url: string
  summary: string
  teacher_id: number
  teacher_name: string
  category_id: number
  category_name: string
  view_count: number
  published_at: string
}

/** 课程搜索结果项 */
interface CourseSearchItem {
  course_id: number
  title: string
  cover_url: string
  summary: string
  teacher_name: string
  highlight: {
    title?: string
  }
}

/** 课程分类 */
interface Category {
  category_id: number
  name: string
  parent_id: number | null
  icon_url?: string
  sort_order: number
  is_enabled: boolean
  children?: Category[]
}

/** Banner 轮播项（前端自定义，可来自公告或配置） */
interface BannerItem {
  id: number
  image_url: string
  title: string
  link_url: string
  link_type: 'course' | 'announcement' | 'external'
}
```

### 3.2 请求参数类型

```typescript
/** 首页课程列表请求参数 */
interface HomepageCoursesParams {
  page: number
  page_size: number
}

/** 课程列表请求参数 */
interface CourseListParams {
  keyword?: string
  category_id?: number
  sort_by?: 'latest' | 'popular'
  page: number
  page_size: number
}

/** 课程搜索请求参数 */
interface CourseSearchParams {
  q: string
  page: number
  page_size: number
}
```

---

## 4. 状态管理逻辑

### 4.1 Pinia State（全局状态）

#### `useUserStore` - 用户状态（跨页面共享）

```typescript
interface UserState {
  userId: number | null
  username: string
  email: string
  nickname: string
  avatarUrl: string
  role: 'student' | 'teacher' | 'admin' | null
  status: 'active' | 'disabled' | 'pending' | null
  accessToken: string
  refreshToken: string
  isLoggedIn: boolean
  unreadMessageCount: number
}
```

首页依赖 `useUserStore` 的字段：
- `isLoggedIn` → 控制导航栏登录/注册按钮 vs 用户头像下拉
- `role` → 控制下拉菜单中"课程管理"（讲师）和"后台管理"（管理员）的显示
- `status` → 待审核讲师提示
- `nickname` / `avatarUrl` → 头像展示
- `unreadMessageCount` → 消息中心 Badge 未读数

#### `useCategoryStore` - 分类状态（全局缓存）

```typescript
interface CategoryState {
  categories: Category[]
  isLoaded: boolean
}
```

首页和课程列表页共享分类数据，首次加载后缓存。

### 4.2 Local State（组件私有状态）

```typescript
/** HomePage.vue 组件内部状态 */
interface HomePageState {
  // 课程列表
  courseList: HomeCourseItem[]
  isLoading: boolean
  isError: boolean
  errorMessage: string

  // 分页
  currentPage: number
  pageSize: number       // 固定 20
  totalCount: number

  // 搜索与筛选
  searchKeyword: string
  selectedCategoryId: number | null
  sortBy: 'latest' | 'popular'
  isSearchMode: boolean  // 当前是否为搜索结果展示

  // 搜索历史
  searchHistory: string[]   // 最近 10 条，存 localStorage

  // Banner
  bannerList: BannerItem[]

  // 搜索结果（搜索模式下使用）
  searchResults: CourseSearchItem[]
}
```

---

## 5. 核心业务逻辑与交互

### 5.1 初始化流程 (onMounted)

```
onMounted
 ├─ 1. 加载分类数据（if !useCategoryStore().isLoaded）
 │    └─ GET /api/v1/categories
 ├─ 2. 加载 Banner 数据（静态配置或公告接口）
 │    └─ GET /api/v1/announcements?status=published
 ├─ 3. 加载首页课程列表
 │    └─ GET /api/v1/courses/homepage?page=1&page_size=20
 ├─ 4. 从 localStorage 恢复搜索历史
 │    └─ JSON.parse(localStorage.getItem('search_history'))
 └─ 5. 如果已登录，获取未读消息数
      └─ GET /api/v1/messages/unread-count
```

### 5.2 用户交互处理

#### 5.2.1 搜索交互

| 操作 | 处理逻辑 |
|------|----------|
| 输入关键词并提交 | 1. 校验非空；2. 切换为搜索模式 `isSearchMode=true`；3. 调用 `GET /api/v1/courses/search?q=xxx`；4. 保存关键词到搜索历史（去重，最多10条）；5. 重置分页为第1页 |
| 点击搜索历史项 | 填充到搜索框并触发搜索 |
| 清空搜索框 | 退出搜索模式，恢复首页课程列表 |
| 点击热门搜索词 | 填充到搜索框并触发搜索 |
| 清除搜索历史 | `localStorage.removeItem('search_history')` 并清空列表 |

#### 5.2.2 分类筛选

| 操作 | 处理逻辑 |
|------|----------|
| 选择分类 | 1. 更新 `selectedCategoryId`；2. 切换为列表模式（非搜索模式）；3. 调用 `GET /api/v1/courses?category_id=xxx`；4. 重置分页为第1页 |
| 选择"全部" | 1. 清空 `selectedCategoryId`；2. 恢复首页课程列表 |

#### 5.2.3 排序切换

| 操作 | 处理逻辑 |
|------|----------|
| 切换排序方式 | 1. 更新 `sortBy`；2. 重新请求当前列表（保留筛选条件）；3. 重置分页为第1页 |

#### 5.2.4 分页

| 操作 | 处理逻辑 |
|------|----------|
| 切换页码 | 1. 更新 `currentPage`；2. 根据当前模式（首页/搜索/筛选）调用对应接口；3. 滚动到列表顶部 |
| 切换每页条数 | 1. 更新 `pageSize`；2. 重置 `currentPage=1`；3. 重新请求 |

#### 5.2.5 课程卡片

| 操作 | 处理逻辑 |
|------|----------|
| 点击课程卡片 | `router.push({ name: 'CourseDetail', params: { courseId } })` |
| 封面加载失败 | 展示默认占位图 |

#### 5.2.6 导航栏交互

| 操作 | 处理逻辑 |
|------|----------|
| 点击"登录" | `router.push('/login')` |
| 点击"注册" | `router.push('/register')` |
| 点击"个人中心" | `router.push('/profile')` |
| 点击"我的学习" | `router.push('/profile/records')` |
| 点击"消息中心" | `router.push('/profile/messages')` |
| 点击"课程管理" | `router.push('/teacher/courses')` |
| 点击"后台管理" | `router.push('/admin/users')` |
| 点击"退出登录" | ElMessageBox.confirm → 调用 `POST /api/v1/auth/logout` → 清除 store 和 localStorage → 跳转首页 |

### 5.3 异常场景处理

| 异常场景 | 处理策略 |
|----------|----------|
| **网络断开** | `isError=true`，展示"网络连接失败，请稍后重试" + 重试按钮 |
| **接口超时** | 同上，axios 设置 15s 超时 |
| **Token 过期 (401)** | axios 拦截器自动尝试 `POST /api/v1/auth/refresh`；刷新失败 → 清除登录态 → 首页保持可用（公开接口无需 Token） |
| **服务端 500** | `isError=true`，展示"服务暂时不可用，请稍后重试" |
| **课程列表为空** | `courseList.length === 0` 时展示 EmptyState 组件："暂无课程，敬请期待" |
| **搜索无结果** | 展示："未找到相关课程，换个关键词试试" |
| **分类加载失败** | 隐藏分类筛选区，不影响课程列表展示 |
| **Banner 加载失败** | 隐藏 Banner 区域，静默降级 |

---

## 6. API 接口清单

| 序号 | 接口用途 | HTTP 方法 | 路径 | 调用函数名 | 入参 | 调用时机 |
|------|----------|-----------|------|------------|------|----------|
| 1 | 首页课程列表 | GET | `/api/v1/courses/homepage` | `fetchHomepageCourses(params: HomepageCoursesParams)` | `{ page, page_size }` | onMounted / 翻页 |
| 2 | 课程列表（筛选） | GET | `/api/v1/courses` | `fetchCourseList(params: CourseListParams)` | `{ keyword?, category_id?, sort_by?, page, page_size }` | 分类筛选 / 排序切换 |
| 3 | 课程搜索 | GET | `/api/v1/courses/search` | `searchCourses(params: CourseSearchParams)` | `{ q, page, page_size }` | 搜索提交 |
| 4 | 分类列表 | GET | `/api/v1/categories` | `fetchCategories()` | 无 | onMounted（首次） |
| 5 | 未读消息数 | GET | `/api/v1/messages/unread-count` | `fetchUnreadCount()` | 无 | onMounted（已登录） |
| 6 | 公告列表（Banner） | GET | `/api/v1/announcements` | `fetchAnnouncements(params)` | `{ status: 'published', page: 1, page_size: 5 }` | onMounted |
| 7 | 退出登录 | POST | `/api/v1/auth/logout` | `logout()` | 无 | 用户点击退出 |

---

## 7. 补充设计要点

### 7.1 性能优化

- **课程封面懒加载**：`<el-image lazy>` 或 Intersection Observer
- **路由懒加载**：`() => import('@/views/home/HomePage.vue')`
- **分类数据缓存**：首次加载后存入 Pinia，后续页面直接读取
- **搜索防抖**：输入框增加 300ms 防抖，避免频繁请求
- **骨架屏**：课程列表加载时展示 8 个骨架卡片，避免布局抖动

### 7.2 响应式布局

| 屏幕宽度 | 课程卡片列数 | 布局调整 |
|----------|-------------|----------|
| ≥1920px | 5 列 | 标准布局 |
| 1440-1919px | 4 列 | 标准布局 |
| 1280-1439px | 3 列 | 搜索栏宽度缩减 |
| <1280px | 2 列 | Banner 高度降低，导航栏收起为 Drawer |

### 7.3 搜索历史存储

```typescript
// localStorage key: 'edu_search_history'
// 存储格式: string[]（最多 10 条）
// 新增时去重并 unshift 到首位
// 超过 10 条时 pop 末尾

function addSearchHistory(keyword: string): void {
  const history = getSearchHistory()
  const filtered = history.filter(item => item !== keyword)
  filtered.unshift(keyword)
  if (filtered.length > 10) filtered.pop()
  localStorage.setItem('edu_search_history', JSON.stringify(filtered))
}
```
