# 优化资源上传计划

## 一、文档目的

本文档按**当前项目实际后端接口**重新适配，并明确区分：

1. 已实现接口
2. 缺失但必须补齐的接口
3. 页面各区域应该对应哪一类后端能力

本文档不再采用原先的 `/api/upload/doc`、`/upload/common`、`/upload/video` 这类与当前项目不一致的接口命名，而是统一对齐当前项目现有接口风格：

- `/api/v1/upload/*`
- `/api/v1/courses/*`

---

## 二、当前项目后端接口现状

### 1. 已实现接口

#### 1.1 小文件上传接口

`POST /api/v1/upload/file`

当前代码中该接口**已经存在**，但目前只实现了“课程封面图片上传”能力，还不是完整的通用小文件上传接口。

当前实现特征：

- 路由已存在
- 只接收 `multipart/form-data`
- 当前只支持课程封面图片
- 当前只允许 `jpg / jpeg / png`
- 当前实际返回 `file_name`、`file_url`、`url`、`file_size`、`content_type`

结论：

- 该接口可以作为后续“小文件上传接口”的基础
- 但如果要支撑课程资料、PPT、PDF、Word、ZIP 等上传，必须扩展其文件类型和业务语义

#### 1.2 课程资料绑定接口

`POST /api/v1/courses/{course_id}/materials`

该接口**已经实现**，但它不是文件上传接口，而是“课程资料记录创建接口”。

当前职责：

- 为课程新增一条资料记录
- 接收前端传入的 `name`、`file_url`、`file_size`，以及可选的 `file_type`

结论：

- 该接口应继续保留
- 它负责“绑定资料到课程”
- 它不负责接收文件二进制

#### 1.3 小节资源绑定接口

`POST /api/v1/courses/{course_id}/sections/{section_id}/resources`

该接口**已经实现**，但它也不是文件上传接口，而是“小节资源记录创建接口”。

当前职责：

- 为指定小节新增一条学习资源记录
- 接收前端传入的 `title`、`type`、`file_url`、`file_size`、`duration`、`sort_order`、`is_free`

当前资源类型限制：

- `video`
- `document`
- `quiz`

结论：

- 该接口应继续保留
- 它负责“绑定资源到小节”
- 它不负责接收文件二进制

### 2. 当前缺失接口

以下接口在当前项目中**尚未实现**，但如果页面要完成资源上传闭环，后续必须开发：

#### 2.1 通用文档/小文件上传能力

建议沿用当前项目已有接口路径，扩展为：

`POST /api/v1/upload/file`

需要补充支持：

- `pdf`
- `doc`
- `docx`
- `ppt`
- `pptx`
- `xls`
- `xlsx`
- `csv`
- `md`
- `zip`

说明：

- 当前代码里这个接口只支持封面图
- 后续开发时要把它升级成真正的“小文件上传接口”
- 如果不希望影响现有封面逻辑，也可以新开 `POST /api/v1/upload/document`，但从当前项目已有接口命名看，优先建议扩展 `/api/v1/upload/file`

#### 2.2 大文件初始化接口

`POST /api/v1/upload/init`

用途：

- 初始化大文件上传任务
- 返回 `upload_id`
- 返回分片大小、总片数等信息

适用文件：

- `mp4`
- 大 `zip`

#### 2.3 分片上传接口

`POST /api/v1/upload/chunk`

用途：

- 上传某一片分片内容
- 记录上传进度

#### 2.4 分片合并完成接口

`POST /api/v1/upload/complete`

用途：

- 合并分片
- 生成最终文件
- 建议统一返回 `file_name`、`file_url`、`url`、`file_size`、`content_type`，视频可额外返回 `duration`
- 可选触发视频元数据提取

---

## 三、前端页面布局优化设计

建议将页面分为三个清晰的逻辑区域，并通过**视觉引导（Tips/Icon）和交互限制（accept 属性）**来规范用户行为。

### 1. 顶部：课程全局资料区

建议将课程级公共资料放在页面顶部，作为独立区域展示。

设计引导：

- 文案建议：`上传课程大纲或全局参考资料（支持 PDF、Word、PPT、Excel、Markdown、ZIP）`
- 前端限制建议：`accept=".xls,.xlsx,.csv,.ppt,.pptx,.pdf,.doc,.docx,.md,.zip"`
- 建议展示已上传文件列表，支持查看文件名、大小和删除操作

接口适配：

- 小文件上传：`POST /api/v1/upload/file`
- 大文件上传：`POST /api/v1/upload/init` -> `POST /api/v1/upload/chunk` -> `POST /api/v1/upload/complete`
- 上传完成后，再调用 `POST /api/v1/courses/{course_id}/materials` 完成课程资料绑定

注意：

- 原方案中的 `POST /api/upload/doc` 与当前项目不一致，不再使用
- `POST /api/v1/courses/{course_id}/materials` 不是文件上传接口，而是资料绑定接口

### 2. 中间：章节目录管理区

由于章节目录下有小节，小节下又挂视频和课件资源，建议采用**按需展开**或**弹窗管理**的方式，避免在主列表中堆叠过多上传控件。

小节（Section）内部布局建议：

- 不要只放一个“管理资源”按钮
- 在小节标题区域直接显示两个明确入口：
  1. `上传视频`：蓝色按钮或强调图标，带必填标识
  2. `课件资源`：灰色按钮，作为选填资源入口

交互建议：

- 如果当前小节还没有 `video` 资源，在标题旁展示明显提示
- 如果已经上传资源，则在按钮附近显示文件名、上传状态和删除入口
- 小节资源状态应和小节本身解耦，不要把所有资源都挤在主列表里直接展示完整表单

### 3. 弹窗交互：小节资源管理

点击“上传视频”或“课件资源”时，建议弹出一个专门处理当前小节资源的对话框。

#### 左侧：视频上传区

引导建议：

- 文案：`请上传本节教学视频，建议支持 MP4 / MOV，大文件走分片上传`
- 前端限制建议：`accept=".mp4,.mov"`
- 必须显示上传进度、分片状态和完成状态

接口适配：

- 视频上传应对接：
  - `POST /api/v1/upload/init`
  - `POST /api/v1/upload/chunk`
  - `POST /api/v1/upload/complete`
- 上传成功后，再调用 `POST /api/v1/courses/{course_id}/sections/{section_id}/resources`
- 资源绑定时：
  - `type` 传 `video`
  - 同时提交 `title`、`file_url`、`file_size`、`duration`、`sort_order`、`is_free`

注意：

- 原方案中的 `POST /api/upload/video` 与当前项目不一致，不再使用
- 当前项目里视频上传接口本身尚未实现，属于待开发项

#### 右侧：本节配套课件

引导建议：

- 文案：`上传本节专属课件或练习资料（支持 PDF、PPT、Word、Excel、Markdown、ZIP）`
- 前端限制建议：`accept=".ppt,.pptx,.pdf,.doc,.docx,.xls,.xlsx,.csv,.md,.zip"`

接口适配：

- 小文件可对接 `POST /api/v1/upload/file`
- 大文件可对接：
  - `POST /api/v1/upload/init`
  - `POST /api/v1/upload/chunk`
  - `POST /api/v1/upload/complete`
- 上传成功后，再调用 `POST /api/v1/courses/{course_id}/sections/{section_id}/resources`
- 资源绑定时：
  - `type` 传 `document`
  - 同时提交 `title`、`file_url`、`file_size`、`duration`、`sort_order`、`is_free`

注意：

- 原方案中的 `POST /api/upload/doc` 与当前项目不一致，不再使用
- 当前项目里课件文档上传接口本身尚未实现，属于待开发项

### 4. 页面设计与后端职责边界

前端页面设计应遵循以下原则：

- 上传接口只负责把文件传上去，并返回标准文件信息
- 业务接口只负责把上传结果绑定到课程或小节资源上
- 顶部资料区对应课程级资料绑定：`POST /api/v1/courses/{course_id}/materials`
- 小节视频和课件区对应小节级资源绑定：`POST /api/v1/courses/{course_id}/sections/{section_id}/resources`

这样页面层次会更清晰，也更符合当前项目后端的接口职责划分。

---

## 四、接口映射表

| 页面区域 | 按钮/操作 | 文件类型 | 应使用的后端接口 | 当前状态 |
| :--- | :--- | :--- | :--- | :--- |
| 课程封面 | 上传封面 | `.jpg,.jpeg,.png` | `POST /api/v1/upload/file` | 已实现，但当前仅支持封面图 |
| 课程全局资料 | 上传资料 | `.xls,.xlsx,.csv,.ppt,.pptx,.pdf,.doc,.docx,.md,.zip` | `POST /api/v1/upload/file` 或 `POST /api/v1/upload/init/chunk/complete` + `POST /api/v1/courses/{course_id}/materials` | 上传层缺失，绑定层已实现 |
| 小节资源 | 上传视频 | `.mp4,.mov` | `POST /api/v1/upload/init` + `POST /api/v1/upload/chunk` + `POST /api/v1/upload/complete` + `POST /api/v1/courses/{course_id}/sections/{section_id}/resources` | 上传层缺失，绑定层已实现 |
| 小节资源 | 上传课件 | `.ppt,.pptx,.pdf,.doc,.docx,.xls,.xlsx,.csv,.md,.zip` | `POST /api/v1/upload/file` 或 `POST /api/v1/upload/init/chunk/complete` + `POST /api/v1/courses/{course_id}/sections/{section_id}/resources` | 上传层缺失，绑定层已实现 |

---

## 五、需要补齐的后端接口清单

为满足当前页面设计，后续建议 AI 直接开发以下接口能力。

### 1. 扩展小文件上传接口

接口：

`POST /api/v1/upload/file`

当前状态：

- 已实现
- 但仅支持课程封面图上传

后续需要补齐：

- 支持文档、表格、PPT、Markdown、ZIP
- 增加业务类型区分，例如：
  - `cover`
  - `material`
  - `section_resource`
- 返回统一文件信息

建议返回结构：

```json
{
  "file_name": "教学大纲.pdf",
  "file_url": "https://example.com/uploads/xxx.pdf",
  "url": "https://example.com/uploads/xxx.pdf",
  "file_size": 102400,
  "content_type": "application/pdf"
}
```

### 2. 新增大文件初始化接口

接口：

`POST /api/v1/upload/init`

建议用途：

- 校验文件名、文件大小、文件类型
- 生成上传任务
- 返回 `upload_id`
- 返回 `chunk_size`

建议请求字段：

- `file_name`
- `file_size`
- `content_type`
- `biz_type`

### 3. 新增分片上传接口

接口：

`POST /api/v1/upload/chunk`

建议请求字段：

- `upload_id`
- `chunk_index`
- `total_chunks`
- `file`

### 4. 新增上传完成接口

接口：

`POST /api/v1/upload/complete`

建议请求字段：

- `upload_id`

建议返回字段：

- `file_name`
- `file_url`
- `url`
- `file_size`
- `content_type`
- `duration`（视频可选）

### 5. 可选补充的视频处理能力

如果后续视频资源需要更完整的学习体验，建议在 `complete` 后增加异步处理：

- 提取时长
- 提取分辨率
- 截缩略图
- 视频转码

但这部分属于增强功能，不是当前页面闭环的第一优先级。

---

## 六、前端页面对应的业务绑定接口

以下两个接口已经存在，后续开发上传接口时不应删除，而应继续保留：

### 1. 课程资料绑定接口

`POST /api/v1/courses/{course_id}/materials`

职责：

- 接收上传完成后的文件信息
- 将资料挂到课程上

当前字段：

```json
{
  "name": "教学大纲.pdf",
  "file_url": "https://example.com/materials/outline.pdf",
  "file_size": 102400,
  "file_type": "pdf"
}
```

### 2. 小节资源绑定接口

`POST /api/v1/courses/{course_id}/sections/{section_id}/resources`

职责：

- 接收上传完成后的文件信息
- 将资源挂到指定小节

当前字段：

```json
{
  "title": "环境配置视频",
  "type": "video",
  "file_url": "https://example.com/videos/lesson-1.mp4",
  "file_size": 104857600,
  "duration": 600,
  "sort_order": 1,
  "is_free": false
}
```

文档资源示例：

```json
{
  "title": "环境配置讲义",
  "type": "document",
  "file_url": "https://example.com/docs/lesson-1.pdf",
  "file_size": 204800,
  "duration": 0,
  "sort_order": 2,
  "is_free": false
}
```

---

## 七、当前文档中原有接口命名的替换关系

原文中的接口命名与当前项目不一致，现统一替换如下：

| 原文接口 | 替换为当前项目接口表达 | 说明 |
| :--- | :--- | :--- |
| `POST /api/upload/doc` | `POST /api/v1/upload/file` 或 `POST /api/v1/upload/init/chunk/complete` | 当前项目没有 `upload/doc`，后续统一归入 `/api/v1/upload/*` |
| `POST /upload/common` | `POST /api/v1/upload/file` 或 `POST /api/v1/upload/init/chunk/complete` | 当前项目没有 `upload/common` |
| `POST /upload/video` | `POST /api/v1/upload/init` + `POST /api/v1/upload/chunk` + `POST /api/v1/upload/complete` | 当前项目没有独立 `upload/video` |

---

## 八、最终结论

当前项目真正已经具备的只有两类能力：

1. 已有一个可扩展的上传入口：`POST /api/v1/upload/file`
2. 已有两个绑定业务数据的接口：
   - `POST /api/v1/courses/{course_id}/materials`
   - `POST /api/v1/courses/{course_id}/sections/{section_id}/resources`

当前真正缺失、需要后续 AI 直接开发的接口是：

1. 扩展版 `POST /api/v1/upload/file`
   - 用于文档、PPT、表格、ZIP 等小文件上传
2. `POST /api/v1/upload/init`
3. `POST /api/v1/upload/chunk`
4. `POST /api/v1/upload/complete`

后续开发时，应遵循这个原则：

- 上传接口只负责把文件传上去，并至少返回 `file_url`；建议统一返回 `file_name`、`file_url`、`url`、`file_size`、`content_type`，视频可额外返回 `duration`
- `materials` 和 `resources` 继续负责业务绑定
- 不再使用原文里的 `/api/upload/doc`、`/upload/common`、`/upload/video`




