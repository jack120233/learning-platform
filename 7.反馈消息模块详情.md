# 反馈消息模块技术详情文档

生成时间：2026-03-19
技术栈：Python + FastAPI
模块名称：反馈消息模块

---

## 模块概述

反馈消息模块包含两大功能：用户反馈管理和站内消息管理。用户反馈用于收集学员对课程或系统的意见和建议；站内消息用于向用户发送系统通知、审核结果等信息。本模块共包含11个接口。

---

## 接口列表

### 反馈管理接口

| 序号 | 接口名称 | 接口路径 | HTTP方法 | 权限要求 |
|------|----------|----------|----------|----------|
| 1 | 提交反馈 | /api/v1/feedbacks | POST | 🔒 需要登录 |
| 2 | 反馈列表（管理员） | /api/v1/feedbacks | GET | 👨‍💼 管理员权限 |
| 3 | 反馈详情（管理员） | /api/v1/feedbacks/{feedback_id} | GET | 👨‍💼 管理员权限 |
| 4 | 标记反馈为已处理（管理员） | /api/v1/feedbacks/{feedback_id}/process | POST | 👨‍💼 管理员权限 |

### 消息管理接口

| 序号 | 接口名称 | 接口路径 | HTTP方法 | 权限要求 |
|------|----------|----------|----------|----------|
| 5 | 消息列表 | /api/v1/messages | GET | 🔒 需要登录 |
| 6 | 消息详情 | /api/v1/messages/{message_id} | GET | 🔒 需要登录 |
| 7 | 标记消息为已读 | /api/v1/messages/{message_id}/read | POST | 🔒 需要登录 |
| 8 | 批量标记已读 | /api/v1/messages/mark-all-read | POST | 🔒 需要登录 |
| 9 | 删除消息 | /api/v1/messages/{message_id} | POST | 🔒 需要登录 |
| 10 | 未读消息数量 | /api/v1/messages/unread-count | GET | 🔒 需要登录 |
| 11 | 发送系统消息（管理员） | /api/v1/messages/send | POST | 👨‍💼 管理员权限 |

---

## 一、提交反馈

### 1. 基本信息

| 项目 | 内容 |
|------|------|
| 接口名称 | 提交反馈 |
| 接口路径 | /api/v1/feedbacks |
| HTTP方法 | POST |
| 功能描述 | 用户提交课程反馈或系统反馈，支持上传截图 |

### 2. 请求参数

#### 请求体JSON

```json
{
  "feedback_type": "course",
  "course_id": 1,
  "content": "课程内容很好，但是视频有点卡顿",
  "images": [
    "https://example.com/screenshot1.jpg",
    "https://example.com/screenshot2.jpg"
  ]
}
```

#### 参数说明

| 参数 | 类型 | 必填 | 说明 | 验证规则 |
|------|------|------|------|----------|
| feedback_type | string | 是 | 反馈类型 | system/course |
| course_id | integer | 否 | 课程ID | 课程反馈时必填 |
| content | string | 是 | 反馈内容 | 10-500字符 |
| images | array | 否 | 截图URL数组 | 最多8张，单张限制5MB |

### 3. 响应结构

#### 成功响应

```json
{
  "code": 200,
  "message": "反馈提交成功",
  "data": {
    "feedback_id": 1,
    "created_at": "2026-03-17T10:00:00"
  }
}
```

#### 失败响应

| 场景 | code | message |
|------|------|---------|
| 未登录 | 401 | 未认证或令牌过期 |
| 参数验证失败 | 422 | 数据验证失败 |
| 课程不存在 | 404 | 课程不存在 |
| 提交过于频繁 | 429 | 提交过于频繁，请稍后再试 |
| 超出每日限制 | 400 | 每日最多提交10条反馈 |

### 4. 业务逻辑

```
开始
  ↓
验证用户登录状态
  ├── 未登录 → 返回401错误
  └── 已登录 → 继续
  ↓
验证参数格式
  ├── 内容长度不符 → 返回422错误
  └── 符合要求 → 继续
  ↓
验证提交频率
  ├── 1分钟内已提交 → 返回429错误
  └── 正常 → 继续
  ↓
验证每日提交数量
  ├── 已达10条 → 返回400错误
  └── 未达限制 → 继续
  ↓
如果是课程反馈
  ├── 验证课程是否存在
  │   ├── 不存在 → 返回404错误
  │   └── 存在 → 继续
  └── 关联课程信息
  ↓
创建反馈记录
  ↓
发送通知给管理员（可选）
  ↓
返回反馈ID
结束
```

### 5. 数据库操作

- **涉及数据表**：feedbacks
- **操作类型**：插入

```python
# SQLAlchemy示例
feedback = Feedback(
    user_id=current_user.id,
    feedback_type=request.feedback_type,
    course_id=request.course_id if request.feedback_type == 'course' else None,
    content=request.content,
    images=json.dumps(request.images),
    status='pending'
)
db.add(feedback)
await db.commit()
```

### 6. 依赖项和工具

- **Python库**：
  - sqlalchemy（ORM）
  - pydantic（数据验证）

### 7. 异常处理

| 异常场景 | 处理方式 |
|----------|----------|
| 未登录 | 中间件拦截，返回401 |
| 参数验证失败 | Pydantic自动验证，返回422 |
| 提交频率限制 | 检查Redis计数器，返回429 |
| 数据库操作失败 | 捕获异常，回滚事务，返回500 |

### 8. 安全要求

- 需要登录认证
- 内容XSS过滤
- 频率限制（1分钟内只能提交1条）
- 每日数量限制（24小时内最多10条）

### 9. 性能考虑

- **缓存策略**：使用Redis记录用户提交频率和每日数量
- 异步处理管理员通知

### 10. 代码组织建议

#### Pydantic模型

```python
# schemas/feedback.py
from pydantic import BaseModel, Field, field_validator
from typing import Optional
from enum import Enum

class FeedbackTypeEnum(str, Enum):
    SYSTEM = "system"
    COURSE = "course"

class FeedbackCreateRequest(BaseModel):
    feedback_type: FeedbackTypeEnum
    course_id: Optional[int] = None
    content: str = Field(..., min_length=10, max_length=500)
    images: Optional[list[str]] = Field(default=[], max_length=8)

    @field_validator('course_id')
    @classmethod
    def validate_course_id(cls, v, info):
        if info.data.get('feedback_type') == 'course' and not v:
            raise ValueError('课程反馈必须指定课程ID')
        return v

class FeedbackCreateResponse(BaseModel):
    feedback_id: int
    created_at: datetime
```

#### 服务层

```python
# services/feedback_service.py
from datetime import datetime, timedelta
import redis

class FeedbackService:
    def __init__(self, redis_client: redis.Redis):
        self.redis = redis_client

    FEEDBACK_RATE_LIMIT = 60  # 60秒
    FEEDBACK_DAILY_LIMIT = 10  # 每日10条

    async def check_rate_limit(self, user_id: int) -> bool:
        """检查提交频率"""
        key = f"feedback:rate:{user_id}"
        return self.redis.exists(key)

    async def check_daily_limit(self, user_id: int) -> int:
        """检查每日提交数量"""
        key = f"feedback:daily:{user_id}:{datetime.now().strftime('%Y%m%d')}"
        count = self.redis.get(key)
        return int(count) if count else 0

    async def increment_rate_limit(self, user_id: int) -> None:
        """设置频率限制标记"""
        key = f"feedback:rate:{user_id}"
        self.redis.setex(key, self.FEEDBACK_RATE_LIMIT, "1")

    async def increment_daily_count(self, user_id: int) -> None:
        """增加每日计数"""
        key = f"feedback:daily:{user_id}:{datetime.now().strftime('%Y%m%d')}"
        self.redis.incr(key)
        self.redis.expire(key, 86400)  # 24小时过期

    async def create_feedback(
        self,
        db: AsyncSession,
        user_id: int,
        request: FeedbackCreateRequest
    ) -> Feedback:
        """创建反馈"""
        # 检查频率限制
        if await self.check_rate_limit(user_id):
            raise ValueError("提交过于频繁，请稍后再试")

        # 检查每日限制
        daily_count = await self.check_daily_limit(user_id)
        if daily_count >= self.FEEDBACK_DAILY_LIMIT:
            raise ValueError("每日最多提交10条反馈")

        # 验证课程
        if request.feedback_type == 'course':
            course = await db.get(Course, request.course_id)
            if not course:
                raise ValueError("课程不存在")

        # 创建反馈
        feedback = Feedback(
            user_id=user_id,
            feedback_type=request.feedback_type,
            course_id=request.course_id,
            content=request.content,
            images=json.dumps(request.images),
            status='pending'
        )
        db.add(feedback)
        await db.commit()
        await db.refresh(feedback)

        # 设置限制标记
        await self.increment_rate_limit(user_id)
        await self.increment_daily_count(user_id)

        return feedback
```

---

## 二、反馈列表（管理员）

### 1. 基本信息

| 项目 | 内容 |
|------|------|
| 接口名称 | 反馈列表 |
| 接口路径 | /api/v1/feedbacks |
| HTTP方法 | GET |
| 功能描述 | 管理员查看用户反馈列表，支持筛选和搜索 |

### 2. 请求参数

#### Query参数

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|------|------|------|--------|------|
| feedback_type | string | 否 | all | 反馈类型：all/system/course |
| status | string | 否 | all | 处理状态：all/pending/processed |
| keyword | string | 否 | - | 搜索关键词（反馈内容、用户名） |
| page | integer | 否 | 1 | 页码 |
| page_size | integer | 否 | 20 | 每页数量 |

### 3. 响应结构

#### 成功响应

```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "items": [
      {
        "feedback_id": 1,
        "user_id": 1,
        "username": "zhangsan",
        "feedback_type": "course",
        "course_id": 1,
        "course_title": "Python入门教程",
        "content": "课程内容很好，但是视频有点卡顿",
        "images": ["https://example.com/screenshot1.jpg"],
        "status": "pending",
        "created_at": "2026-03-17T10:00:00"
      }
    ],
    "total": 50,
    "page": 1,
    "page_size": 20,
    "total_pages": 3
  }
}
```

### 4. 业务逻辑

```
开始
  ↓
验证管理员权限
  ├── 非管理员 → 返回403错误
  └── 管理员 → 继续
  ↓
解析筛选条件
  ├── feedback_type筛选
  ├── status筛选
  └── keyword搜索
  ↓
构建数据库查询
  ↓
关联查询用户和课程信息
  ↓
分页处理
  ↓
返回反馈列表
结束
```

### 5. 数据库操作

- **涉及数据表**：feedbacks, users, courses
- **操作类型**：查询

```sql
-- SQL示例
SELECT
    f.id as feedback_id,
    f.user_id,
    u.username,
    f.feedback_type,
    f.course_id,
    c.title as course_title,
    f.content,
    f.images,
    f.status,
    f.created_at
FROM feedbacks f
LEFT JOIN users u ON f.user_id = u.id
LEFT JOIN courses c ON f.course_id = c.id
WHERE (? = 'all' OR f.feedback_type = ?)
  AND (? = 'all' OR f.status = ?)
  AND (? = '' OR f.content LIKE ? OR u.username LIKE ?)
ORDER BY f.created_at DESC
LIMIT ? OFFSET ?;
```

### 6. 代码组织建议

```python
# schemas/feedback.py
class FeedbackListItem(BaseModel):
    feedback_id: int
    user_id: int
    username: str
    feedback_type: str
    course_id: Optional[int] = None
    course_title: Optional[str] = None
    content: str
    images: list[str]
    status: str
    created_at: datetime

# api/v1/feedback.py
@router.get("", response_model=ApiResponse[PaginatedResponse[FeedbackListItem]])
async def get_feedbacks(
    feedback_type: str = Query(default="all"),
    status: str = Query(default="all"),
    keyword: str = Query(default=""),
    page: int = Query(default=1, ge=1),
    page_size: int = Query(default=20, ge=1, le=100),
    current_user: User = Depends(require_admin),
    db: AsyncSession = Depends(get_db)
):
    result = await feedback_service.get_feedback_list(
        db, feedback_type, status, keyword, page, page_size
    )
    return ApiResponse(data=result)
```

---

## 三、反馈详情（管理员）

### 1. 基本信息

| 项目 | 内容 |
|------|------|
| 接口名称 | 反馈详情 |
| 接口路径 | /api/v1/feedbacks/{feedback_id} |
| HTTP方法 | GET |
| 功能描述 | 管理员查看反馈的详细信息 |

### 2. 请求参数

#### Path参数

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| feedback_id | integer | 是 | 反馈ID |

### 3. 响应结构

#### 成功响应

```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "feedback_id": 1,
    "user_id": 1,
    "username": "zhangsan",
    "email": "zhangsan@example.com",
    "feedback_type": "course",
    "course_id": 1,
    "course_title": "Python入门教程",
    "content": "课程内容很好，但是视频有点卡顿",
    "images": [
      "https://example.com/screenshot1.jpg",
      "https://example.com/screenshot2.jpg"
    ],
    "status": "pending",
    "created_at": "2026-03-17T10:00:00"
  }
}
```

#### 失败响应

| 场景 | code | message |
|------|------|---------|
| 权限不足 | 403 | 权限不足 |
| 反馈不存在 | 404 | 反馈不存在 |

### 4. 数据库操作

- **涉及数据表**：feedbacks, users, courses
- **操作类型**：查询

### 5. 代码组织建议

```python
# api/v1/feedback.py
@router.get("/{feedback_id}", response_model=ApiResponse[FeedbackDetailResponse])
async def get_feedback_detail(
    feedback_id: int,
    current_user: User = Depends(require_admin),
    db: AsyncSession = Depends(get_db)
):
    feedback = await feedback_service.get_feedback_detail(db, feedback_id)
    if not feedback:
        raise HTTPException(status_code=404, detail="反馈不存在")
    return ApiResponse(data=feedback)
```

---

## 四、标记反馈为已处理（管理员）

### 1. 基本信息

| 项目 | 内容 |
|------|------|
| 接口名称 | 标记反馈为已处理 |
| 接口路径 | /api/v1/feedbacks/{feedback_id}/process |
| HTTP方法 | POST |
| 功能描述 | 管理员将反馈标记为已处理状态 |

### 2. 请求参数

#### Path参数

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| feedback_id | integer | 是 | 反馈ID |

### 3. 响应结构

#### 成功响应

```json
{
  "code": 200,
  "message": "操作成功",
  "data": null
}
```

#### 失败响应

| 场景 | code | message |
|------|------|---------|
| 权限不足 | 403 | 权限不足 |
| 反馈不存在 | 404 | 反馈不存在 |
| 已处理过 | 400 | 该反馈已处理 |

### 4. 业务逻辑

```
开始
  ↓
验证管理员权限
  ├── 非管理员 → 返回403错误
  └── 管理员 → 继续
  ↓
查询反馈是否存在
  ├── 不存在 → 返回404错误
  └── 存在 → 继续
  ↓
检查当前状态
  ├── 已处理 → 返回400错误
  └── 待处理 → 继续
  ↓
更新反馈状态
  ├── status = 'processed'
  └── processed_at = now
  ↓
返回成功
结束
```

### 5. 数据库操作

- **涉及数据表**：feedbacks
- **操作类型**：更新

```python
# SQLAlchemy示例
await db.execute(
    update(Feedback)
    .where(Feedback.id == feedback_id)
    .values(
        status='processed',
        processed_at=datetime.now()
    )
)
await db.commit()
```

### 6. 代码组织建议

```python
# api/v1/feedback.py
@router.post("/{feedback_id}/process", response_model=ApiResponse)
async def process_feedback(
    feedback_id: int,
    current_user: User = Depends(require_admin),
    db: AsyncSession = Depends(get_db)
):
    feedback = await db.get(Feedback, feedback_id)
    if not feedback:
        raise HTTPException(status_code=404, detail="反馈不存在")

    if feedback.status == 'processed':
        raise HTTPException(status_code=400, detail="该反馈已处理")

    await feedback_service.process_feedback(db, feedback_id)
    return ApiResponse(message="操作成功")
```

---

## 五、消息列表

### 1. 基本信息

| 项目 | 内容 |
|------|------|
| 接口名称 | 消息列表 |
| 接口路径 | /api/v1/messages |
| HTTP方法 | GET |
| 功能描述 | 获取当前用户的消息列表，支持类型筛选和已读状态筛选 |

### 2. 请求参数

#### Query参数

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|------|------|------|--------|------|
| message_type | string | 否 | all | 消息类型：all/announcement/notification |
| is_read | boolean | 否 | - | 是否已读：true/false |
| page | integer | 否 | 1 | 页码 |
| page_size | integer | 否 | 20 | 每页数量 |

### 3. 响应结构

#### 成功响应

```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "items": [
      {
        "message_id": 1,
        "message_type": "notification",
        "title": "讲师审核通过",
        "content": "恭喜您，您的讲师申请已通过审核",
        "is_read": false,
        "created_at": "2026-03-17T10:00:00"
      }
    ],
    "total": 10,
    "page": 1,
    "page_size": 20,
    "total_pages": 1,
    "unread_count": 5
  }
}
```

#### 响应字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| items | array | 消息列表 |
| items[].message_id | integer | 消息ID |
| items[].message_type | string | 消息类型：announcement/notification |
| items[].title | string | 消息标题 |
| items[].content | string | 消息内容（摘要） |
| items[].is_read | boolean | 是否已读 |
| items[].created_at | datetime | 创建时间 |
| total | integer | 总记录数 |
| page | integer | 当前页码 |
| page_size | integer | 每页数量 |
| total_pages | integer | 总页数 |
| unread_count | integer | 未读消息总数 |

### 4. 业务逻辑

```
开始
  ↓
验证用户登录状态
  ├── 未登录 → 返回401错误
  └── 已登录 → 继续
  ↓
解析筛选条件
  ├── message_type筛选
  └── is_read筛选
  ↓
查询消息列表
  ├── 条件：user_id = current_user.id
  └── 排序：created_at DESC
  ↓
统计未读消息数量
  ↓
分页处理
  ↓
返回消息列表和未读数量
结束
```

### 5. 数据库操作

- **涉及数据表**：messages
- **操作类型**：查询

```sql
-- SQL示例
-- 查询消息列表
SELECT
    id as message_id,
    message_type,
    title,
    SUBSTRING(content, 1, 100) as content,
    is_read,
    created_at
FROM messages
WHERE user_id = ?
  AND (? = 'all' OR message_type = ?)
  AND (? IS NULL OR is_read = ?)
ORDER BY created_at DESC
LIMIT ? OFFSET ?;

-- 统计未读数量
SELECT COUNT(*) FROM messages
WHERE user_id = ? AND is_read = false;
```

### 6. 性能考虑

- **数据库索引**：(user_id, created_at)复合索引

### 7. 代码组织建议

```python
# schemas/message.py
from pydantic import BaseModel
from datetime import datetime
from typing import Optional
from enum import Enum

class MessageTypeEnum(str, Enum):
    ANNOUNCEMENT = "announcement"
    NOTIFICATION = "notification"

class MessageListItem(BaseModel):
    message_id: int
    message_type: str
    title: str
    content: str
    is_read: bool
    created_at: datetime

class MessageListResponse(BaseModel):
    items: list[MessageListItem]
    total: int
    page: int
    page_size: int
    total_pages: int
    unread_count: int

# api/v1/message.py
@router.get("", response_model=ApiResponse[MessageListResponse])
async def get_messages(
    message_type: str = Query(default="all"),
    is_read: Optional[bool] = Query(default=None),
    page: int = Query(default=1, ge=1),
    page_size: int = Query(default=20, ge=1, le=100),
    current_user: User = Depends(get_current_user),
    db: AsyncSession = Depends(get_db)
):
    result = await message_service.get_messages(
        db, current_user.id, message_type, is_read, page, page_size
    )
    return ApiResponse(data=result)
```

---

## 六、消息详情

### 1. 基本信息

| 项目 | 内容 |
|------|------|
| 接口名称 | 消息详情 |
| 接口路径 | /api/v1/messages/{message_id} |
| HTTP方法 | GET |
| 功能描述 | 获取消息的完整内容，查看时自动标记为已读 |

### 2. 请求参数

#### Path参数

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| message_id | integer | 是 | 消息ID |

### 3. 响应结构

#### 成功响应

```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "message_id": 1,
    "message_type": "notification",
    "title": "讲师审核通过",
    "content": "恭喜您，您的讲师申请已通过审核，现在您可以创建和发布课程了。",
    "is_read": true,
    "read_at": "2026-03-17T10:30:00",
    "created_at": "2026-03-17T10:00:00"
  }
}
```

#### 失败响应

| 场景 | code | message |
|------|------|---------|
| 未登录 | 401 | 未认证或令牌过期 |
| 消息不存在 | 404 | 消息不存在 |
| 无权限查看 | 403 | 无权限查看该消息 |

### 4. 业务逻辑

```
开始
  ↓
验证用户登录状态
  ├── 未登录 → 返回401错误
  └── 已登录 → 继续
  ↓
查询消息是否存在
  ├── 不存在 → 返回404错误
  └── 存在 → 继续
  ↓
验证消息所有权
  ├── 非本人消息 → 返回403错误
  └── 本人消息 → 继续
  ↓
如果未读，标记为已读
  ├── is_read = true
  └── read_at = now
  ↓
返回消息详情
结束
```

### 5. 数据库操作

- **涉及数据表**：messages
- **操作类型**：查询、更新

```python
# SQLAlchemy示例
# 查询消息
message = await db.execute(
    select(Message).where(Message.id == message_id)
)
message = message.scalar_one()

# 标记已读
if not message.is_read:
    await db.execute(
        update(Message)
        .where(Message.id == message_id)
        .values(is_read=True, read_at=datetime.now())
    )
    await db.commit()
```

### 6. 代码组织建议

```python
# api/v1/message.py
@router.get("/{message_id}", response_model=ApiResponse[MessageDetailResponse])
async def get_message_detail(
    message_id: int,
    current_user: User = Depends(get_current_user),
    db: AsyncSession = Depends(get_db)
):
    message = await db.get(Message, message_id)
    if not message:
        raise HTTPException(status_code=404, detail="消息不存在")

    if message.user_id != current_user.id:
        raise HTTPException(status_code=403, detail="无权限查看该消息")

    # 自动标记已读
    if not message.is_read:
        await message_service.mark_as_read(db, message_id)

    return ApiResponse(data=MessageDetailResponse.from_orm(message))
```

---

## 七、标记消息为已读

### 1. 基本信息

| 项目 | 内容 |
|------|------|
| 接口名称 | 标记消息为已读 |
| 接口路径 | /api/v1/messages/{message_id}/read |
| HTTP方法 | POST |
| 功能描述 | 将指定消息标记为已读状态 |

### 2. 请求参数

#### Path参数

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| message_id | integer | 是 | 消息ID |

### 3. 响应结构

#### 成功响应

```json
{
  "code": 200,
  "message": "操作成功",
  "data": null
}
```

### 4. 业务逻辑

```
开始
  ↓
验证用户登录状态
  ↓
查询消息是否存在
  ├── 不存在 → 返回404错误
  └── 存在 → 继续
  ↓
验证消息所有权
  ├── 非本人 → 返回403错误
  └── 本人 → 继续
  ↓
更新消息状态
  ├── is_read = true
  └── read_at = now
  ↓
返回成功
结束
```

### 5. 代码组织建议

```python
# api/v1/message.py
@router.post("/{message_id}/read", response_model=ApiResponse)
async def mark_message_read(
    message_id: int,
    current_user: User = Depends(get_current_user),
    db: AsyncSession = Depends(get_db)
):
    message = await db.get(Message, message_id)
    if not message:
        raise HTTPException(status_code=404, detail="消息不存在")

    if message.user_id != current_user.id:
        raise HTTPException(status_code=403, detail="无权限操作该消息")

    await message_service.mark_as_read(db, message_id)
    return ApiResponse(message="操作成功")
```

---

## 八、批量标记已读

### 1. 基本信息

| 项目 | 内容 |
|------|------|
| 接口名称 | 批量标记已读 |
| 接口路径 | /api/v1/messages/mark-all-read |
| HTTP方法 | POST |
| 功能描述 | 将当前用户的所有未读消息标记为已读 |

### 2. 请求参数

无需请求参数

### 3. 响应结构

#### 成功响应

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "marked_count": 5
  }
}
```

### 4. 业务逻辑

```
开始
  ↓
验证用户登录状态
  ↓
批量更新未读消息
  ├── 条件：user_id = ? AND is_read = false
  └── 更新：is_read = true, read_at = now
  ↓
返回更新数量
结束
```

### 5. 数据库操作

```python
# SQLAlchemy示例
result = await db.execute(
    update(Message)
    .where(
        Message.user_id == user_id,
        Message.is_read == False
    )
    .values(is_read=True, read_at=datetime.now())
)
await db.commit()
marked_count = result.rowcount
```

### 6. 代码组织建议

```python
# api/v1/message.py
@router.post("/mark-all-read", response_model=ApiResponse[MarkAllReadResponse])
async def mark_all_read(
    current_user: User = Depends(get_current_user),
    db: AsyncSession = Depends(get_db)
):
    count = await message_service.mark_all_as_read(db, current_user.id)
    return ApiResponse(
        message="操作成功",
        data=MarkAllReadResponse(marked_count=count)
    )
```

---

## 九、删除消息

### 1. 基本信息

| 项目 | 内容 |
|------|------|
| 接口名称 | 删除消息 |
| 接口路径 | /api/v1/messages/{message_id} |
| HTTP方法 | POST |
| 功能描述 | 删除指定消息 |

### 2. 请求参数

#### Path参数

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| message_id | integer | 是 | 消息ID |

### 3. 响应结构

#### 成功响应

```json
{
  "code": 200,
  "message": "删除成功",
  "data": null
}
```

### 4. 业务逻辑

```
开始
  ↓
验证用户登录状态
  ↓
查询消息是否存在
  ├── 不存在 → 返回404错误
  └── 存在 → 继续
  ↓
验证消息所有权
  ├── 非本人 → 返回403错误
  └── 本人 → 继续
  ↓
删除消息
  ↓
返回成功
结束
```

### 5. 代码组织建议

```python
# api/v1/message.py
@router.post("/{message_id}", response_model=ApiResponse)
async def delete_message(
    message_id: int,
    current_user: User = Depends(get_current_user),
    db: AsyncSession = Depends(get_db)
):
    message = await db.get(Message, message_id)
    if not message:
        raise HTTPException(status_code=404, detail="消息不存在")

    if message.user_id != current_user.id:
        raise HTTPException(status_code=403, detail="无权限操作该消息")

    await db.execute(delete(Message).where(Message.id == message_id))
    await db.commit()

    return ApiResponse(message="删除成功")
```

---

## 十、未读消息数量

### 1. 基本信息

| 项目 | 内容 |
|------|------|
| 接口名称 | 未读消息数量 |
| 接口路径 | /api/v1/messages/unread-count |
| HTTP方法 | GET |
| 功能描述 | 获取当前用户的未读消息数量 |

### 2. 请求参数

无需请求参数

### 3. 响应结构

#### 成功响应

```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "unread_count": 5
  }
}
```

### 4. 数据库操作

```sql
-- SQL示例
SELECT COUNT(*) FROM messages
WHERE user_id = ? AND is_read = false;
```

### 5. 性能考虑

- **缓存策略**：未读数量可缓存1分钟
- **数据库索引**：(user_id, is_read)复合索引

### 6. 代码组织建议

```python
# api/v1/message.py
@router.get("/unread-count", response_model=ApiResponse[UnreadCountResponse])
async def get_unread_count(
    current_user: User = Depends(get_current_user),
    db: AsyncSession = Depends(get_db)
):
    count = await message_service.get_unread_count(db, current_user.id)
    return ApiResponse(data=UnreadCountResponse(unread_count=count))
```

---

## 十一、发送系统消息（管理员）

### 1. 基本信息

| 项目 | 内容 |
|------|------|
| 接口名称 | 发送系统消息 |
| 接口路径 | /api/v1/messages/send |
| HTTP方法 | POST |
| 功能描述 | 管理员向指定用户发送系统消息 |

### 2. 请求参数

#### 请求体JSON

```json
{
  "user_ids": [1, 2, 3],
  "message_type": "notification",
  "title": "系统维护通知",
  "content": "系统将于今晚22:00-24:00进行维护，期间无法访问"
}
```

#### 参数说明

| 参数 | 类型 | 必填 | 说明 | 验证规则 |
|------|------|------|------|----------|
| user_ids | array | 是 | 接收用户ID数组 | 非空数组，最多100个用户 |
| message_type | string | 是 | 消息类型 | announcement/notification |
| title | string | 是 | 消息标题 | 最多50字符 |
| content | string | 是 | 消息内容 | 最多500字符 |

### 3. 响应结构

#### 成功响应

```json
{
  "code": 200,
  "message": "发送成功",
  "data": {
    "sent_count": 3
  }
}
```

#### 失败响应

| 场景 | code | message |
|------|------|---------|
| 权限不足 | 403 | 权限不足 |
| 用户ID不存在 | 400 | 部分用户ID不存在 |
| 参数验证失败 | 422 | 数据验证失败 |

### 4. 业务逻辑

```
开始
  ↓
验证管理员权限
  ├── 非管理员 → 返回403错误
  └── 管理员 → 继续
  ↓
验证参数格式
  ├── user_ids为空 → 返回422错误
  └── 符合要求 → 继续
  ↓
验证用户ID是否存在
  ├── 部分不存在 → 返回400错误
  └── 全部存在 → 继续
  ↓
批量创建消息记录
  ├── 遍历user_ids
  └── 为每个用户创建消息
  ↓
返回发送数量
结束
```

### 5. 数据库操作

- **涉及数据表**：messages
- **操作类型**：批量插入

```python
# SQLAlchemy示例
messages = [
    Message(
        user_id=user_id,
        message_type=request.message_type,
        title=request.title,
        content=request.content,
        is_read=False
    )
    for user_id in request.user_ids
]
db.add_all(messages)
await db.commit()
```

### 6. 性能考虑

- 批量插入优化
- 大量用户时使用异步任务队列

### 7. 代码组织建议

```python
# schemas/message.py
class SendMessageRequest(BaseModel):
    user_ids: list[int] = Field(..., min_length=1, max_length=100)
    message_type: MessageTypeEnum
    title: str = Field(..., max_length=50)
    content: str = Field(..., max_length=500)

class SendMessageResponse(BaseModel):
    sent_count: int

# api/v1/message.py
@router.post("/send", response_model=ApiResponse[SendMessageResponse])
async def send_message(
    request: SendMessageRequest,
    current_user: User = Depends(require_admin),
    db: AsyncSession = Depends(get_db)
):
    # 验证用户ID
    existing_ids = await message_service.validate_user_ids(db, request.user_ids)
    invalid_ids = set(request.user_ids) - existing_ids
    if invalid_ids:
        raise HTTPException(
            status_code=400,
            detail=f"用户ID不存在: {list(invalid_ids)}"
        )

    # 发送消息
    count = await message_service.send_messages(
        db, request.user_ids, request.message_type, request.title, request.content
    )

    return ApiResponse(
        message="发送成功",
        data=SendMessageResponse(sent_count=count)
    )
```

---

## 模块依赖汇总

### 数据库表

| 表名 | 说明 | 操作类型 |
|------|------|----------|
| feedbacks | 用户反馈表 | 查询、插入、更新 |
| messages | 站内消息表 | 查询、插入、更新、删除 |
| users | 用户表 | 查询 |
| courses | 课程表 | 查询 |

### 外部服务依赖

| 服务 | 说明 |
|------|------|
| 认证服务 | JWT令牌验证、权限验证 |
| 缓存服务 | Redis（频率限制、每日计数） |

### 公共工具

| 工具 | 说明 |
|------|------|
| get_current_user | 获取当前登录用户 |
| require_admin | 管理员权限验证 |
| feedback_service | 反馈业务逻辑 |
| message_service | 消息业务逻辑 |

---

## 文件组织建议

```
backend/app/
├── api/v1/
│   ├── feedback.py          # 反馈管理路由
│   └── message.py           # 消息管理路由
├── models/
│   ├── feedback.py          # 反馈模型
│   └── message.py           # 消息模型
├── schemas/
│   ├── feedback.py          # 反馈相关Pydantic模型
│   └── message.py           # 消息相关Pydantic模型
├── services/
│   ├── feedback_service.py  # 反馈业务逻辑
│   └── message_service.py   # 消息业务逻辑
└── utils/
    └── rate_limiter.py      # 频率限制工具
```

---

## 数据模型定义

### Feedback 模型

```python
# models/feedback.py
from sqlalchemy import Column, Integer, String, DateTime, ForeignKey, Text, Enum
from sqlalchemy.orm import relationship
from datetime import datetime
import enum

class FeedbackStatus(str, enum.Enum):
    PENDING = "pending"
    PROCESSED = "processed"

class FeedbackType(str, enum.Enum):
    SYSTEM = "system"
    COURSE = "course"

class Feedback(Base):
    """用户反馈"""
    __tablename__ = "feedbacks"

    id = Column(Integer, primary_key=True, index=True)
    user_id = Column(Integer, ForeignKey("users.id"), nullable=False)
    feedback_type = Column(Enum(FeedbackType), nullable=False)
    course_id = Column(Integer, ForeignKey("courses.id"), nullable=True)
    content = Column(Text, nullable=False)
    images = Column(Text, default="[]")  # JSON数组
    status = Column(Enum(FeedbackStatus), default=FeedbackStatus.PENDING)
    created_at = Column(DateTime, default=datetime.utcnow)
    processed_at = Column(DateTime, nullable=True)

    # 关系
    user = relationship("User")
    course = relationship("Course")
```

### Message 模型

```python
# models/message.py
from sqlalchemy import Column, Integer, String, DateTime, ForeignKey, Text, Boolean, Enum
from sqlalchemy.orm import relationship
from datetime import datetime
import enum

class MessageType(str, enum.Enum):
    ANNOUNCEMENT = "announcement"
    NOTIFICATION = "notification"

class Message(Base):
    """站内消息"""
    __tablename__ = "messages"

    id = Column(Integer, primary_key=True, index=True)
    user_id = Column(Integer, ForeignKey("users.id"), nullable=False)
    message_type = Column(Enum(MessageType), nullable=False)
    title = Column(String(50), nullable=False)
    content = Column(Text, nullable=False)
    is_read = Column(Boolean, default=False)
    created_at = Column(DateTime, default=datetime.utcnow)
    read_at = Column(DateTime, nullable=True)

    # 关系
    user = relationship("User")
```

---

## 频率限制配置

| 限制项 | 时间窗口 | 最大次数 | 实现方式 |
|--------|----------|----------|----------|
| 反馈提交 | 60秒 | 1次 | Redis计数器 |
| 反馈每日 | 24小时 | 10条 | Redis计数器 |
| 消息发送 | 1分钟 | 100用户 | 数据库批量插入 |

---

**文档版本**：v1.0
**生成时间**：2026-03-19
**维护人员**：开发团队