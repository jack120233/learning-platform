# 前后端接口文档差异对照

对照文档：

- 前端文档：`E:\video_project\UI\前端接口文档.md`
- 后端文档：`E:\video_project\UI\api-frontend-quick-reference.md`

说明：

- 重点对照：请求字段、响应字段、字段名、是否必填、枚举值、嵌套结构、分页结构、认证约定
- 差异类型：
  - 前端文档有，后端文档没有
  - 后端文档有，前端文档没有
  - 同接口但字段名 / 语义 / 结构不一致

---

## 一、总览

### 1. 差异最严重的模块

1. 认证模块
2. 课程模块
3. 学习模块
4. 讲师端课程内容模块
5. 管理后台模块

### 2. 高风险接口

以下接口差异最大，最容易直接影响联调：

- `POST /api/v1/auth/login`
- `POST /api/v1/auth/register`
- `POST /api/v1/auth/reset-password`
- `GET /api/v1/courses/homepage`
- `GET /api/v1/courses/search`
- `GET /api/v1/learning/progress`
- `POST /api/v1/learning/progress`
- `POST /api/v1/feedbacks`
- `POST /api/v1/users/me`
- `POST /api/v1/courses`
- `POST /api/v1/courses/{course_id}`
- `POST /api/v1/courses/{course_id}/sections/{section_id}/resources`
- `POST /api/v1/users/teacher-audits/{audit_id}/review`

### 3. 删除类接口普遍不一致

前端文档大量写法为：

- `POST .../delete`

后端文档大量实际写法为：

- `DELETE /resource/{id}`

这类差异会直接导致请求方法或路径错误。

### 4. 认证约定差异

前端文档只在开头写了全局说明：

- `基础路径: /api/v1 | 认证方式: JWT Bearer Token`

后端文档给了更细的接口级规则：

- 使用 `CurrentUserId` 的接口统一视为需要 `Authorization: Bearer <token>`

结论：后端文档的认证要求更细，联调时应优先参考后端文档。

---

## 二、认证模块

### 1. 登录 `POST /api/v1/auth/login`

前端文档：

- `login_id`
- `password`
- `remember_me`

后端文档：

- `username`
- `password`
- `remember_me`
- `captcha_key?`
- `captcha_text?`

差异：

- `login_id` 与 `username` 不一致
- 后端多验证码字段：`captcha_key?`、`captcha_text?`

后端返回字段：

- `access_token`
- `refresh_token`
- `token_type`
- `expires_in`
- `user`

前端文档未写返回字段。

---

### 2. 注册 `POST /api/v1/auth/register`

前端文档：

- `username`
- `email`
- `phone`
- `password`
- `confirm_password`
- `role`
- `captcha`
- `captcha_id`
- `email_code`
- `referrer_email?`

后端文档：

- `username`
- `email`
- `password`
- `role`
- `captcha_key?`
- `captcha_text?`

前端文档有、后端文档没有：

- `phone`
- `confirm_password`
- `captcha`
- `captcha_id`
- `email_code`
- `referrer_email?`

后端文档有、前端文档没有：

- `captcha_key?`
- `captcha_text?`

后端返回字段：

- `id`
- `username`
- `email`
- `nickname?`
- `avatar?`
- `role`
- `status`
- `created_at`

前端文档未写返回字段。

---

### 3. 获取图形验证码 `GET /api/v1/auth/captcha`

前端文档：

- 未写返回字段

后端文档返回：

- `captcha_key`
- `captcha_image`

---

### 4. 发送邮箱验证码 `POST /api/v1/auth/send-email-code`

前端文档：

- `email`
- `purpose` `(register/reset_password)`

后端文档：

- `email`
- `purpose`
- `captcha_key?`
- `captcha_text?`

差异：

- 后端多验证码字段
- 前端写了 `purpose` 的枚举说明，后端未写枚举

---

### 5. 重置密码 `POST /api/v1/auth/reset-password`

前端文档：

- `login_id`
- `email_code`
- `new_password`
- `confirm_password`

后端文档：

- `email`
- `code`
- `new_password`

差异：

- `login_id` vs `email`
- `email_code` vs `code`
- 前端多 `confirm_password`

---

### 6. 刷新 Token `POST /api/v1/auth/refresh`

请求字段基本一致：

- `refresh_token`

后端返回：

- `access_token`
- `token_type`
- `expires_in`

前端文档未写返回字段。

---

### 7. 退出登录 `POST /api/v1/auth/logout`

前端文档：

- 无请求字段

后端文档：

- 查询：`refresh_token?`
- 需要登录

差异：

- 后端多可选查询参数 `refresh_token?`
- 后端明确要求登录，前端文档未按接口写明

---

## 三、课程模块

### 1. 首页课程列表 `GET /api/v1/courses/homepage`

前端文档：

- `page?`
- `page_size?`

后端文档：

- `limit`

差异：

- 前端按分页参数设计
- 后端按数量限制设计

后端返回：

- `list[CourseListResponse]`

不是分页结构。

---

### 2. 课程列表 `GET /api/v1/courses`

前端文档：

- `keyword?`
- `category_id?`
- `sort_by?` `(latest/popular)`
- `page?`
- `page_size?`

后端文档：

- `category_id?`
- `is_free?`
- `page`
- `page_size`

前端文档有、后端文档没有：

- `keyword?`
- `sort_by?`

后端文档有、前端文档没有：

- `is_free?`

后端列表项字段：

- `id`
- `title`
- `subtitle?`
- `cover_url?`
- `teacher_name?`
- `price`
- `original_price?`
- `level`
- `is_free`
- `total_duration`
- `student_count`
- `rating`

前端文档未写返回项字段。

---

### 3. 课程搜索 `GET /api/v1/courses/search`

前端文档：

- `q`
- `page?`
- `page_size?`

后端文档：

- `keyword?`
- `category_id?`
- `level?`
- `is_free?`
- `min_price?`
- `max_price?`
- `sort_by`
- `sort_order`
- `page`
- `page_size`

差异：

- `q` vs `keyword?`
- 后端多了大量筛选字段

---

### 4. 课程详情 `GET /api/v1/courses/{course_id}`

前端文档：

- 未写返回字段

后端文档：

- 返回 `CourseResponse`

---

## 四、分类模块

### 1. 获取分类列表 `GET /api/v1/categories`

前端文档：

- `parent_id?`
- `is_enabled?`

后端文档：

- `parent_id?`
- `is_active?`

差异：

- `is_enabled?` vs `is_active?`

后端返回字段：

- `id`
- `name`
- `slug`
- `description?`
- `icon?`
- `sort_order`
- `parent_id?`
- `is_active`
- `created_at`

---

### 2. 后端文档有、前端文档没有

- `POST /api/v1/categories`
- `PUT /api/v1/categories/{category_id}`
- `DELETE /api/v1/categories/{category_id}`

---

## 五、学习模块

### 1. 获取继续学习信息 `GET /api/v1/learning/courses/{course_id}/continue`

前端文档：

- 未写返回字段

后端返回：

- `course_id`
- `chapter_id?`
- `section_id?`
- `resource_id?`
- `position`

---

### 2. 开始学习 `POST /api/v1/learning/courses/{course_id}/start`

前端文档：

- 未写返回字段

后端返回：

- `dict`

说明：返回结构未固定。

---

### 3. 获取资源播放地址 `GET /api/v1/learning/resources/{resource_id}/play`

前端文档：

- 未写返回字段

后端返回：

- `resource_id`
- `title`
- `play_url`
- `duration`
- `is_free`

---

### 4. 获取学习进度 `GET /api/v1/learning/progress`

前端文档：

- `section_id`
- `resource_id`

后端文档：

- `course_id`

差异：

- 查询参数完全不同

后端返回：

- `list[ProgressResponse]`

---

### 5. 保存学习进度 `POST /api/v1/learning/progress`

前端文档：

- `section_id`
- `resource_id`
- `current_time`
- `total_time`
- `is_completed`

后端文档：

- `course_id`
- `chapter_id`
- `section_id`
- `resource_id`
- `position`
- `progress`

前端文档有、后端文档没有：

- `current_time`
- `total_time`
- `is_completed`（请求字段）

后端文档有、前端文档没有：

- `course_id`
- `chapter_id`
- `position`
- `progress`

语义差异：

- 前端偏时间进度模型
- 后端偏位置 + 进度百分比模型

后端返回字段：

- `course_id`
- `chapter_id`
- `section_id`
- `resource_id`
- `progress`
- `position`
- `is_completed`
- `last_play_at?`

---

### 6. 提交反馈 `POST /api/v1/feedbacks`

前端文档：

- `feedback_type` `(system/course)`
- `course_id?`
- `content`
- `images?`

后端文档：

- `type`
- `title`
- `content`
- `contact?`
- `images?`

差异：

- `feedback_type` vs `type`
- 前端有 `course_id?`，后端没有
- 后端有 `title`、`contact?`，前端没有

后端返回字段：

- `id`
- `user_id`
- `type`
- `title`
- `content`
- `contact?`
- `images?`
- `status`
- `reply?`
- `replied_at?`
- `created_at`

---

## 六、个人中心模块

### 1. 获取个人信息 `GET /api/v1/users/me`

前端文档：

- 未写返回字段

后端返回字段：

- `id`
- `username`
- `email`
- `phone?`
- `nickname?`
- `avatar?`
- `bio?`
- `role`
- `status`
- `created_at`
- `last_login_at?`

---

### 2. 更新个人信息 `POST /api/v1/users/me`

前端文档：

- `nickname?`
- `email?`
- `phone?`
- `email_code?`
- `avatar_url?`

后端文档：

- `nickname?`
- `avatar?`
- `bio?`
- `phone?`

前端文档有、后端文档没有：

- `email?`
- `email_code?`
- `avatar_url?`

后端文档有、前端文档没有：

- `avatar?`
- `bio?`

差异：

- `avatar_url?` vs `avatar?`

---

### 3. 修改密码 `POST /api/v1/users/me/change-password`

前端文档：

- `old_password`
- `new_password`
- `confirm_password`

后端文档：

- `old_password`
- `new_password`

差异：

- 前端多 `confirm_password`

---

### 4. 获取学习记录 `GET /api/v1/users/me/learning-records`

前端文档：

- `time_range` `(recent_7/recent_30/all)`
- `page?`
- `page_size?`

后端文档：

- `page`
- `page_size`

差异：

- 前端多 `time_range`

---

### 5. 获取我的反馈 `GET /api/v1/users/me/feedbacks`

前端文档有，后端文档没有。

---

## 七、消息模块

### 1. 获取消息列表 `GET /api/v1/messages`

前端文档：

- `message_type?` `(all/announcement/notification)`
- `is_read?`
- `page?`
- `page_size?`

后端文档：

- `type?`
- `is_read?`
- `page`
- `page_size`

差异：

- `message_type?` vs `type?`
- 前端枚举与后端文档的消息类型体系可能不一致

后端未读数接口返回：

- `total`
- `system`
- `course`
- `interaction`

这说明消息类型可能并不只是 `announcement/notification`。

---

### 2. 获取消息详情 `GET /api/v1/messages/{message_id}`

前端文档未写返回字段。

后端返回字段：

- `id`
- `type`
- `title`
- `content`
- `link?`
- `is_read`
- `read_at?`
- `created_at`

---

### 3. 标记消息已读 `POST /api/v1/messages/{message_id}/read`

前端文档未写返回字段。

后端返回：

- `MessageResponse`

---

### 4. 批量标记全部已读 `POST /api/v1/messages/mark-all-read`

前端文档未写返回字段。

后端返回：

- `count`

说明：`data.count` 表示本次已读数量。

---

### 5. 删除消息

前端文档：

- `POST /api/v1/messages/{id}/delete`

后端文档：

- `DELETE /api/v1/messages/{message_id}`

差异：

- 方法不同
- 路径不同

---

### 6. 获取未读消息数 `GET /api/v1/messages/unread-count`

前端文档未写返回字段。

后端返回：

- `total`
- `system`
- `course`
- `interaction`

---

### 7. 后端文档有、前端文档没有

- `POST /api/v1/messages/send`

请求字段：

- `user_id`
- `type`
- `title`
- `content`
- `link?`

---

## 八、讲师端模块

### 1. 我的课程列表 `GET /api/v1/courses/my-courses`

前端文档：

- `status?` `(all/draft/published/archived)`
- `keyword?`
- `page?`
- `page_size?`

后端文档：

- `status?`
- `page`
- `page_size`

差异：

- 前端多 `keyword?`
- 前端给了 `status` 枚举，后端未写枚举

---

### 2. 创建课程 `POST /api/v1/courses`

前端文档：

- `title`
- `cover_url`
- `summary`
- `description?`
- `category_id`
- `tags?`

后端文档：

- `title`
- `subtitle?`
- `description?`
- `cover_url?`
- `category_id?`
- `price`
- `original_price?`
- `level`
- `is_free`
- `tag_ids?`

前端文档有、后端文档没有：

- `summary`
- `tags?`

后端文档有、前端文档没有：

- `subtitle?`
- `price`
- `original_price?`
- `level`
- `is_free`
- `tag_ids?`

差异：

- `summary` vs `subtitle?`
- `tags?` vs `tag_ids?`
- `cover_url`、`category_id` 必填性不一致

---

### 3. 更新课程 `POST /api/v1/courses/{course_id}`

前端文档：

- `title?`
- `cover_url?`
- `summary?`
- `description?`
- `category_id?`
- `tags?`

后端文档：

- `title?`
- `subtitle?`
- `description?`
- `cover_url?`
- `category_id?`
- `price?`
- `original_price?`
- `level?`
- `is_free?`
- `tag_ids?`

差异：

- `summary?` vs `subtitle?`
- `tags?` vs `tag_ids?`
- 后端多价格、等级、免费标记字段

---

### 4. 发布课程 `POST /api/v1/courses/{course_id}/publish`

前端文档未写返回字段。

后端返回：

- `CourseResponse`

---

### 5. 下架课程 `POST /api/v1/courses/{course_id}/archive`

前端文档：

- `archive_reason`

后端文档：

- 未写请求体字段

差异：

- 前端有 `archive_reason`
- 后端未体现

---

### 6. 删除课程

前端文档：

- `POST /api/v1/courses/{courseId}/delete`

后端文档：

- `DELETE /api/v1/courses/{course_id}`

差异：

- 方法不同
- 路径不同

---

## 九、章节 / 小节 / 资源 / 资料

### 1. 创建章节 `POST /api/v1/courses/{course_id}/chapters`

前端文档：

- `title`
- `sort_order?`

后端文档：

- `title`
- `description?`
- `sort_order`
- `is_free`

差异：

- 后端多 `description?`
- 后端多 `is_free`
- `sort_order` 必填性不一致

---

### 2. 更新章节 `POST /api/v1/courses/{course_id}/chapters/{chapter_id}`

前端文档：

- `title`
- `sort_order?`

后端文档：

- `title?`
- `description?`
- `sort_order?`
- `is_free?`

差异：

- 前端把 `title` 当必填，后端为可选
- 后端多 `description?`
- 后端多 `is_free?`

---

### 3. 删除章节

前端文档：

- `POST /api/v1/courses/{courseId}/chapters/{chapterId}/delete`

后端文档：

- `DELETE /api/v1/courses/{course_id}/chapters/{chapter_id}`

差异：

- 方法不同
- 路径不同

---

### 4. 更新章节排序

前端文档有：

- `POST /api/v1/courses/{courseId}/chapters/sort`
- `chapter_ids` (数组)

后端文档没有。

---

### 5. 创建小节 `POST /api/v1/courses/{course_id}/chapters/{chapter_id}/sections`

前端文档：

- `title`
- `sort_order?`

后端文档：

- `title`
- `description?`
- `sort_order`
- `is_free`

差异同创建章节。

---

### 6. 更新小节 `POST /api/v1/courses/{course_id}/chapters/{chapter_id}/sections/{section_id}`

前端文档：

- `title`
- `sort_order?`

后端文档：

- `title?`
- `description?`
- `sort_order?`
- `is_free?`

差异同更新章节。

---

### 7. 删除小节

前端文档：

- `POST /api/v1/courses/{courseId}/chapters/{chapterId}/sections/{sectionId}/delete`

后端文档：

- `DELETE /api/v1/courses/{course_id}/chapters/{chapter_id}/sections/{section_id}`

差异：

- 方法不同
- 路径不同

---

### 8. 更新小节排序

前端文档有：

- `POST /api/v1/courses/{courseId}/chapters/{chapterId}/sections/sort`
- `section_ids` (数组)

后端文档没有。

---

### 9. 上传资源到小节 `POST /api/v1/courses/{course_id}/sections/{section_id}/resources`

前端文档：

- `resource_id?`
- `resource_type` `(video/audio/document/image)`
- `file_name`
- `file_url`
- `file_size`
- `duration?`
- `resolution?`
- `thumbnail_url?`

后端文档：

- `title`
- `type`
- `file_url`
- `file_size`
- `duration`
- `sort_order`
- `is_free`

前端文档有、后端文档没有：

- `resource_id?`
- `resource_type`
- `file_name`
- `resolution?`
- `thumbnail_url?`

后端文档有、前端文档没有：

- `title`
- `type`
- `sort_order`
- `is_free`

差异：

- `resource_type` vs `type`
- `duration?` vs `duration`

---

### 10. 删除资源

前端文档：

- `POST /api/v1/courses/{courseId}/sections/{sectionId}/resources/{resourceId}/delete`

后端文档：

- `DELETE /api/v1/courses/{course_id}/sections/{section_id}/resources/{resource_id}`

差异：

- 方法不同
- 路径不同

---

### 11. 上传配套资料 `POST /api/v1/courses/{course_id}/materials`

前端文档：

- `file` (FormData)

后端文档：

- `name`
- `file_url`
- `file_size`
- `file_type?`

差异：

- 前端按文件直传设计
- 后端按文件地址和元信息设计

---

### 12. 删除配套资料

前端文档：

- `POST /api/v1/courses/{courseId}/materials/{materialId}/delete`

后端文档：

- `DELETE /api/v1/courses/{course_id}/materials/{material_id}`

差异：

- 方法不同
- 路径不同

---

### 13. 文件上传模块

前端文档有、后端文档没有：

- `POST /api/v1/upload/file`
- `POST /api/v1/upload/init`
- `POST /api/v1/upload/chunk`
- `POST /api/v1/upload/complete`

说明：前端文档定义了完整上传流程，但后端联调速查表未列出这些接口。

---

### 14. 后端文档有、前端文档没有

- `GET /api/v1/courses/{course_id}/chapters`
- `GET /api/v1/courses/{course_id}/chapters/{chapter_id}/sections`

---

## 十、标签模块

### 1. 获取标签列表 `GET /api/v1/tags`

前端文档：

- 无请求字段

后端文档：

- `keyword?`
- `page`
- `page_size`

后端返回字段：

- `id`
- `name`
- `slug`
- `color?`
- `use_count`
- `created_at`

差异：

- 前端未写查询字段
- 前端未写返回字段

---

### 2. 创建标签 `POST /api/v1/tags`

前端文档：

- `name`

后端文档：

- `name`
- `slug`
- `color?`

差异：

- 后端多 `slug`
- 后端多 `color?`

---

## 十一、管理后台模块

### 1. 用户列表 `GET /api/v1/users`

前端文档：

- `role?` `(all/student/teacher/admin/pending)`
- `status?` `(all/active/disabled/pending)`
- `keyword?`
- `page?`
- `page_size?`

后端文档：

- `keyword?`
- `role?`
- `status?`
- `page`
- `page_size`

差异：

- 前端写了枚举，后端未写枚举说明
- 前端 `role` 中的 `pending` 看起来更像状态，不像角色，需重点确认

---

### 2. 用户详情 `GET /api/v1/users/{userId}`

前端文档有，后端文档没有。

---

### 3. 更新用户状态 `POST /api/v1/users/{userId}/status`

前端文档：

- `status` `(active/disabled)`

后端文档：

- 路径：`target_user_id`
- 体：`status`

差异：

- 路径变量名不同
- 前端写了枚举说明，后端未写枚举

---

### 4. 删除用户

前端文档：

- `POST /api/v1/users/{userId}/delete`

后端文档：

- `POST /api/v1/users/{target_user_id}`

差异：

- 路径不同
- 前端多 `/delete`

---

### 5. 讲师审核列表 `GET /api/v1/users/teacher-audits`

前端文档：

- `status?` `(all/pending/approved/rejected)`
- `page?`
- `page_size?`

后端文档：

- `status?`
- `page`
- `page_size`

差异：

- 前端给了枚举，后端未写枚举

---

### 6. 审核讲师 `POST /api/v1/users/teacher-audits/{auditId}/review`

前端文档：

- `audit_status` `(approved/rejected)`
- `reject_reason?`

后端文档：

- `approve`
- `comment?`

差异：

- `audit_status` vs `approve`
- `reject_reason?` vs `comment?`

---

### 7. 角色权限

前端文档有、后端文档没有：

- `GET /api/v1/permissions/tree`
- `GET /api/v1/roles/{role}/permissions`
- `POST /api/v1/roles/{role}/permissions`

---

### 8. 公告管理

前端文档：

- `GET /api/v1/announcements`
- `GET /api/v1/announcements/{id}`
- `POST /api/v1/announcements`
- `POST /api/v1/announcements/{id}`
- `POST /api/v1/announcements/{id}/delete`

后端文档：

- `GET /api/v1/announcements`
- `GET /api/v1/announcements/active`
- `GET /api/v1/announcements/{announcement_id}`

差异：

- 前端文档包含后台管理写接口，后端文档没有
- 后端文档有 `GET /api/v1/announcements/active`，前端文档没有

字段差异：

前端列表查询：

- `status?` `(all/draft/published)`
- `keyword?`
- `page?`
- `page_size?`

后端列表查询：

- `is_published?`
- `type?`
- `page`
- `page_size`

差异：

- `status?` vs `is_published?`
- 前端有 `keyword?`，后端没有
- 后端有 `type?`，前端没有

前端创建 / 更新字段：

- `title`
- `content`
- `status` `(draft/published)`

后端列表 / 详情字段含：

- `id`
- `title`
- `content`
- `type`
- `is_top`
- `is_published`
- `publish_at?`
- `expire_at?`
- `view_count`
- `author_id?`
- `created_at`

---

### 9. 反馈管理

#### 反馈列表 `GET /api/v1/feedbacks`

前端文档：

- `feedback_type?` `(all/system/course)`
- `status?` `(all/pending/processed)`
- `keyword?`
- `page?`
- `page_size?`

后端文档：

- `status?`
- `page`
- `page_size`

前端文档有、后端文档没有：

- `feedback_type?`
- `keyword?`

---

#### 反馈详情 `GET /api/v1/feedbacks/{id}`

前端有，后端也有，主要差异是路径变量名不同：

- 前端：`{id}`
- 后端：`{feedback_id}`

---

#### 标记已处理 `POST /api/v1/feedbacks/{id}/process`

前端文档：

- 无请求字段

后端文档：

- `reply`

差异：

- 后端要求 `reply`
- 前端文档未写

---

#### 批量标记已处理

前端文档有：

- `POST /api/v1/feedbacks/batch-process`
- `feedback_ids` (数组)

后端文档没有。

---

## 十二、后端文档有、前端文档没有的重点接口汇总

- `GET /api/v1/health`
- `GET /api/v1/ping`
- `POST /api/v1/categories`
- `PUT /api/v1/categories/{category_id}`
- `DELETE /api/v1/categories/{category_id}`
- `GET /api/v1/announcements/active`
- `GET /api/v1/users/admin-applications`
- `POST /api/v1/users/admin-applications/{application_id}/review`
- `GET /api/v1/courses/{course_id}/chapters`
- `GET /api/v1/courses/{course_id}/chapters/{chapter_id}/sections`
- `GET /api/v1/learning/resources/{resource_id}/preview`
- `POST /api/v1/messages/send`

---

## 十三、前端文档有、后端文档没有的重点接口汇总

- `GET /api/v1/users/me/feedbacks`
- `POST /api/v1/courses/{courseId}/chapters/sort`
- `POST /api/v1/courses/{courseId}/chapters/{chapterId}/sections/sort`
- `POST /api/v1/upload/file`
- `POST /api/v1/upload/init`
- `POST /api/v1/upload/chunk`
- `POST /api/v1/upload/complete`
- `GET /api/v1/users/{userId}`
- `GET /api/v1/permissions/tree`
- `GET /api/v1/roles/{role}/permissions`
- `POST /api/v1/roles/{role}/permissions`
- `POST /api/v1/announcements`
- `POST /api/v1/announcements/{id}`
- `POST /api/v1/announcements/{id}/delete`
- `POST /api/v1/feedbacks/batch-process`

---

## 十四、建议优先确认项

建议前后端先优先对齐以下接口：

1. `POST /api/v1/auth/login`
2. `POST /api/v1/auth/register`
3. `POST /api/v1/auth/reset-password`
4. `GET /api/v1/courses/homepage`
5. `GET /api/v1/courses/search`
6. `GET /api/v1/learning/progress`
7. `POST /api/v1/learning/progress`
8. `POST /api/v1/feedbacks`
9. `POST /api/v1/users/me`
10. `POST /api/v1/courses`
11. `POST /api/v1/courses/{course_id}`
12. `POST /api/v1/courses/{course_id}/sections/{section_id}/resources`
13. `POST /api/v1/users/teacher-audits/{audit_id}/review`

这些接口差异最大，最容易造成联调失败。
