# 前后端接口文档差异对照（表格版）

对照文档：

- 前端文档：`E:\video_project\UI\前端接口文档.md`
- 后端文档：`E:\video_project\UI\api-frontend-quick-reference.md`

---

## 一、总览统计

### 1. 差异最严重的模块

| 优先级 | 模块 | 主要问题 |
|--------|------|----------|
| 1 | 认证模块 | 字段名不一致、验证码体系不同 |
| 2 | 课程模块 | 参数设计差异、搜索接口参数完全不同 |
| 3 | 学习模块 | 进度模型语义差异、参数完全不同 |
| 4 | 讲师端课程内容模块 | 字段名/必填性不一致、删除类接口方法错误 |
| 5 | 管理后台模块 | 枚举定义缺失、路径变量名不一致 |

### 2. 高风险接口清单

| 接口路径 | 风险类型 | 严重程度 |
|----------|----------|----------|
| `POST /api/v1/auth/login` | 字段名不一致 (`login_id` vs `username`) | 🔴 高 |
| `POST /api/v1/auth/register` | 大量字段差异、验证码体系不同 | 🔴 高 |
| `POST /api/v1/auth/reset-password` | 字段名完全不一致 | 🔴 高 |
| `GET /api/v1/courses/homepage` | 参数设计差异 (`page/page_size` vs `limit`) | 🔴 高 |
| `GET /api/v1/courses/search` | 参数完全不同 (`q` vs `keyword`) | 🔴 高 |
| `GET /api/v1/learning/progress` | 查询参数完全不同 | 🔴 高 |
| `POST /api/v1/learning/progress` | 字段名/语义差异 | 🔴 高 |
| `POST /api/v1/feedbacks` | 字段名不一致 (`feedback_type` vs `type`) | 🔴 高 |
| `POST /api/v1/users/me` | 字段名不一致 (`avatar_url` vs `avatar`) | 🔴 高 |
| `POST /api/v1/courses` | 必填性/字段名不一致 | 🔴 高 |
| `POST /api/v1/courses/{course_id}` | 字段名不一致 (`summary` vs `subtitle`) | 🔴 高 |
| `POST /api/v1/courses/{course_id}/sections/{section_id}/resources` | 字段名/必填性不一致 | 🔴 高 |
| `POST /api/v1/users/teacher-audits/{audit_id}/review` | 字段名完全不一致 | 🔴 高 |

### 3. 删除类接口差异汇总

| 前端文档写法 | 后端文档写法 | 影响范围 |
|--------------|--------------|----------|
| `POST .../delete` | `DELETE /resource/{id}` | 所有删除接口 |

**受影响的删除接口：**

| 资源类型 | 前端路径 | 后端路径 |
|----------|----------|----------|
| 课程 | `POST /courses/{courseId}/delete` | `DELETE /courses/{course_id}` |
| 章节 | `POST /courses/{courseId}/chapters/{chapterId}/delete` | `DELETE /courses/{course_id}/chapters/{chapter_id}` |
| 小节 | `POST /courses/{courseId}/chapters/{chapterId}/sections/{sectionId}/delete` | `DELETE /courses/{course_id}/chapters/{chapter_id}/sections/{section_id}` |
| 资源 | `POST /courses/{courseId}/sections/{sectionId}/resources/{resourceId}/delete` | `DELETE /courses/{course_id}/sections/{section_id}/resources/{resource_id}` |
| 配套资料 | `POST /courses/{courseId}/materials/{materialId}/delete` | `DELETE /courses/{course_id}/materials/{material_id}` |
| 消息 | `POST /messages/{id}/delete` | `DELETE /messages/{message_id}` |
| 分类 | 前端无 | `DELETE /categories/{category_id}` |

---

## 二、认证模块差异详情

### 登录 `POST /api/v1/auth/login`

| 字段 | 前端文档 | 后端文档 | 差异说明 |
|------|----------|----------|----------|
| 用户标识 | `login_id` | `username` | 🔴 字段名不一致 |
| 密码 | `password` | `password` | ✅ 一致 |
| 记住我 | `remember_me` | `remember_me` | ✅ 一致 |
| 验证码Key | ❌ 无 | `captcha_key?` | 🟡 后端额外字段 |
| 验证码文本 | ❌ 无 | `captcha_text?` | 🟡 后端额外字段 |

**返回字段（前端未写）：**

| 字段 | 后端返回 |
|------|----------|
| `access_token` | ✅ |
| `refresh_token` | ✅ |
| `token_type` | ✅ |
| `expires_in` | ✅ |
| `user` | ✅ |

---

### 注册 `POST /api/v1/auth/register`

| 字段 | 前端文档 | 后端文档 | 差异说明 |
|------|----------|----------|----------|
| `username` | ✅ 必填 | ✅ 必填 | ✅ 一致 |
| `email` | ✅ 必填 | ✅ 必填 | ✅ 一致 |
| `password` | ✅ 必填 | ✅ 必填 | ✅ 一致 |
| `role` | ✅ 必填 | ✅ 必填 | ✅ 一致 |
| `phone` | ✅ 有 | ❌ 无 | 🟡 前端额外字段 |
| `confirm_password` | ✅ 有 | ❌ 无 | 🟡 前端额外字段 |
| `captcha` | ✅ 有 | ❌ 无 | 🔴 字段名体系不同 |
| `captcha_id` | ✅ 有 | ❌ 无 | 🔴 字段名体系不同 |
| `captcha_key?` | ❌ 无 | ✅ 有 | 🔴 字段名体系不同 |
| `captcha_text?` | ❌ 无 | ✅ 有 | 🔴 字段名体系不同 |
| `email_code` | ✅ 有 | ❌ 无 | 🟡 前端额外字段 |
| `referrer_email?` | ✅ 有 | ❌ 无 | 🟡 前端额外字段 |

---

### 发送邮箱验证码 `POST /api/v1/auth/send-email-code`

| 字段 | 前端文档 | 后端文档 | 差异说明 |
|------|----------|----------|----------|
| `email` | ✅ | ✅ | ✅ 一致 |
| `purpose` | ✅ `(register/reset_password)` | ✅ 无枚举说明 | 🟡 前端有枚举 |
| `captcha_key?` | ❌ 无 | ✅ 有 | 🟡 后端额外字段 |
| `captcha_text?` | ❌ 无 | ✅ 有 | 🟡 后端额外字段 |

---

### 重置密码 `POST /api/v1/auth/reset-password`

| 字段 | 前端文档 | 后端文档 | 差异说明 |
|------|----------|----------|----------|
| 用户标识 | `login_id` | `email` | 🔴 字段名不一致 |
| 验证码 | `email_code` | `code` | 🔴 字段名不一致 |
| 新密码 | `new_password` | `new_password` | ✅ 一致 |
| 确认密码 | `confirm_password` | ❌ 无 | 🟡 前端额外字段 |

---

### 退出登录 `POST /api/v1/auth/logout`

| 字段 | 前端文档 | 后端文档 | 差异说明 |
|------|----------|----------|----------|
| 请求体 | 无字段 | 无字段 | ✅ 一致 |
| 查询参数 | ❌ 无 | `refresh_token?` | 🟡 后端额外字段 |
| 认证要求 | 未明确写 | ✅ 需要登录 | 🟡 后端明确 |

---

## 三、课程模块差异详情

### 首页课程列表 `GET /api/v1/courses/homepage`

| 字段 | 前端文档 | 后端文档 | 差异说明 |
|------|----------|----------|----------|
| 分页参数 | `page?`, `page_size?` | ❌ 无 | 🔴 参数设计差异 |
| 数量限制 | ❌ 无 | `limit` | 🔴 参数设计差异 |
| 返回结构 | 分页结构 | `list[CourseListResponse]` | 🔴 不是分页结构 |

---

### 课程列表 `GET /api/v1/courses`

| 字段 | 前端文档 | 后端文档 | 差异说明 |
|------|----------|----------|----------|
| `keyword?` | ✅ 有 | ❌ 无 | 🟡 前端额外字段 |
| `category_id?` | ✅ 有 | ✅ 有 | ✅ 一致 |
| `sort_by?` | ✅ `(latest/popular)` | ❌ 无 | 🟡 前端额外字段 |
| `is_free?` | ❌ 无 | ✅ 有 | 🟡 后端额外字段 |
| `page?` | ✅ 有 | ✅ 有 | ✅ 一致 |
| `page_size?` | ✅ 有 | ✅ 有 | ✅ 一致 |

---

### 课程搜索 `GET /api/v1/courses/search`

| 字段 | 前端文档 | 后端文档 | 差异说明 |
|------|----------|----------|----------|
| 关键词 | `q` | `keyword?` | 🔴 字段名不一致 |
| `category_id?` | ❌ 无 | ✅ 有 | 🟡 后端额外字段 |
| `level?` | ❌ 无 | ✅ 有 | 🟡 后端额外字段 |
| `is_free?` | ❌ 无 | ✅ 有 | 🟡 后端额外字段 |
| `min_price?` | ❌ 无 | ✅ 有 | 🟡 后端额外字段 |
| `max_price?` | ❌ 无 | ✅ 有 | 🟡 后端额外字段 |
| `sort_by` | ❌ 无 | ✅ 有 | 🟡 后端额外字段 |
| `sort_order` | ❌ 无 | ✅ 有 | 🟡 后端额外字段 |
| `page` | ✅ 有 | ✅ 有 | ✅ 一致 |
| `page_size` | ✅ 有 | ✅ 有 | ✅ 一致 |

---

## 四、分类模块差异详情

### 获取分类列表 `GET /api/v1/categories`

| 字段 | 前端文档 | 后端文档 | 差异说明 |
|------|----------|----------|----------|
| `parent_id?` | ✅ 有 | ✅ 有 | ✅ 一致 |
| 启用状态 | `is_enabled?` | `is_active?` | 🔴 字段名不一致 |

---

## 五、学习模块差异详情

### 获取学习进度 `GET /api/v1/learning/progress`

| 字段 | 前端文档 | 后端文档 | 差异说明 |
|------|----------|----------|----------|
| `section_id` | ✅ 有 | ❌ 无 | 🔴 参数完全不同 |
| `resource_id` | ✅ 有 | ❌ 无 | 🔴 参数完全不同 |
| `course_id` | ❌ 无 | ✅ 有 | 🔴 参数完全不同 |

---

### 保存学习进度 `POST /api/v1/learning/progress`

| 字段 | 前端文档 | 后端文档 | 差异说明 |
|------|----------|----------|----------|
| `course_id` | ❌ 无 | ✅ 必填 | 🔴 后端额外字段 |
| `chapter_id` | ❌ 无 | ✅ 必填 | 🔴 后端额外字段 |
| `section_id` | ✅ 有 | ✅ 必填 | 🟡 必填性不同 |
| `resource_id` | ✅ 有 | ✅ 必填 | 🟡 必填性不同 |
| 当前时间 | `current_time` | ❌ 无 | 🔴 字段名不一致 |
| 总时间 | `total_time` | ❌ 无 | 🔴 字段名不一致 |
| 位置 | ❌ 无 | `position` | 🔴 字段名不一致 |
| 进度 | ❌ 无 | `progress` | 🔴 字段名不一致 |
| 完成标记 | `is_completed` | ❌ 无（请求） | 🟡 前端额外字段 |

**语义差异：**
- 前端：时间进度模型 (`current_time`, `total_time`)
- 后端：位置 + 进度百分比模型 (`position`, `progress`)

---

### 提交反馈 `POST /api/v1/feedbacks`

| 字段 | 前端文档 | 后端文档 | 差异说明 |
|------|----------|----------|----------|
| 类型 | `feedback_type` `(system/course)` | `type` | 🔴 字段名不一致 |
| `title` | ❌ 无 | ✅ 必填 | 🟡 后端额外字段 |
| `content` | ✅ 必填 | ✅ 必填 | ✅ 一致 |
| `course_id?` | ✅ 有 | ❌ 无 | 🟡 前端额外字段 |
| `contact?` | ❌ 无 | ✅ 有 | 🟡 后端额外字段 |
| `images?` | ✅ 有 | ✅ 有 | ✅ 一致 |

---

## 六、个人中心模块差异详情

### 更新个人信息 `POST /api/v1/users/me`

| 字段 | 前端文档 | 后端文档 | 差异说明 |
|------|----------|----------|----------|
| `nickname?` | ✅ 有 | ✅ 有 | ✅ 一致 |
| 头像 | `avatar_url?` | `avatar?` | 🔴 字段名不一致 |
| `bio?` | ❌ 无 | ✅ 有 | 🟡 后端额外字段 |
| `phone?` | ✅ 有 | ✅ 有 | ✅ 一致 |
| `email?` | ✅ 有 | ❌ 无 | 🟡 前端额外字段 |
| `email_code?` | ✅ 有 | ❌ 无 | 🟡 前端额外字段 |

---

### 修改密码 `POST /api/v1/users/me/change-password`

| 字段 | 前端文档 | 后端文档 | 差异说明 |
|------|----------|----------|----------|
| `old_password` | ✅ 必填 | ✅ 必填 | ✅ 一致 |
| `new_password` | ✅ 必填 | ✅ 必填 | ✅ 一致 |
| `confirm_password` | ✅ 必填 | ❌ 无 | 🟡 前端额外字段 |

---

### 获取学习记录 `GET /api/v1/users/me/learning-records`

| 字段 | 前端文档 | 后端文档 | 差异说明 |
|------|----------|----------|----------|
| `time_range` | ✅ `(recent_7/recent_30/all)` | ❌ 无 | 🟡 前端额外字段 |
| `page?` | ✅ 有 | ✅ 必填 | 🟡 必填性不同 |
| `page_size?` | ✅ 有 | ✅ 必填 | 🟡 必填性不同 |

---

## 七、消息模块差异详情

### 获取消息列表 `GET /api/v1/messages`

| 字段 | 前端文档 | 后端文档 | 差异说明 |
|------|----------|----------|----------|
| 类型 | `message_type?` `(all/announcement/notification)` | `type?` | 🔴 字段名不一致 |
| `is_read?` | ✅ 有 | ✅ 有 | ✅ 一致 |
| `page?` | ✅ 有 | ✅ 必填 | 🟡 必填性不同 |
| `page_size?` | ✅ 有 | ✅ 必填 | 🟡 必填性不同 |

**枚举体系差异：** 前端枚举 `announcement/notification`，后端未读数接口返回 `system/course/interaction`，类型体系可能不一致。

---

## 八、讲师端模块差异详情

### 我的课程列表 `GET /api/v1/courses/my-courses`

| 字段 | 前端文档 | 后端文档 | 差异说明 |
|------|----------|----------|----------|
| `status?` | ✅ `(all/draft/published/archived)` | ✅ 有（无枚举） | 🟡 前端有枚举 |
| `keyword?` | ✅ 有 | ❌ 无 | 🟡 前端额外字段 |
| `page?` | ✅ 有 | ✅ 必填 | 🟡 必填性不同 |
| `page_size?` | ✅ 有 | ✅ 必填 | 🟡 必填性不同 |

---

### 创建课程 `POST /api/v1/courses`

| 字段 | 前端文档 | 后端文档 | 差异说明 |
|------|----------|----------|----------|
| `title` | ✅ 必填 | ✅ 必填 | ✅ 一致 |
| 简介 | `summary` ✅ 必填 | `subtitle?` | 🔴 字段名+必填性不一致 |
| `description?` | ✅ 有 | ✅ 有 | ✅ 一致 |
| `cover_url` | ✅ 必填 | `cover_url?` | 🟡 必填性不一致 |
| `category_id` | ✅ 必填 | `category_id?` | 🟡 必填性不一致 |
| 标签 | `tags?` | `tag_ids?` | 🔴 字段名不一致 |
| `price` | ❌ 无 | ✅ 必填 | 🔴 后端额外必填字段 |
| `original_price?` | ❌ 无 | ✅ 有 | 🟡 后端额外字段 |
| `level` | ❌ 无 | ✅ 必填 | 🔴 后端额外必填字段 |
| `is_free` | ❌ 无 | ✅ 必填 | 🔴 后端额外必填字段 |

---

### 更新课程 `POST /api/v1/courses/{course_id}`

| 字段 | 前端文档 | 后端文档 | 差异说明 |
|------|----------|----------|----------|
| `title?` | ✅ 有 | ✅ 有 | ✅ 一致 |
| 简介 | `summary?` | `subtitle?` | 🔴 字段名不一致 |
| `description?` | ✅ 有 | ✅ 有 | ✅ 一致 |
| `cover_url?` | ✅ 有 | ✅ 有 | ✅ 一致 |
| `category_id?` | ✅ 有 | ✅ 有 | ✅ 一致 |
| 标签 | `tags?` | `tag_ids?` | 🔴 字段名不一致 |
| `price?` | ❌ 无 | ✅ 有 | 🟡 后端额外字段 |
| `original_price?` | ❌ 无 | ✅ 有 | 🟡 后端额外字段 |
| `level?` | ❌ 无 | ✅ 有 | 🟡 后端额外字段 |
| `is_free?` | ❌ 无 | ✅ 有 | 🟡 后端额外字段 |

---

### 下架课程 `POST /api/v1/courses/{course_id}/archive`

| 字段 | 前端文档 | 后端文档 | 差异说明 |
|------|----------|----------|----------|
| `archive_reason` | ✅ 有 | ❌ 未体现 | 🟡 前端额外字段 |

---

## 九、章节/小节/资源模块差异详情

### 创建章节 `POST /api/v1/courses/{course_id}/chapters`

| 字段 | 前端文档 | 后端文档 | 差异说明 |
|------|----------|----------|----------|
| `title` | ✅ 必填 | ✅ 必填 | ✅ 一致 |
| `description?` | ❌ 无 | ✅ 有 | 🟡 后端额外字段 |
| `sort_order?` | ✅ 有 | ✅ 必填 | 🟡 必填性不一致 |
| `is_free` | ❌ 无 | ✅ 必填 | 🔴 后端额外必填字段 |

---

### 更新章节 `POST /api/v1/courses/{course_id}/chapters/{chapter_id}`

| 字段 | 前端文档 | 后端文档 | 差异说明 |
|------|----------|----------|----------|
| `title` | ✅ 必填 | `title?` | 🟡 必填性不一致 |
| `description?` | ❌ 无 | ✅ 有 | 🟡 后端额外字段 |
| `sort_order?` | ✅ 有 | ✅ 有 | ✅ 一致 |
| `is_free?` | ❌ 无 | ✅ 有 | 🟡 后端额外字段 |

---

### 创建小节 `POST /api/v1/courses/{course_id}/chapters/{chapter_id}/sections`

| 字段 | 前端文档 | 后端文档 | 差异说明 |
|------|----------|----------|----------|
| `title` | ✅ 必填 | ✅ 必填 | ✅ 一致 |
| `description?` | ❌ 无 | ✅ 有 | 🟡 后端额外字段 |
| `sort_order?` | ✅ 有 | ✅ 必填 | 🟡 必填性不一致 |
| `is_free` | ❌ 无 | ✅ 必填 | 🔴 后端额外必填字段 |

---

### 上传资源到小节 `POST /api/v1/courses/{course_id}/sections/{section_id}/resources`

| 字段 | 前端文档 | 后端文档 | 差异说明 |
|------|----------|----------|----------|
| `resource_id?` | ✅ 有 | ❌ 无 | 🟡 前端额外字段 |
| 类型 | `resource_type` `(video/audio/document/image)` | `type` | 🔴 字段名不一致 |
| `title` | ❌ 无 | ✅ 必填 | 🔴 后端额外必填字段 |
| `file_name` | ✅ 有 | ❌ 无 | 🟡 前端额外字段 |
| `file_url` | ✅ 必填 | ✅ 必填 | ✅ 一致 |
| `file_size` | ✅ 必填 | ✅ 必填 | ✅ 一致 |
| 时长 | `duration?` | `duration` 必填 | 🟡 必填性不一致 |
| `resolution?` | ✅ 有 | ❌ 无 | 🟡 前端额外字段 |
| `thumbnail_url?` | ✅ 有 | ❌ 无 | 🟡 前端额外字段 |
| `sort_order` | ❌ 无 | ✅ 必填 | 🔴 后端额外必填字段 |
| `is_free` | ❌ 无 | ✅ 必填 | 🔴 后端额外必填字段 |

---

### 上传配套资料 `POST /api/v1/courses/{course_id}/materials`

| 字段 | 前端文档 | 后端文档 | 差异说明 |
|------|----------|----------|----------|
| 上传方式 | `file` (FormData) | `file_url` + 元信息 | 🔴 设计理念不同 |
| `name` | ❌ 无 | ✅ 必填 | 🔴 后端额外字段 |
| `file_url` | ❌ 无 | ✅ 必填 | 🔴 后端额外字段 |
| `file_size` | ❌ 无 | ✅ 必填 | 🔴 后端额外字段 |
| `file_type?` | ❌ 无 | ✅ 有 | 🟡 后端额外字段 |

---

## 十、管理后台模块差异详情

### 用户列表 `GET /api/v1/users`

| 字段 | 前端文档 | 后端文档 | 差异说明 |
|------|----------|----------|----------|
| `role?` | ✅ `(all/student/teacher/admin/pending)` | ✅ 有（无枚举） | 🟡 前端有枚举，`pending` 像状态非角色 |
| `status?` | ✅ `(all/active/disabled/pending)` | ✅ 有（无枚举） | 🟡 前端有枚举 |
| `keyword?` | ✅ 有 | ✅ 有 | ✅ 一致 |
| `page?` | ✅ 有 | ✅ 必填 | 🟡 必填性不同 |
| `page_size?` | ✅ 有 | ✅ 必填 | 🟡 必填性不同 |

---

### 更新用户状态 `POST /api/v1/users/{userId}/status`

| 字段 | 前端文档 | 后端文档 | 差异说明 |
|------|----------|----------|----------|
| 路径变量 | `{userId}` | `{target_user_id}` | 🔴 路径变量名不一致 |
| `status` | ✅ `(active/disabled)` | ✅ 有（无枚举） | 🟡 前端有枚举 |

---

### 删除用户

| 项目 | 前端文档 | 后端文档 |
|------|----------|----------|
| 路径 | `POST /api/v1/users/{userId}/delete` | `POST /api/v1/users/{target_user_id}` |
| 方法 | POST | POST |
| 路径 | 多 `/delete` | 无 `/delete` |

---

### 审核讲师 `POST /api/v1/users/teacher-audits/{auditId}/review`

| 字段 | 前端文档 | 后端文档 | 差异说明 |
|------|----------|----------|----------|
| 路径变量 | `{auditId}` | `{audit_id}` | 🟡 命名风格不一致 |
| 审核结果 | `audit_status` `(approved/rejected)` | `approve` (boolean) | 🔴 字段名+类型不一致 |
| 拒绝原因 | `reject_reason?` | `comment?` | 🔴 字段名不一致 |

---

### 公告管理差异汇总

| 接口 | 前端文档 | 后端文档 | 差异说明 |
|------|----------|----------|----------|
| 列表查询状态 | `status?` `(all/draft/published)` | `is_published?` | 🔴 字段名不一致 |
| 关键词搜索 | `keyword?` ✅ | ❌ 无 | 🟡 前端额外字段 |
| 类型筛选 | ❌ 无 | `type?` ✅ | 🟡 后端额外字段 |
| 创建/更新状态 | `status` `(draft/published)` | `is_published` | 🔴 字段名不一致 |
| 激活公告 | ❌ 无 | `GET /announcements/active` | 🟡 后端额外接口 |

---

### 反馈管理差异汇总

| 接口/字段 | 前端文档 | 后端文档 | 差异说明 |
|-----------|----------|----------|----------|
| 列表类型筛选 | `feedback_type?` `(all/system/course)` | ❌ 无 | 🟡 前端额外字段 |
| 列表关键词 | `keyword?` ✅ | ❌ 无 | 🟡 前端额外字段 |
| 详情路径 | `{id}` | `{feedback_id}` | 🟡 路径变量名不一致 |
| 处理时回复 | ❌ 无 | `reply` 必填 | 🔴 后端要求回复 |
| 批量处理 | ✅ `POST /batch-process` | ❌ 无 | 🟡 前端额外接口 |

---

## 十一、接口存在性差异汇总

### 后端有、前端无

| 接口路径 | 功能说明 |
|----------|----------|
| `GET /api/v1/health` | 健康检查 |
| `GET /api/v1/ping` | Ping 测试 |
| `POST /api/v1/categories` | 创建分类 |
| `PUT /api/v1/categories/{category_id}` | 更新分类 |
| `DELETE /api/v1/categories/{category_id}` | 删除分类 |
| `GET /api/v1/announcements/active` | 获取激活公告 |
| `GET /api/v1/users/admin-applications` | 管理员申请列表 |
| `POST /api/v1/users/admin-applications/{application_id}/review` | 审核管理员申请 |
| `GET /api/v1/courses/{course_id}/chapters` | 获取章节列表 |
| `GET /api/v1/courses/{course_id}/chapters/{chapter_id}/sections` | 获取小节列表 |
| `GET /api/v1/learning/resources/{resource_id}/preview` | 资源预览 |
| `POST /api/v1/messages/send` | 发送消息 |

### 前端有、后端无

| 接口路径 | 功能说明 |
|----------|----------|
| `GET /api/v1/users/me/feedbacks` | 获取我的反馈 |
| `POST /api/v1/courses/{courseId}/chapters/sort` | 更新章节排序 |
| `POST /api/v1/courses/{courseId}/chapters/{chapterId}/sections/sort` | 更新小节排序 |
| `POST /api/v1/upload/file` | 文件上传 |
| `POST /api/v1/upload/init` | 分片上传初始化 |
| `POST /api/v1/upload/chunk` | 分片上传 |
| `POST /api/v1/upload/complete` | 分片上传完成 |
| `GET /api/v1/users/{userId}` | 用户详情 |
| `GET /api/v1/permissions/tree` | 权限树 |
| `GET /api/v1/roles/{role}/permissions` | 角色权限 |
| `POST /api/v1/roles/{role}/permissions` | 更新角色权限 |
| `POST /api/v1/announcements` | 创建公告 |
| `POST /api/v1/announcements/{id}` | 更新公告 |
| `POST /api/v1/announcements/{id}/delete` | 删除公告 |
| `POST /api/v1/feedbacks/batch-process` | 批量处理反馈 |

---

## 十二、建议优先确认清单

以下接口差异最大，最容易造成联调失败，建议优先对齐：

| 序号 | 接口路径 | 主要差异 |
|------|----------|----------|
| 1 | `POST /api/v1/auth/login` | `login_id` vs `username` |
| 2 | `POST /api/v1/auth/register` | 验证码体系不同、大量字段差异 |
| 3 | `POST /api/v1/auth/reset-password` | 字段名完全不一致 |
| 4 | `GET /api/v1/courses/homepage` | 分页 vs 数量限制 |
| 5 | `GET /api/v1/courses/search` | `q` vs `keyword`、筛选参数差异 |
| 6 | `GET /api/v1/learning/progress` | 查询参数完全不同 |
| 7 | `POST /api/v1/learning/progress` | 时间模型 vs 位置模型 |
| 8 | `POST /api/v1/feedbacks` | `feedback_type` vs `type` |
| 9 | `POST /api/v1/users/me` | `avatar_url` vs `avatar` |
| 10 | `POST /api/v1/courses` | 必填字段差异大 |
| 11 | `POST /api/v1/courses/{course_id}` | `summary` vs `subtitle` |
| 12 | `POST /api/v1/courses/{course_id}/sections/{section_id}/resources` | 字段名+必填性差异 |
| 13 | `POST /api/v1/users/teacher-audits/{audit_id}/review` | `audit_status` vs `approve` |

---

## 附录：差异标记说明

| 标记 | 含义 | 示例 |
|------|------|------|
| 🔴 高 | 字段名完全不一致、参数设计差异、会导致请求失败 | `login_id` vs `username` |
| 🟡 中 | 字段存在性差异、必填性不一致、枚举说明缺失 | 前端有后端无、前端可选后端必填 |
| ✅ 一致 | 字段名和语义一致 | `password` |
| ❌ 无 | 该文档未定义该字段 | - |