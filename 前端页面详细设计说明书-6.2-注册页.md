# 前端页面详细设计说明书 - 6.2 注册页

> 文档版本：v1.0 | 生成时间：2026-03-24 | 技术栈：Vue 3 + TypeScript + Element Plus + Pinia

---

## 1. 页面概述

| 属性 | 说明 |
|------|------|
| 页面名称 | 注册页 |
| 路由地址 | `/register` |
| 路由名称 | `Register` |
| 路由文件 | `src/views/auth/RegisterPage.vue` |
| 访问权限 | 公开（仅未登录用户访问；已登录用户访问时重定向至首页） |
| 页面目标 | 支持学员 / 讲师 / 管理员注册，覆盖完整校验、验证码交互和审核状态提示 |

---

## 2. UI 组件结构图

```
RegisterPage.vue
├── AuthLayout.vue                          ← 认证页通用布局（左侧品牌区 + 右侧表单区）
│   ├── BrandSide                           ← 左侧：平台 Logo + 标语 + 背景插画
│   └── FormSide                            ← 右侧：注册表单容器
│
├── RegisterForm.vue                        ← 注册表单核心组件
│   └── el-form (ref="registerFormRef")
│       ├── RolePicker                      ← 角色选择器
│       │   └── el-radio-group
│       │       ├── el-radio-button[学员]
│       │       ├── el-radio-button[讲师]
│       │       └── el-radio-button[管理员]
│       │
│       ├── UsernameField                   ← 用户名输入
│       │   └── el-form-item + el-input
│       │       └── prefix-icon: User
│       │
│       ├── PasswordField                   ← 密码输入
│       │   └── el-form-item + el-input[type=password, show-password]
│       │       └── prefix-icon: Lock
│       │
│       ├── ConfirmPasswordField            ← 确认密码
│       │   └── el-form-item + el-input[type=password, show-password]
│       │       └── prefix-icon: Lock
│       │
│       ├── PhoneField                      ← 手机号输入
│       │   └── el-form-item + el-input
│       │       └── prefix-icon: Phone
│       │
│       ├── EmailField                      ← 邮箱输入
│       │   └── el-form-item + el-input
│       │       └── prefix-icon: Message
│       │
│       ├── CaptchaField                    ← 图形验证码
│       │   └── el-form-item
│       │       ├── el-input (4位验证码)
│       │       └── CaptchaImage.vue        ← 验证码图片（可点击刷新）
│       │           └── <img :src="captchaImage" @click="refreshCaptcha" />
│       │
│       ├── EmailCodeField                  ← 邮箱验证码
│       │   └── el-form-item
│       │       ├── el-input (6位验证码)
│       │       └── el-button (发送验证码 / 倒计时)
│       │
│       ├── [管理员] ReferrerEmailField     ← 推荐管理员邮箱（条件渲染）
│       │   └── el-form-item + el-input
│       │       └── prefix-icon: Promotion
│       │
│       ├── SubmitButton                    ← 注册按钮
│       │   └── el-button[type=primary, :loading="isSubmitting"]
│       │
│       └── LoginLink                       ← 已有账号？去登录
│           └── router-link[to="/login"]
│
└── TeacherPendingDialog.vue               ← 讲师审核中提示弹窗
    └── el-dialog
        ├── 审核中图标 + 提示文案
        └── el-button (我知道了 → 跳转首页)
```

---

## 3. 数据模型 (TypeScript)

### 3.1 API 请求 / 响应类型

```typescript
/** 注册请求参数 */
interface RegisterRequest {
  username: string
  email: string
  phone: string
  password: string
  confirm_password: string
  role: 'student' | 'teacher' | 'admin'
  captcha: string
  email_code: string
  referrer_email?: string  // 管理员注册时必填
}

/** 注册成功响应 */
interface RegisterResponse {
  user_id: number
  username: string
  email: string
  role: string
  status: 'active' | 'pending'
  access_token: string
  refresh_token: string
  token_type: string
  expires_in: number
}

/** 图形验证码响应 */
interface CaptchaResponse {
  captcha_id: string
  captcha_image: string  // Base64 data URL
}

/** 发送邮箱验证码请求 */
interface SendEmailCodeRequest {
  email: string
  purpose: 'register' | 'reset_password'
}

/** 发送邮箱验证码响应 */
interface SendEmailCodeResponse {
  expires_in: number  // 秒
}
```

### 3.2 表单校验规则类型

```typescript
/** 注册表单字段 */
interface RegisterFormData {
  role: 'student' | 'teacher' | 'admin'
  username: string
  password: string
  confirmPassword: string
  phone: string
  email: string
  captcha: string
  captchaId: string      // 隐藏字段，图形验证码 ID
  emailCode: string
  referrerEmail: string  // 管理员专用
}
```

---

## 4. 状态管理逻辑

### 4.1 Pinia State（全局状态）

注册成功后写入 `useUserStore`：

```typescript
// 注册成功 → 调用 userStore.setLoginInfo(response)
interface UserStoreActions {
  setLoginInfo(data: RegisterResponse): void
  // 将 access_token, refresh_token, user_id, role, status 等存入 store + localStorage
}
```

### 4.2 Local State（组件私有状态）

```typescript
interface RegisterPageState {
  // 表单数据
  formData: RegisterFormData

  // UI 状态
  isSubmitting: boolean            // 注册按钮 loading
  isSendingCode: boolean           // 发送验证码按钮 loading
  emailCodeCountdown: number       // 邮箱验证码倒计时（60s）
  captchaImage: string             // 图形验证码 Base64 图片
  captchaId: string                // 图形验证码 ID
  captchaRefreshCooldown: boolean  // 验证码刷新冷却（4s）

  // 弹窗控制
  showTeacherPendingDialog: boolean  // 讲师审核中弹窗
}
```

---

## 5. 核心业务逻辑与交互

### 5.1 初始化流程 (onMounted)

```
onMounted
 ├─ 1. 检查登录态：若已登录 → router.replace('/')
 ├─ 2. 加载图形验证码
 │    └─ GET /api/v1/auth/captcha
 │    └─ 将 captcha_id 存入 formData.captchaId
 │    └─ 将 captcha_image 存入 captchaImage
 └─ 3. 默认选中角色"学员"
      └─ formData.role = 'student'
```

### 5.2 表单校验规则

```typescript
const registerRules: FormRules = {
  username: [
    { required: true, message: '请输入用户名', trigger: 'blur' },
    { min: 4, max: 20, message: '用户名长度为 4-20 位', trigger: 'blur' },
    { pattern: /^[a-zA-Z0-9_]+$/, message: '仅支持字母、数字和下划线', trigger: 'blur' }
  ],
  password: [
    { required: true, message: '请输入密码', trigger: 'blur' },
    { min: 8, max: 20, message: '密码长度为 8-20 位', trigger: 'blur' },
    { pattern: /^(?=.*[a-zA-Z])(?=.*\d).+$/, message: '密码需包含字母和数字', trigger: 'blur' }
  ],
  confirmPassword: [
    { required: true, message: '请确认密码', trigger: 'blur' },
    { validator: validateConfirmPassword, trigger: 'blur' }
    // 自定义校验：value !== formData.password → '两次输入的密码不一致'
  ],
  phone: [
    { required: true, message: '请输入手机号', trigger: 'blur' },
    { pattern: /^1[3-9]\d{9}$/, message: '请输入正确的 11 位手机号', trigger: 'blur' }
  ],
  email: [
    { required: true, message: '请输入邮箱', trigger: 'blur' },
    { type: 'email', message: '请输入正确的邮箱地址', trigger: 'blur' }
  ],
  captcha: [
    { required: true, message: '请输入图形验证码', trigger: 'blur' },
    { len: 4, message: '验证码为 4 位', trigger: 'blur' }
  ],
  emailCode: [
    { required: true, message: '请输入邮箱验证码', trigger: 'blur' },
    { len: 6, message: '验证码为 6 位', trigger: 'blur' }
  ],
  referrerEmail: [
    // 仅当 role === 'admin' 时必填
    { required: true, message: '请输入推荐管理员邮箱', trigger: 'blur' },
    { type: 'email', message: '请输入正确的邮箱地址', trigger: 'blur' }
  ]
}
```

### 5.3 用户交互处理

#### 5.3.1 角色切换

| 操作 | 处理逻辑 |
|------|----------|
| 切换角色 | 1. 更新 `formData.role`；2. 若切换到"管理员"，显示推荐管理员邮箱字段；3. 若切换离开"管理员"，隐藏并清空该字段；4. 清除已有校验错误 |

#### 5.3.2 图形验证码

| 操作 | 处理逻辑 |
|------|----------|
| 点击验证码图片 | 1. 检查冷却期（4s 内不可重复刷新）；2. 调用 `GET /api/v1/auth/captcha`；3. 更新 `captchaImage` 和 `captchaId`；4. 清空 `formData.captcha` 输入 |
| 验证码加载失败 | 展示"点击刷新"占位文字 |

#### 5.3.3 邮箱验证码

| 操作 | 处理逻辑 |
|------|----------|
| 点击"发送验证码" | 1. 校验邮箱字段格式；2. 若未填写 → 提示"请先输入邮箱"；3. `isSendingCode=true`；4. 调用 `POST /api/v1/auth/send-email-code { email, purpose: 'register' }`；5. 成功 → 开始 60s 倒计时；6. 失败 → 恢复按钮状态并展示错误信息 |
| 倒计时中 | 按钮禁用，文案显示"重新发送 (Ns)" |
| 倒计时结束 | 按钮恢复，文案显示"重新发送" |

#### 5.3.4 提交注册

```
handleSubmit()
 ├─ 1. el-form.validate() → 全量校验
 │    └─ 校验失败 → 滚动到第一个错误字段
 ├─ 2. 组装请求参数 RegisterRequest
 │    └─ 若 role !== 'admin'，不发送 referrer_email
 ├─ 3. isSubmitting = true
 ├─ 4. POST /api/v1/auth/register
 ├─ 5. 响应处理：
 │    ├─ 成功 (code=200)
 │    │   ├─ 存储 Token 和用户信息到 userStore
 │    │   ├─ 若 role='student' → ElMessage.success('注册成功') → router.push('/')
 │    │   ├─ 若 role='teacher' → showTeacherPendingDialog = true
 │    │   └─ 若 role='admin' → ElMessage.success('注册成功，等待审核') → router.push('/')
 │    └─ 失败
 │        ├─ code=409 → 根据 message 提示（用户名/手机号/邮箱已存在）
 │        ├─ code=422 → 展示具体校验错误
 │        └─ 其他 → ElMessage.error(message)
 └─ 6. isSubmitting = false
      └─ 若失败 → 自动刷新图形验证码
```

#### 5.3.5 讲师审核中弹窗

| 操作 | 处理逻辑 |
|------|----------|
| 点击"我知道了" | 关闭弹窗 → `router.push('/')` |
| 弹窗展示文案 | "您的讲师申请已提交，审核通过后将通过站内消息通知您。当前您可以作为学员浏览和学习课程。" |

### 5.4 异常场景处理

| 异常场景 | 处理策略 |
|----------|----------|
| **用户名已存在 (409)** | 字段下方红色提示"该用户名已被使用"，并聚焦到用户名输入框 |
| **手机号已存在 (409)** | 字段下方提示"该手机号已被使用" |
| **邮箱已存在 (409)** | 字段下方提示"该邮箱已被使用" |
| **图形验证码错误 (422)** | 提示"验证码错误"，自动刷新图形验证码 |
| **邮箱验证码错误/过期 (422)** | 提示"验证码错误或已过期，请重新获取" |
| **推荐管理员邮箱无效 (404/422)** | 提示"推荐管理员账号不存在，请确认后重新输入" |
| **邮箱发送频率限制 (429)** | 提示"发送过于频繁，请 60 秒后重试" |
| **网络断开** | ElMessage.error("网络连接失败，请稍后重试") |
| **服务端 500** | ElMessage.error("服务暂时不可用，请稍后重试") |
| **Token 已存在（已登录用户访问）** | 路由守卫拦截 → `router.replace('/')` |

---

## 6. API 接口清单

| 序号 | 接口用途 | HTTP 方法 | 路径 | 调用函数名 | 入参 | 调用时机 |
|------|----------|-----------|------|------------|------|----------|
| 1 | 用户注册 | POST | `/api/v1/auth/register` | `register(data: RegisterRequest)` | `RegisterRequest` 全字段 | 点击注册按钮 |
| 2 | 获取图形验证码 | GET | `/api/v1/auth/captcha` | `getCaptcha()` | 无 | onMounted / 点击刷新 / 注册失败 |
| 3 | 发送邮箱验证码 | POST | `/api/v1/auth/send-email-code` | `sendEmailCode(data: SendEmailCodeRequest)` | `{ email, purpose: 'register' }` | 点击发送验证码 |

---

## 7. 补充设计要点

### 7.1 密码强度指示器（可选增强）

```typescript
// 密码强度评级逻辑
function getPasswordStrength(password: string): 'weak' | 'medium' | 'strong' {
  let score = 0
  if (password.length >= 8) score++
  if (password.length >= 12) score++
  if (/[A-Z]/.test(password)) score++
  if (/[0-9]/.test(password)) score++
  if (/[^a-zA-Z0-9]/.test(password)) score++
  if (score <= 2) return 'weak'
  if (score <= 3) return 'medium'
  return 'strong'
}
```

展示为密码输入框下方的三段颜色条：红（弱）→ 橙（中）→ 绿（强）。

### 7.2 邮箱验证码倒计时

```typescript
let countdownTimer: ReturnType<typeof setInterval> | null = null

function startCountdown(): void {
  emailCodeCountdown.value = 60
  countdownTimer = setInterval(() => {
    emailCodeCountdown.value--
    if (emailCodeCountdown.value <= 0) {
      clearInterval(countdownTimer!)
      countdownTimer = null
    }
  }, 1000)
}

// 组件卸载时清除定时器
onUnmounted(() => {
  if (countdownTimer) clearInterval(countdownTimer)
})
```

### 7.3 响应式布局

| 屏幕宽度 | 布局方案 |
|----------|----------|
| ≥1280px | 左右分栏：左侧品牌区（40%）+ 右侧表单区（60%） |
| <1280px | 全屏表单，品牌区隐藏，背景使用渐变色 |
