# 前端页面详细设计说明书 - 6.9 讲师端 - 课程管理

> 文档版本：v1.0 | 生成时间：2026-03-24 | 技术栈：Vue 3 + TypeScript + Element Plus + Pinia
> 本文档覆盖 6.9.1-6.9.6：课程列表、创建/编辑课程、目录管理、资源上传、发布/下架/删除

---

## 1. 页面概述（总览）

| 属性 | 说明 |
|------|------|
| 模块名称 | 讲师端 - 课程管理 |
| 布局组件 | `src/views/teacher/TeacherLayout.vue`（顶栏 + 侧边菜单 + 内容区） |
| 访问权限 | 讲师（status=active）/ 管理员；待审核讲师禁止访问并提示 |

| 子页面 | 路由地址 | 路由名称 | 文件路径 |
|--------|----------|----------|----------|
| 课程管理列表 | `/teacher/courses` | `TeacherCourses` | `src/views/teacher/CourseListPage.vue` |
| 创建课程 | `/teacher/courses/create` | `TeacherCourseCreate` | `src/views/teacher/CourseFormPage.vue` |
| 编辑课程 | `/teacher/courses/:courseId/edit` | `TeacherCourseEdit` | `src/views/teacher/CourseFormPage.vue`（复用） |

---

## 2. 数据模型 (TypeScript)

```typescript
/** 讲师课程列表项 */
interface TeacherCourseItem {
  course_id: number
  title: string
  cover_url: string
  status: 'draft' | 'published' | 'archived'
  view_count: number
  created_at: string
  published_at: string | null
}

/** 课程表单数据 */
interface CourseFormData {
  title: string                 // 2-100 字符
  cover_url: string             // 封面 URL
  summary: string               // 10-500 字符
  description: string           // 富文本 HTML
  category_id: number | null
  tags: string[]                // 最多 5 个
}

/** 创建课程请求 */
interface CreateCourseRequest {
  title: string
  cover_url: string
  summary: string
  description?: string
  category_id: number
  tags?: string[]
}

/** 章节数据 */
interface ChapterFormData {
  chapter_id?: number
  title: string                 // 2-50 字符
  sort_order: number
}

/** 小节数据 */
interface SectionFormData {
  section_id?: number
  title: string                 // 2-50 字符
  sort_order: number
}

/** 资源上传项 */
interface ResourceUploadItem {
  resource_id?: number
  resource_type: 'video' | 'audio' | 'document' | 'image'
  file_name: string
  file_url: string
  file_size: number
  duration?: number
  resolution?: string
  thumbnail_url?: string
  upload_status: 'uploading' | 'success' | 'error'
  upload_progress: number       // 0-100
}

/** 下架请求 */
interface ArchiveCourseRequest {
  archive_reason: string        // 10-200 字符
}

/** 配套资料项 */
interface MaterialItem {
  material_id: number
  file_name: string
  file_url: string
  file_size: number
}

/** 大文件分片上传初始化请求 */
interface ChunkUploadInitRequest {
  file_name: string
  file_size: number
  chunk_size: number
}

/** 分片上传状态 */
interface ChunkUploadState {
  uploadId: string
  totalChunks: number
  uploadedChunks: number
  progress: number              // 0-100
  status: 'uploading' | 'paused' | 'completed' | 'error'
}
```

---

## 3. 子页面详细设计

---

### 3.1 课程管理列表页 (`/teacher/courses`)

#### UI 组件结构

```
CourseListPage.vue
├── PageHeader
│   ├── "我的课程"
│   └── el-button[type=primary, icon=Plus] → "创建课程"
├── FilterBar
│   ├── StatusFilter → el-radio-group
│   │   ├── el-radio-button[全部]   value="all"
│   │   ├── el-radio-button[草稿]   value="draft"
│   │   ├── el-radio-button[已发布] value="published"
│   │   └── el-radio-button[已下架] value="archived"
│   └── SearchInput → el-input + el-button
├── [加载中] el-skeleton × 3
├── [空状态] el-empty → "暂无课程，请先创建课程"
├── [正常] CourseTable
│   └── el-table[:data]
│       ├── el-table-column[封面] → el-image (60×40)
│       ├── el-table-column[课程名称]
│       ├── el-table-column[状态] → StatusTag
│       │   ├── draft → el-tag[type=info] "草稿"
│       │   ├── published → el-tag[type=success] "已发布"
│       │   └── archived → el-tag[type=danger] "已下架"
│       ├── el-table-column[浏览量]
│       ├── el-table-column[创建时间]
│       ├── el-table-column[发布时间]
│       └── el-table-column[操作]
│           ├── el-button[编辑] → 跳转编辑页
│           ├── [draft] el-button[发布] → 发布确认
│           ├── [published] el-button[下架] → 下架弹窗
│           ├── [draft|archived] el-button[删除, type=danger] → 二次确认
│           └── [published] el-button[删除, disabled] → tooltip "已发布课程需先下架"
└── el-pagination
```

#### 核心交互

| 操作 | 处理逻辑 |
|------|----------|
| 创建课程 | `router.push('/teacher/courses/create')` |
| 编辑课程 | `router.push(\`/teacher/courses/${courseId}/edit\`)` |
| 发布课程 | ElMessageBox.confirm → `POST /api/v1/courses/{id}/publish` → 刷新列表 |
| 下架课程 | 弹窗填写下架原因(10-200字) → `POST /api/v1/courses/{id}/archive` → 刷新列表 |
| 删除课程 | ElMessageBox.confirm(状态相关文案) → `POST /api/v1/courses/{id}` → 刷新列表 |

#### API 清单

| 接口 | 方法 | 路径 | 函数名 |
|------|------|------|--------|
| 我的课程列表 | GET | `/api/v1/courses/my-courses` | `fetchMyCourses(params)` |
| 发布课程 | POST | `/api/v1/courses/{id}/publish` | `publishCourse(id)` |
| 下架课程 | POST | `/api/v1/courses/{id}/archive` | `archiveCourse(id, data)` |
| 删除课程 | POST | `/api/v1/courses/{id}` | `deleteCourse(id)` |

---

### 3.2 创建 / 编辑课程页 (`/teacher/courses/create` | `/teacher/courses/:courseId/edit`)

#### UI 组件结构

```
CourseFormPage.vue
├── PageHeader → "创建课程" / "编辑课程"
├── el-form (label-position="top")
│   ├── el-form-item[课程标题]
│   │   └── el-input[:maxlength="100", show-word-limit]
│   ├── el-form-item[课程封面]
│   │   └── CoverUploader.vue
│   │       ├── el-upload[drag]
│   │       ├── 预览区 → el-image
│   │       └── 提示："建议 16:9 比例，JPG/PNG，≤10MB"
│   ├── el-form-item[课程分类]
│   │   └── el-cascader (分类树) / el-select
│   ├── el-form-item[课程标签]
│   │   └── TagInput.vue
│   │       ├── el-tag[v-for, closable] × N
│   │       └── el-input (新增标签，最多 5 个)
│   ├── el-form-item[课程简介]
│   │   └── el-input[type=textarea, :maxlength="500", show-word-limit]
│   ├── el-form-item[课程描述]
│   │   └── RichTextEditor.vue (TinyMCE / Quill / WangEditor)
│   ├── el-form-item[配套资料]
│   │   └── MaterialUploader.vue
│   │       ├── el-upload[:limit="10", accept=".pdf,.doc,.docx,.zip"]
│   │       └── 已上传列表 (文件名 + 大小 + 删除)
│   │
│   ├── el-divider → "课程内容管理"
│   ├── ChapterManager.vue                 ← 章节目录管理（内嵌）
│   │   └── [展开见 3.3]
│   │
│   └── ActionBar (固定底部)
│       ├── el-button → "保存草稿"
│       ├── el-button[type=primary] → "保存并发布"（校验完整性）
│       └── el-button → "返回列表"
```

#### 核心交互

| 操作 | 处理逻辑 |
|------|----------|
| 创建模式 onMounted | 加载分类列表、标签列表 |
| 编辑模式 onMounted | 加载课程详情 → 填充表单 → 加载分类/标签 |
| 保存草稿 | 表单校验(仅标题必填) → `POST /api/v1/courses` (创建) 或 `POST /api/v1/courses/{id}` (更新) |
| 保存并发布 | 完整校验 → 保存 → `POST /api/v1/courses/{id}/publish` |
| 封面上传 | `POST /api/v1/upload/file` → 获取 URL → 填入 cover_url |
| 资料上传 | `POST /api/v1/courses/{id}/materials` → 添加到资料列表 |
| 资料删除 | `POST /api/v1/courses/{id}/materials/{materialId}` |

#### API 清单

| 接口 | 方法 | 路径 | 函数名 |
|------|------|------|--------|
| 创建课程 | POST | `/api/v1/courses` | `createCourse(data)` |
| 更新课程 | POST | `/api/v1/courses/{id}` | `updateCourse(id, data)` |
| 课程详情 | GET | `/api/v1/courses/{id}` | `fetchCourseDetail(id)` |
| 分类列表 | GET | `/api/v1/categories` | `fetchCategories()` |
| 标签列表 | GET | `/api/v1/tags` | `fetchTags()` |
| 创建标签 | POST | `/api/v1/tags` | `createTag(data)` |
| 上传文件 | POST | `/api/v1/upload/file` | `uploadFile(file)` |
| 上传资料 | POST | `/api/v1/courses/{id}/materials` | `uploadMaterial(id, file)` |
| 删除资料 | POST | `/api/v1/courses/{id}/materials/{mid}` | `deleteMaterial(id, mid)` |

---

### 3.3 章节目录管理（内嵌组件）

#### UI 组件结构

```
ChapterManager.vue
├── AddChapterBtn → el-button[icon=Plus] → "添加章节"
├── ChapterList (可拖拽排序 - vuedraggable / SortableJS)
│   └── ChapterBlock × N
│       ├── ChapterHeader (可拖拽手柄)
│       │   ├── DragHandle → el-icon[Rank]
│       │   ├── ChapterTitle → el-input (内联编辑)
│       │   ├── AddSectionBtn → el-button[icon=Plus, text] → "添加小节"
│       │   └── DeleteChapterBtn → el-button[icon=Delete, type=danger, text]
│       └── SectionList (可拖拽排序)
│           └── SectionBlock × N
│               ├── DragHandle → el-icon[Rank]
│               ├── SectionTitle → el-input (内联编辑)
│               ├── ResourceManager → 资源上传/管理入口
│               │   └── [展开见 3.4]
│               └── DeleteSectionBtn → el-button[icon=Delete, type=danger, text]
```

#### 核心交互

| 操作 | 处理逻辑 |
|------|----------|
| 添加章节 | 弹窗输入标题(2-50字) → `POST /api/v1/courses/{id}/chapters` → 追加到列表 |
| 添加小节 | 弹窗输入标题(2-50字) → `POST /api/v1/courses/{id}/chapters/{cid}/sections` |
| 修改章节名 | 内联编辑 blur → `POST /api/v1/courses/{id}/chapters/{cid}` |
| 修改小节名 | 内联编辑 blur → `POST /api/v1/courses/{id}/chapters/{cid}/sections/{sid}` |
| 拖拽排序 | 更新 sort_order → 批量 POST 更新排序 |
| 删除章节 | ElMessageBox.confirm("删除后下属内容将一并删除，是否确认？") → `POST /api/v1/courses/{id}/chapters/{cid}` |
| 删除小节 | ElMessageBox.confirm → `POST /api/v1/courses/{id}/chapters/{cid}/sections/{sid}` |

---

### 3.4 资源上传管理（内嵌组件）

#### UI 组件结构

```
ResourceManager.vue
├── UploadArea
│   └── el-upload[drag, multiple]
│       ├── accept=".mp4,.mp3,.pdf,.doc,.docx,.ppt,.pptx,.jpg,.jpeg,.png"
│       └── 提示文案："拖拽或点击上传学习资源"
├── ResourceList
│   └── ResourceItem × N
│       ├── TypeIcon → 视频/音频/文档/图片图标
│       ├── FileName → 文件名
│       ├── FileSize → 格式化大小
│       ├── [视频] Duration + Resolution
│       ├── UploadProgress → el-progress (上传中)
│       ├── StatusTag
│       │   ├── uploading → el-tag[type=warning] "上传中"
│       │   ├── success → el-tag[type=success] "已上传"
│       │   └── error → el-tag[type=danger] "上传失败" + 重试
│       ├── PreviewBtn → el-button[预览]
│       ├── ReplaceBtn → el-button[替换]
│       └── DeleteBtn → el-button[删除, type=danger]
```

#### 大文件分片上传流程

```
handleFileUpload(file: File)
 ├─ 1. 前端校验格式和大小
 ├─ 2. 判断文件大小
 │    ├─ ≤100MB → 直接上传 POST /api/v1/upload/file
 │    └─ >100MB → 分片上传
 │        ├─ a. POST /api/v1/upload/init → 获取 upload_id
 │        ├─ b. 切分 File 为 10MB 分片
 │        ├─ c. 逐片 POST /api/v1/upload/chunk (并发 3 片)
 │        │    └─ 每片完成更新 upload_progress
 │        └─ d. 全部完成 → POST /api/v1/upload/complete
 ├─ 3. 上传完成 → POST /api/v1/courses/{id}/sections/{sid}/resources
 └─ 4. 失败 → 展示错误 + 重试按钮（断点续传）
```

---

## 4. 发布 / 下架 / 删除规则汇总

### 4.1 发布前检查

```typescript
interface PublishCheckResult {
  canPublish: boolean
  missingItems: string[]
}

function checkPublishReady(course: CourseDetail): PublishCheckResult {
  const missing: string[] = []
  if (!course.title) missing.push('课程标题')
  if (!course.cover_url) missing.push('课程封面')
  if (!course.summary) missing.push('课程简介')
  if (!course.chapters?.length) missing.push('至少 1 个章节')
  const hasSections = course.chapters?.some(c => c.sections?.length > 0)
  if (!hasSections) missing.push('至少 1 个小节')
  const hasResources = course.chapters?.some(c =>
    c.sections?.some(s => s.resources?.length > 0)
  )
  if (!hasResources) missing.push('至少 1 个学习资源')

  return { canPublish: missing.length === 0, missingItems: missing }
}
```

### 4.2 状态操作矩阵

| 当前状态 | 可执行操作 | 不可执行操作 |
|----------|-----------|-------------|
| draft | 编辑、发布、删除 | 下架 |
| published | 编辑、下架 | 发布、删除 |
| archived | 编辑、删除 | 发布、下架 |

---

## 5. 异常场景

| 异常场景 | 处理策略 |
|----------|----------|
| 待审核讲师访问 | 路由守卫拦截 → ElMessage.warning('讲师审核通过后可使用该功能') → 跳转首页 |
| 课程内容不完整时发布 | 弹窗展示缺失项清单，阻止发布 |
| 封面上传失败 | ElMessage.error('封面上传失败，请重试') |
| 大文件上传中断 | 保留 upload_id → 展示"继续上传"按钮 → 从断点续传 |
| 存储空间不足 (507) | ElMessage.error('存储空间不足，请联系管理员') |
| 权限不足 (403) | ElMessage.error('无权限操作该课程') |
| 网络异常 | 表单数据保留在内存中不丢失，提示重试 |
