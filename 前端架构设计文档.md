# 在线教育学习视频播放项目 - 前端架构设计文档

## 1. 前端技术栈选型方案

针对本项目的界面复杂度（包含复杂的多媒体文件渲染、多级树形菜单渲染及较高的交互性需求），结合团队背景和业界主流实践，制定如下前端技术栈选型：

- **核心框架**：**Vue 3** (Composition API)
  - *理由*：Vue 3 学习曲线平滑，与后端（特别是Java生态）对接友好；其提供的高效响应式系统非常适合处理播放页右侧视频与左侧目录进度的联动状态。
- **开发语言**：**TypeScript (TS)**
  - *理由*：在视频播放流、PPT页码流流转中增强数据结构的健壮性，提供更好的IDE智能提示，减少低级错误。
- **构建工具**：**Vite**
  - *理由*：提供极速的冷启动和热更新(HMR)，极大提升开发体验。
- **UI 组件库**：**Element Plus** 
  - *理由*：成熟的PC端UI库，直接提供完善的Card（课程卡片）、Tabs（课程简介/目录切换）、Tree（多级任务与目录折叠）组件。
- **路由与状态管理**：**Vue Router 4** + **Pinia**
- **网络请求**：**Axios**
- **第三方核心多媒体依赖**：
  - **视频播放**：`video.js` 或基于Vue封装的组件 (如 `vue3-video-play`)
  - **PDF/PPT渲染**：`pdf.js` (渲染PDF)、或者服务端将PPT转为PDF或图片集合后供前端按页渲染。

## 2. 项目目录结构规划

推荐采用按业务特性加功能模块的方式进行目录划分（Feature-Based Layout），结构如下：

```text
src/
├── api/            # 集中管理 Axios 请求和 API 接口定义，如 `course.ts`, `task.ts`
├── assets/         # 静态资源，如缺省图（空视图公文包图片）、全局样式变量
├── components/     # 全局复用组件
│   ├── layout/     # 页面布局组件（Header导航栏, Sidebar侧边栏等）
│   ├── common/     # 基础共用组件（PDF阅读器, 视频播放器外壳, 课程卡片等）
├── router/         # 路由配置，包含路由守卫等
├── store/          # 基于 Pinia 的状态管理模块，如 `useCourseStore.ts`
├── utils/          # 工具函数，如时间格式化、本地缓存封装、Axios拦截器
├── views/          # 业务视图页面
│   ├── Home/       # 首页 (A课程页面)
│   ├── Course/     # 课程详情页 (A2, A3, A4页面)
│   ├── Learn/      # 沉浸式学习播放页 (A5, A6, A7, A8页面)
├── App.vue         # 根组件
└── main.ts         # 项目入口，实例挂载与插件注册
```

## 3. 组件设计规范与状态管理方案

### 3.1 核心组件划分与设计
- **视频与文档查看器组件 (`<MediaPlayer />`, `<DocViewer />`)**：
  - 设计为“受控组件”，仅接收 `src` (资源地址) 和 `type` (视频/PDF) 传入，不直接发起网络请求获取资源，保证其高复用性。
  - 对外抛出事件（Events）：`@progress` (播放进度)、`@page-change` (翻页进度)、`@end` (完成学习)。
- **自适应任务树组件 (`<CourseTreeSidebar />`)**：
  - 根据 A5~A8 设计左侧暗色调菜单。通过 Element Plus 的 `el-menu` 深度定制样式，支持Hover时触发 Popover 浮层。

### 3.2 状态管理方案 (Pinia)
以“学习播放页”为例，该页面状态最复杂。我们需要建立一个独立的 Store (`useLearnStore`)：
- **State**:
  - `currentCourseId`: 当前正在学习的课程ID。
  - `activeTaskId`: 当前选中的某个子任务。
  - `taskTreeData`: 整个课程的目录树数据。
  - `progressMap`: 记录各个子任务当前完成的百分比（例如 A7 截图中的 “0%”）。
- **Actions**:
  - `fetchCourseDocs()`: 异步加载目录数据。
  - `updateProgress(taskId, progress)`: 监听右侧组件上报的进度，并更新到 State 中，使左侧边栏产生响应式视图更新。

## 4. API 接口交互策略与数据处理流程

### 4.1 数据交互流程
1. **统一拦截**：使用 Axios 配置请求拦截器（Request Interceptor），统一在头部注入 JWT Token 进行鉴权。
2. **响应拦截与错误处理**：响应拦截器（Response Interceptor）统一处理 HTTP 状态码和业务错误码。针对 Token 过期、网络超时等进行全局提示（如 Element Plus Message）。
3. **播放进度防抖上报**：对于视频播放进度或文档阅读停留时间的上报，不能过于频繁。采用防抖 (Debounce) 或节流 (Throttle) 策略，每 5-10 秒或者在组件销毁前 (`beforeUnmount`) 向服务端发送一次最后进度。

### 4.2 核心 API 设计示例
- `GET /api/v1/courses` 获取首页课程列表。
- `GET /api/v1/courses/{id}` 获取某个课程详情及大纲。
- `GET /api/v1/tasks/{taskId}/materials` 获取鼠标 Hover (A6/A7) 时需要展示的具体视频与图文资源清单。
- `POST /api/v1/study/progress` 提交某资源的学习进度。

## 5. 前端构建流程与部署方案

### 5.1 构建流程设置 (Vite 优化)
- **代码分割 (Code Splitting)**：对第三方库（如 `pdf.js`、`video.js`）以及各级路由视图，做异步打包（Async Route Components）。用户仅在进入“沉浸式学习页”时才加载播放器相关依赖，减少首页首屏时间。
- **静态资源压缩**：对项目中的小图标和背景图片启用 gzip 压缩和 WebP 转化插件。
- **环境隔离**：通过 `.env.development`, `.env.production` 隔离开发与生产环境的请求前缀 (API Base URL)。

### 5.2 部署方案
- **Nginx 静态托管**：
  - 产物（`dist` 目录）上传至前端服务器（或 CDN）。
  - 配置 Nginx 将所有前端路由请求（History模式下）`try_files` 重定向至 `index.html`，防止路由刷新 404。
  - Nginx 配置接口反向代理 (Reverse Proxy) `/api` 至 python后端，解决跨域问题。
