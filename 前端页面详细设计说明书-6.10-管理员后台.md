# 前端页面详细设计说明书 - 6.10 管理员后台

> 文档版本：v1.0 | 生成时间：2026-03-24 | 技术栈：Vue 3 + TypeScript + Element Plus + Pinia
> 本文档覆盖 6.10.1-6.10.4：用户管理、角色权限管理、公告管理、反馈管理

---

## 1. 页面概述（总览）

| 属性 | 说明 |
|------|------|
| 模块名称 | 管理员后台 |
| 布局组件 | `src/views/admin/AdminLayout.vue`（侧边菜单 + 内容区） |
| 访问权限 | 仅管理员（role=admin）；其他角色访问 → "无权限访问" → 跳转首页 |

| 子页面 | 路由地址 | 路由名称 | 文件路径 |
|--------|----------|----------|----------|
| 用户管理 | `/admin/users` | `AdminUsers` | `src/views/admin/UserManagePage.vue` |
| 角色权限管理 | `/admin/roles` | `AdminRoles` | `src/views/admin/RolePermissionPage.vue` |
| 公告管理 | `/admin/announcements` | `AdminAnnouncements` | `src/views/admin/AnnouncementPage.vue` |
| 反馈管理 | `/admin/feedbacks` | `AdminFeedbacks` | `src/views/admin/FeedbackManagePage.vue` |

---

## 2. 布局组件结构

```
AdminLayout.vue
├── AdminSidebar.vue                        ← 左侧菜单 (width: 220px)
│   ├── LogoArea → 平台名 + "管理后台"
│   └── el-menu (router, :default-active)
│       ├── el-menu-item[用户管理, icon=User]       → /admin/users
│       ├── el-menu-item[角色权限, icon=Key]        → /admin/roles
│       ├── el-menu-item[公告管理, icon=Bell]       → /admin/announcements
│       ├── el-menu-item[反馈管理, icon=ChatDotRound] → /admin/feedbacks
│       └── el-menu-item[返回前台, icon=Back]       → /
└── el-main → <router-view />
```

---

## 3. 数据模型 (TypeScript)

```typescript
/** 管理端用户列表项 */
interface AdminUserItem {
  user_id: number
  username: string
  email: string
  phone: string
  nickname: string
  role: 'student' | 'teacher' | 'admin'
  status: 'active' | 'disabled' | 'pending'
  created_at: string
  last_login_at: string
}

/** 用户列表请求参数 */
interface AdminUsersParams {
  role?: 'all' | 'student' | 'teacher' | 'admin' | 'pending'
  status?: 'all' | 'active' | 'disabled' | 'pending'
  keyword?: string
  page: number
  page_size: number
}

/** 讲师审核列表项 */
interface TeacherAuditItem {
  audit_id: number
  user_id: number
  username: string
  email: string
  audit_status: 'pending' | 'approved' | 'rejected'
  created_at: string
}

/** 审核请求 */
interface ReviewTeacherRequest {
  audit_status: 'approved' | 'rejected'
  reject_reason?: string
}

/** 公告项 */
interface AnnouncementItem {
  announcement_id: number
  title: string
  content: string
  status: 'draft' | 'published'
  published_at: string | null
  creator_name: string
}

/** 公告表单数据 */
interface AnnouncementFormData {
  title: string
  content: string
  status: 'draft' | 'published'
}

/** 反馈列表项（管理端） */
interface AdminFeedbackItem {
  feedback_id: number
  user_id: number
  username: string
  feedback_type: 'system' | 'course'
  course_id: number | null
  course_title: string | null
  content: string
  images: string[]
  status: 'pending' | 'processed'
  created_at: string
}

/** 反馈列表请求参数 */
interface AdminFeedbacksParams {
  feedback_type?: 'all' | 'system' | 'course'
  status?: 'all' | 'pending' | 'processed'
  keyword?: string
  page: number
  page_size: number
}
```

---

## 4. 子页面详细设计

---

### 4.1 用户管理页 (`/admin/users`)

#### UI 组件结构

```
UserManagePage.vue
├── PageHeader → "用户管理"
├── FilterBar
│   ├── RoleFilter → el-select
│   │   └── 全部/学员/讲师/管理员/待审核讲师
│   ├── StatusFilter → el-select
│   │   └── 全部/正常/禁用/待审核
│   └── KeywordSearch → el-input + el-button
│       └── placeholder="搜索邮箱/手机号"
├── el-table[:data, border, stripe]
│   ├── el-table-column[用户名]
│   ├── el-table-column[邮箱]
│   ├── el-table-column[手机号]
│   ├── el-table-column[昵称]
│   ├── el-table-column[角色] → el-tag (颜色区分)
│   ├── el-table-column[状态] → StatusTag
│   │   ├── active → el-tag[type=success] "正常"
│   │   ├── disabled → el-tag[type=danger] "禁用"
│   │   └── pending → el-tag[type=warning] "待审核"
│   ├── el-table-column[注册时间]
│   ├── el-table-column[最近登录]
│   └── el-table-column[操作, width=220]
│       ├── [active] el-button[禁用] → 二次确认
│       ├── [disabled] el-button[启用] → 二次确认
│       ├── [pending] el-button[审核] → 打开审核弹窗
│       ├── el-button[详情] → 查看详情抽屉
│       └── el-button[删除, type=danger] → 二次确认
├── el-pagination
│
├── UserDetailDrawer.vue                    ← 用户详情抽屉
│   └── el-drawer → 用户完整信息展示
│
└── TeacherAuditDialog.vue                  ← 讲师审核弹窗
    └── el-dialog
        ├── 用户基本信息展示
        ├── el-radio-group[审核结果]
        │   ├── el-radio[通过]
        │   └── el-radio[驳回]
        ├── [驳回] el-input[type=textarea] → 驳回原因
        └── el-button[确认] → POST 审核接口
```

#### 核心交互

| 操作 | 处理逻辑 |
|------|----------|
| 禁用用户 | ElMessageBox.confirm → `POST /api/v1/users/{id}/status { status: 'disabled' }` |
| 启用用户 | ElMessageBox.confirm → `POST /api/v1/users/{id}/status { status: 'active' }` |
| 审核讲师 | 弹窗选择通过/驳回 → `POST /api/v1/users/teacher-audits/{auditId}/review` |
| 删除用户 | ElMessageBox.confirm("删除用户将级联删除学习记录和反馈，是否确认？") → `POST /api/v1/users/{id}` |
| 筛选/搜索 | 重置 page=1 → 重新请求 |

#### API 清单

| 接口 | 方法 | 路径 | 函数名 |
|------|------|------|--------|
| 用户列表 | GET | `/api/v1/users` | `fetchUsers(params)` |
| 禁用/启用 | POST | `/api/v1/users/{id}/status` | `toggleUserStatus(id, status)` |
| 删除用户 | POST | `/api/v1/users/{id}` | `deleteUser(id)` |
| 讲师审核列表 | GET | `/api/v1/users/teacher-audits` | `fetchTeacherAudits(params)` |
| 审核讲师 | POST | `/api/v1/users/teacher-audits/{id}/review` | `reviewTeacher(id, data)` |
| 管理员申请列表 | GET | `/api/v1/users/admin-applications` | `fetchAdminApplications(params)` |
| 审核管理员 | POST | `/api/v1/users/admin-applications/{id}/review` | `reviewAdminApp(id, data)` |

---

### 4.2 角色权限管理页 (`/admin/roles`)

#### UI 组件结构

```
RolePermissionPage.vue
├── PageHeader → "角色权限管理"
├── RoleSelector
│   └── el-tabs (v-model="selectedRole")
│       ├── el-tab-pane[学员]
│       ├── el-tab-pane[讲师]
│       └── el-tab-pane[管理员]
├── PermissionTree
│   └── el-tree
│       ├── :data="permissionTree"
│       ├── show-checkbox
│       ├── node-key="permission_id"
│       └── :default-checked-keys="currentRolePermissions"
└── ActionBar
    └── el-button[type=primary, :loading] → "保存配置"
```

#### 核心交互

| 操作 | 处理逻辑 |
|------|----------|
| 切换角色 Tab | 加载该角色当前权限 → 设置 Tree checked 状态 |
| 勾选/取消权限 | 更新本地选中列表 |
| 保存配置 | 提交选中的权限 ID 列表 → 成功提示 |

> **接口备注**：接口文档未明确权限管理 CRUD 接口，需后端补充。前端预设接口：
> - `GET /api/v1/roles/{role}/permissions` → 获取角色权限
> - `POST /api/v1/roles/{role}/permissions` → 更新角色权限

---

### 4.3 公告管理页 (`/admin/announcements`)

#### UI 组件结构

```
AnnouncementPage.vue
├── PageHeader
│   ├── "公告管理"
│   └── el-button[type=primary, icon=Plus] → "新增公告"
├── el-table[:data, border]
│   ├── el-table-column[标题]
│   ├── el-table-column[状态] → el-tag (draft/published)
│   ├── el-table-column[发布时间]
│   ├── el-table-column[创建人]
│   └── el-table-column[操作]
│       ├── el-button[编辑]
│       ├── [draft] el-button[发布]
│       ├── [published] el-button[转为草稿]
│       └── el-button[删除, type=danger]
├── el-pagination
│
└── AnnouncementFormDialog.vue              ← 新增/编辑弹窗
    └── el-dialog
        └── el-form
            ├── el-form-item[标题] → el-input
            ├── el-form-item[内容] → el-input[type=textarea] 或 RichTextEditor
            └── el-form-item[状态] → el-radio-group[草稿/发布]
```

#### 核心交互

| 操作 | 处理逻辑 |
|------|----------|
| 新增公告 | 打开弹窗 → 填写 → 提交创建 |
| 编辑公告 | 打开弹窗(回填数据) → 修改 → 提交更新 |
| 发布公告 | 更新状态为 published |
| 删除公告 | ElMessageBox.confirm → 删除 |

#### API 清单

| 接口 | 方法 | 路径 | 函数名 |
|------|------|------|--------|
| 公告列表 | GET | `/api/v1/announcements` | `fetchAnnouncements(params)` |
| 创建公告 | POST | `/api/v1/announcements` | `createAnnouncement(data)` |
| 更新公告 | POST | `/api/v1/announcements/{id}` | `updateAnnouncement(id, data)` |
| 删除公告 | POST | `/api/v1/announcements/{id}` | `deleteAnnouncement(id)` |

> **接口备注**：接口文档仅提供公告列表查询 `GET /api/v1/announcements`，CRUD 操作需后端补充。

---

### 4.4 反馈管理页 (`/admin/feedbacks`)

#### UI 组件结构

```
FeedbackManagePage.vue
├── PageHeader → "反馈管理"
├── FilterBar
│   ├── TypeFilter → el-select[全部/系统问题/课程问题]
│   ├── StatusFilter → el-select[全部/待处理/已处理]
│   └── KeywordSearch → el-input → "搜索反馈内容/用户名"
├── el-table[:data, border]
│   ├── el-table-column[用户名]
│   ├── el-table-column[反馈类型] → el-tag
│   ├── el-table-column[关联课程]
│   ├── el-table-column[反馈内容, show-overflow-tooltip]
│   ├── el-table-column[截图] → 缩略图(可预览)
│   ├── el-table-column[状态] → el-tag
│   │   ├── pending → el-tag[type=warning] "待处理"
│   │   └── processed → el-tag[type=success] "已处理"
│   ├── el-table-column[提交时间]
│   └── el-table-column[操作]
│       ├── el-button[查看详情]
│       ├── [pending] el-button[标记已处理]
│       └── el-button[批量处理] (表格多选模式)
├── el-pagination
│
└── FeedbackDetailDrawer.vue                ← 反馈详情抽屉
    └── el-drawer
        ├── 用户信息
        ├── 反馈类型 + 关联课程
        ├── 反馈完整内容
        ├── 截图大图预览 (el-image[:preview-src-list])
        ├── 处理状态
        └── [pending] el-button[标记为已处理]
```

#### 核心交互

| 操作 | 处理逻辑 |
|------|----------|
| 查看详情 | `GET /api/v1/feedbacks/{id}` → 打开 Drawer |
| 标记已处理 | `POST /api/v1/feedbacks/{id}/process` → 更新列表状态 |
| 批量处理 | el-table 多选 → 遍历 POST process → 刷新列表 |
| 查看截图 | el-image preview 模式大图浏览 |
| 筛选/搜索 | 重置 page=1 → 重新请求 |

#### API 清单

| 接口 | 方法 | 路径 | 函数名 |
|------|------|------|--------|
| 反馈列表 | GET | `/api/v1/feedbacks` | `fetchFeedbacks(params)` |
| 反馈详情 | GET | `/api/v1/feedbacks/{id}` | `fetchFeedbackDetail(id)` |
| 标记已处理 | POST | `/api/v1/feedbacks/{id}/process` | `processFeedback(id)` |

---

## 5. 全局异常场景

| 异常场景 | 处理策略 |
|----------|----------|
| 非管理员访问 | 路由守卫拦截 → ElMessage.error('无权限访问') → `router.replace('/')` |
| 用户不存在 (404) | ElMessage.error('该用户不存在或已被删除') → 刷新列表 |
| 操作权限不足 (403) | ElMessage.error('无权限执行此操作') |
| 网络异常 | ElMessage.error + 重试 |
| 审核驳回未填原因 | 表单校验提示"请填写驳回原因" |
| 公告标题/内容为空 | 表单校验提示"公告标题和内容为必填项" |

---

## 6. 补充设计要点

### 6.1 管理端路由守卫

```typescript
// router/guards.ts 中管理端路由守卫
{
  path: '/admin',
  component: AdminLayout,
  meta: { requiresAuth: true, roles: ['admin'] },
  children: [
    { path: 'users', component: () => import('@/views/admin/UserManagePage.vue') },
    { path: 'roles', component: () => import('@/views/admin/RolePermissionPage.vue') },
    { path: 'announcements', component: () => import('@/views/admin/AnnouncementPage.vue') },
    { path: 'feedbacks', component: () => import('@/views/admin/FeedbackManagePage.vue') }
  ]
}
```

### 6.2 接口缺口标注

| 缺失接口 | 建议路径 | 说明 |
|----------|----------|------|
| 用户端反馈列表 | `GET /api/v1/users/me/feedbacks` | 当前仅有管理端反馈列表 |
| 公告 CRUD | `POST /api/v1/announcements` 等 | 当前仅有列表查询 |
| 角色权限 CRUD | `GET/POST /api/v1/roles/{role}/permissions` | 接口文档未定义 |
| 公告详情 | `GET /api/v1/announcements/{id}` | 接口文档未定义 |
