# 课程管理模块技术详情文档

生成时间：2026-03-19
技术栈：Python + FastAPI
模块名称：课程管理模块

---

## 模块概述

课程管理模块负责处理课程的创建、编辑、发布、下架、删除等核心操作，以及课程列表查询、课程详情展示、课程搜索等功能。本模块包含12个接口，支持讲师、管理员和普通用户三种角色的操作场景。

---

## 接口列表

| 序号 | 接口名称 | 接口路径 | HTTP方法 | 权限要求 |
|------|----------|----------|----------|----------|
| 1 | 课程列表 | /api/v1/courses | GET | 🔓 公开接口 |
| 2 | 课程详情 | /api/v1/courses/{course_id} | GET | 🔓 公开接口 |
| 3 | 创建课程 | /api/v1/courses | POST | 👨‍🏫👨‍💼 讲师或管理员 |
| 4 | 更新课程 | /api/v1/courses/{course_id} | POST | 👨‍🏫👨‍💼 讲师或管理员 |
| 5 | 发布课程 | /api/v1/courses/{course_id}/publish | POST | 👨‍🏫👨‍💼 讲师或管理员 |
| 6 | 下架课程 | /api/v1/courses/{course_id}/archive | POST | 👨‍🏫👨‍💼 讲师或管理员 |
| 7 | 删除课程 | /api/v1/courses/{course_id} | POST | 👨‍🏫👨‍💼 讲师或管理员 |
| 8 | 我的课程列表（讲师） | /api/v1/courses/my-courses | GET | 👨‍🏫👨‍💼 讲师或管理员 |
| 9 | 上传配套资料 | /api/v1/courses/{course_id}/materials | POST | 👨‍🏫👨‍💼 讲师或管理员 |
| 10 | 删除配套资料 | /api/v1/courses/{course_id}/materials/{material_id} | POST | 👨‍🏫👨‍💼 讲师或管理员 |
| 11 | 课程搜索 | /api/v1/courses/search | GET | 🔓 公开接口 |
| 12 | 首页课程列表 | /api/v1/courses/homepage | GET | 🔓 公开接口 |

---

## 一、课程列表

### 1. 基本信息

| 项目 | 内容 |
|------|------|
| 接口名称 | 课程列表 |
| 接口路径 | /api/v1/courses |
| HTTP方法 | GET |
| 功能描述 | 获取已发布的课程列表，支持关键词搜索、分类筛选和排序 |

### 2. 请求参数

#### Query参数

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|------|------|------|--------|------|
| keyword | string | 否 | - | 搜索关键词（标题、讲师名称） |
| category_id | integer | 否 | - | 分类ID |
| sort_by | string | 否 | latest | 排序方式：latest（最新）/ popular（最热） |
| page | integer | 否 | 1 | 页码，最小值1 |
| page_size | integer | 否 | 20 | 每页数量，最小1，最大100 |

#### 请求示例

```http
GET /api/v1/courses?keyword=Python&category_id=1&sort_by=latest&page=1&page_size=20
```

### 3. 响应结构

#### 成功响应

```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "items": [
      {
        "course_id": 1,
        "title": "Python入门教程",
        "cover_url": "https://example.com/cover.jpg",
        "thumbnail_url": "https://example.com/thumbnail.jpg",
        "summary": "从零开始学习Python编程",
        "teacher_id": 2,
        "teacher_name": "李老师",
        "category_id": 1,
        "category_name": "编程开发",
        "view_count": 1000,
        "published_at": "2026-03-01T10:00:00"
      }
    ],
    "total": 50,
    "page": 1,
    "page_size": 20,
    "total_pages": 3
  }
}
```

#### 响应字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| items | array | 课程列表 |
| items[].course_id | integer | 课程ID |
| items[].title | string | 课程标题 |
| items[].cover_url | string | 课程封面URL |
| items[].thumbnail_url | string | 课程缩略图URL |
| items[].summary | string | 课程简介 |
| items[].teacher_id | integer | 讲师ID |
| items[].teacher_name | string | 讲师名称 |
| items[].category_id | integer | 分类ID |
| items[].category_name | string | 分类名称 |
| items[].view_count | integer | 浏览次数 |
| items[].published_at | datetime | 发布时间 |
| total | integer | 总记录数 |
| page | integer | 当前页码 |
| page_size | integer | 每页数量 |
| total_pages | integer | 总页数 |

#### 失败响应

| 场景 | code | message |
|------|------|---------|
| 参数验证失败 | 422 | 数据验证失败 |

### 4. 业务逻辑

```
开始
  ↓
解析查询参数
  ↓
构建数据库查询条件
  ├── keyword搜索：title LIKE ? OR teacher_name LIKE ?
  ├── category_id筛选：category_id = ?
  └── 状态筛选：status = 'published'
  ↓
根据sort_by排序
  ├── latest → published_at DESC
  └── popular → view_count DESC
  ↓
执行分页查询
  ↓
关联查询讲师和分类信息
  ↓
返回课程列表
结束
```

### 5. 数据库操作

- **涉及数据表**：courses, users, categories
- **操作类型**：查询
- **查询条件**：status = 'published'
- **排序规则**：published_at DESC 或 view_count DESC

```sql
-- SQL示例
SELECT
    c.id as course_id,
    c.title,
    c.cover_url,
    c.thumbnail_url,
    c.summary,
    c.teacher_id,
    u.nickname as teacher_name,
    c.category_id,
    cat.name as category_name,
    c.view_count,
    c.published_at
FROM courses c
LEFT JOIN users u ON c.teacher_id = u.id
LEFT JOIN categories cat ON c.category_id = cat.id
WHERE c.status = 'published'
  AND (? IS NULL OR c.title LIKE ? OR u.nickname LIKE ?)
  AND (? IS NULL OR c.category_id = ?)
ORDER BY
    CASE WHEN ? = 'latest' THEN c.published_at END DESC,
    CASE WHEN ? = 'popular' THEN c.view_count END DESC
LIMIT ? OFFSET ?;
```

### 6. 依赖项和工具

- **Python库**：
  - sqlalchemy（ORM）
  - pydantic（数据模型）

### 7. 异常处理

| 异常场景 | 处理方式 |
|----------|----------|
| 参数验证失败 | Pydantic自动验证，返回422 |
| 数据库查询失败 | 捕获异常，返回500 |

### 8. 安全要求

- 公开接口，无需认证
- 只返回已发布的课程

### 9. 性能考虑

- **数据库索引**：
  - courses表：(status, published_at)复合索引
  - courses表：(status, view_count)复合索引
  - courses表：category_id索引
- **缓存策略**：热门课程列表可缓存5分钟
- **分页查询**：避免大数据量返回

### 10. 代码组织建议

#### Pydantic模型

```python
# schemas/course.py
from pydantic import BaseModel, Field
from datetime import datetime
from typing import Optional
from enum import Enum

class SortByEnum(str, Enum):
    LATEST = "latest"
    POPULAR = "popular"

class CourseListItem(BaseModel):
    course_id: int
    title: str
    cover_url: str
    thumbnail_url: Optional[str] = None
    summary: str
    teacher_id: int
    teacher_name: str
    category_id: int
    category_name: str
    view_count: int
    published_at: datetime

    class Config:
        from_attributes = True
```

#### 路由函数

```python
# api/v1/courses.py
from fastapi import APIRouter, Query, Depends
from sqlalchemy.ext.asyncio import AsyncSession

router = APIRouter(prefix="/courses", tags=["课程管理"])

@router.get("", response_model=ApiResponse[PaginatedResponse[CourseListItem]])
async def get_courses(
    keyword: Optional[str] = Query(default=None),
    category_id: Optional[int] = Query(default=None),
    sort_by: SortByEnum = Query(default=SortByEnum.LATEST),
    page: int = Query(default=1, ge=1),
    page_size: int = Query(default=20, ge=1, le=100),
    db: AsyncSession = Depends(get_db)
):
    result = await course_service.get_course_list(
        db, keyword, category_id, sort_by, page, page_size
    )
    return ApiResponse(data=result)
```

---

## 二、课程详情

### 1. 基本信息

| 项目 | 内容 |
|------|------|
| 接口名称 | 课程详情 |
| 接口路径 | /api/v1/courses/{course_id} |
| HTTP方法 | GET |
| 功能描述 | 获取课程的详细信息，包括课程基本信息、章节结构、学习资源等 |

### 2. 请求参数

#### Path参数

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| course_id | integer | 是 | 课程ID |

### 3. 响应结构

#### 成功响应

```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "course_id": 1,
    "title": "Python入门教程",
    "cover_url": "https://example.com/cover.jpg",
    "thumbnail_url": "https://example.com/thumbnail.jpg",
    "summary": "从零开始学习Python编程",
    "description": "<p>课程详细描述...</p>",
    "teacher_id": 2,
    "teacher_name": "李老师",
    "teacher_avatar": "https://example.com/avatar.jpg",
    "category_id": 1,
    "category_name": "编程开发",
    "tags": ["Python", "编程", "入门"],
    "status": "published",
    "view_count": 1000,
    "published_at": "2026-03-01T10:00:00",
    "materials": [
      {
        "material_id": 1,
        "file_name": "课程资料.pdf",
        "file_url": "https://example.com/material.pdf",
        "file_size": 1048576,
        "download_count": 100
      }
    ],
    "chapters": [
      {
        "chapter_id": 1,
        "title": "第一章 Python基础",
        "sort_order": 1,
        "sections": [
          {
            "section_id": 1,
            "title": "1.1 Python简介",
            "sort_order": 1,
            "resources": [
              {
                "resource_id": 1,
                "resource_type": "video",
                "file_name": "Python简介.mp4",
                "duration": 600
              }
            ]
          }
        ]
      }
    ]
  }
}
```

#### 响应字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| course_id | integer | 课程ID |
| title | string | 课程标题 |
| cover_url | string | 课程封面URL |
| thumbnail_url | string | 课程缩略图URL |
| summary | string | 课程简介 |
| description | string | 课程详细描述（富文本） |
| teacher_id | integer | 讲师ID |
| teacher_name | string | 讲师名称 |
| teacher_avatar | string | 讲师头像URL |
| category_id | integer | 分类ID |
| category_name | string | 分类名称 |
| tags | array | 标签数组 |
| status | string | 课程状态：draft/published/archived |
| view_count | integer | 浏览次数 |
| published_at | datetime | 发布时间 |
| materials | array | 配套资料列表 |
| chapters | array | 章节列表 |

#### 失败响应

| 场景 | code | message |
|------|------|---------|
| 课程不存在 | 404 | 课程不存在或已下架 |
| 课程未发布（非创建者） | 404 | 课程不存在或已下架 |

### 4. 业务逻辑

```
开始
  ↓
根据course_id查询课程基本信息
  ├── 不存在 → 返回404错误
  └── 存在 → 继续
  ↓
检查课程状态
  ├── 已发布 → 继续返回详情
  └── 未发布 → 检查是否为创建者
      ├── 是创建者 → 返回详情
      └── 否 → 返回404错误
  ↓
增加浏览次数（view_count + 1）
  ↓
查询配套资料列表
  ↓
查询章节列表
  ↓
查询小节列表
  ↓
查询资源列表
  ↓
组装返回数据
  ↓
返回课程详情
结束
```

### 5. 数据库操作

- **涉及数据表**：courses, users, categories, course_materials, chapters, sections, resources, course_tags, tags
- **操作类型**：查询、更新

```sql
-- 查询课程基本信息
SELECT * FROM courses WHERE id = ?;

-- 更新浏览次数
UPDATE courses SET view_count = view_count + 1 WHERE id = ?;

-- 查询配套资料
SELECT * FROM course_materials WHERE course_id = ?;

-- 查询章节
SELECT * FROM chapters WHERE course_id = ? ORDER BY sort_order;

-- 查询小节
SELECT * FROM sections WHERE chapter_id IN (?) ORDER BY sort_order;

-- 查询资源
SELECT * FROM resources WHERE section_id IN (?);

-- 查询标签
SELECT t.* FROM tags t
JOIN course_tags ct ON t.id = ct.tag_id
WHERE ct.course_id = ?;
```

### 6. 依赖项和工具

- **Python库**：
  - sqlalchemy（ORM）

### 7. 异常处理

| 异常场景 | 处理方式 |
|----------|----------|
| 课程不存在 | 查询后判断，返回404 |
| 数据库查询失败 | 捕获异常，返回500 |

### 8. 安全要求

- 公开接口，但只能查看已发布的课程
- 草稿和已下架课程只有创建者可以查看

### 9. 性能考虑

- **数据库索引**：
  - courses表：id主键
  - chapters表：(course_id, sort_order)复合索引
  - sections表：(chapter_id, sort_order)复合索引
  - resources表：section_id索引
- **缓存策略**：课程详情可缓存10分钟，更新后清除缓存
- **N+1问题**：使用预加载或批量查询避免N+1

### 10. 代码组织建议

#### Pydantic模型

```python
# schemas/course.py
class MaterialItem(BaseModel):
    material_id: int
    file_name: str
    file_url: str
    file_size: int
    download_count: int

class ResourceItem(BaseModel):
    resource_id: int
    resource_type: str
    file_name: str
    duration: Optional[int] = None

class SectionItem(BaseModel):
    section_id: int
    title: str
    sort_order: int
    resources: list[ResourceItem]

class ChapterItem(BaseModel):
    chapter_id: int
    title: str
    sort_order: int
    sections: list[SectionItem]

class CourseDetailResponse(BaseModel):
    course_id: int
    title: str
    cover_url: str
    thumbnail_url: Optional[str] = None
    summary: str
    description: Optional[str] = None
    teacher_id: int
    teacher_name: str
    teacher_avatar: Optional[str] = None
    category_id: int
    category_name: str
    tags: list[str]
    status: str
    view_count: int
    published_at: Optional[datetime] = None
    materials: list[MaterialItem]
    chapters: list[ChapterItem]
```

#### 服务层

```python
# services/course_service.py
class CourseService:
    async def get_course_detail(
        self,
        db: AsyncSession,
        course_id: int,
        current_user: Optional[User] = None
    ) -> CourseDetailResponse:
        # 查询课程
        course = await self.get_course_by_id(db, course_id)
        if not course:
            raise HTTPException(status_code=404, detail="课程不存在或已下架")

        # 权限检查
        if course.status != 'published':
            if not current_user or course.teacher_id != current_user.id:
                raise HTTPException(status_code=404, detail="课程不存在或已下架")

        # 增加浏览次数
        await self.increment_view_count(db, course_id)

        # 查询关联数据
        materials = await self.get_materials(db, course_id)
        chapters = await self.get_chapters_with_sections(db, course_id)
        tags = await self.get_tags(db, course_id)

        return CourseDetailResponse(
            course_id=course.id,
            title=course.title,
            # ... 其他字段
            materials=materials,
            chapters=chapters,
            tags=tags
        )
```

---

## 三、创建课程

### 1. 基本信息

| 项目 | 内容 |
|------|------|
| 接口名称 | 创建课程 |
| 接口路径 | /api/v1/courses |
| HTTP方法 | POST |
| 功能描述 | 讲师或管理员创建新课程，创建后状态为草稿 |

### 2. 请求参数

#### 请求体JSON

```json
{
  "title": "Python入门教程",
  "cover_url": "https://example.com/cover.jpg",
  "summary": "从零开始学习Python编程",
  "description": "<p>课程详细描述...</p>",
  "category_id": 1,
  "tags": ["Python", "编程", "入门"]
}
```

#### 参数说明

| 参数 | 类型 | 必填 | 说明 | 验证规则 |
|------|------|------|------|----------|
| title | string | 是 | 课程标题 | 2-100字符 |
| cover_url | string | 是 | 课程封面URL | 有效URL格式 |
| summary | string | 是 | 课程简介 | 10-500字符 |
| description | string | 否 | 课程详细描述 | 富文本，最多10000字符 |
| category_id | integer | 是 | 分类ID | 必须存在且启用 |
| tags | array | 否 | 标签数组 | 最多5个标签 |

### 3. 响应结构

#### 成功响应

```json
{
  "code": 200,
  "message": "课程创建成功",
  "data": {
    "course_id": 1,
    "title": "Python入门教程",
    "status": "draft",
    "created_at": "2026-03-17T10:00:00"
  }
}
```

#### 失败响应

| 场景 | code | message |
|------|------|---------|
| 权限不足 | 403 | 权限不足 |
| 参数验证失败 | 422 | 数据验证失败 |
| 分类不存在 | 400 | 分类不存在或已禁用 |

### 4. 业务逻辑

```
开始
  ↓
验证讲师或管理员权限
  ├── 否 → 返回403错误
  └── 是 → 继续
  ↓
验证请求参数格式
  ↓
验证分类是否存在且启用
  ├── 否 → 返回400错误
  └── 是 → 继续
  ↓
处理标签（新标签自动创建）
  ↓
开启数据库事务
  ↓
创建课程记录
  ├── status = 'draft'
  └── teacher_id = current_user.id
  ↓
关联标签（course_tags）
  ↓
提交事务
  ↓
记录操作日志
  ↓
返回课程ID
结束
```

### 5. 数据库操作

- **涉及数据表**：courses, course_tags, tags, categories
- **操作类型**：插入

```python
# SQLAlchemy示例
async with db.begin():
    # 创建课程
    course = Course(
        title=request.title,
        cover_url=request.cover_url,
        summary=request.summary,
        description=request.description,
        category_id=request.category_id,
        teacher_id=current_user.id,
        status='draft'
    )
    db.add(course)
    await db.flush()  # 获取生成的ID

    # 处理标签
    for tag_name in request.tags:
        # 查找或创建标签
        tag = await get_or_create_tag(db, tag_name)
        # 关联标签
        course_tag = CourseTag(
            course_id=course.id,
            tag_id=tag.id
        )
        db.add(course_tag)
```

### 6. 依赖项和工具

- **Python库**：
  - sqlalchemy（ORM）
  - pydantic（数据验证）

### 7. 异常处理

| 异常场景 | 处理方式 |
|----------|----------|
| 权限不足 | 中间件拦截，返回403 |
| 参数验证失败 | Pydantic自动验证，返回422 |
| 分类不存在 | 查询后判断，返回400 |
| 数据库插入失败 | 捕获异常，回滚事务，返回500 |

### 8. 安全要求

- 仅讲师和管理员可创建
- 课程内容需进行XSS过滤（富文本）
- 记录操作日志

### 9. 性能考虑

- 使用事务保证数据一致性
- 标签批量处理

### 10. 代码组织建议

#### Pydantic模型

```python
# schemas/course.py
from pydantic import BaseModel, Field, field_validator
from typing import Optional

class CourseCreateRequest(BaseModel):
    title: str = Field(..., min_length=2, max_length=100)
    cover_url: str
    summary: str = Field(..., min_length=10, max_length=500)
    description: Optional[str] = Field(None, max_length=10000)
    category_id: int
    tags: Optional[list[str]] = Field(default=[], max_length=5)

    @field_validator('tags')
    @classmethod
    def validate_tags(cls, v):
        if v:
            return [tag.strip() for tag in v if tag.strip()]
        return []

class CourseCreateResponse(BaseModel):
    course_id: int
    title: str
    status: str
    created_at: datetime
```

#### 路由函数

```python
# api/v1/courses.py
@router.post("", response_model=ApiResponse[CourseCreateResponse])
async def create_course(
    request: CourseCreateRequest,
    current_user: User = Depends(require_teacher_or_admin),
    db: AsyncSession = Depends(get_db)
):
    course = await course_service.create_course(db, request, current_user.id)
    return ApiResponse(
        message="课程创建成功",
        data=CourseCreateResponse(
            course_id=course.id,
            title=course.title,
            status=course.status,
            created_at=course.created_at
        )
    )
```

---

## 四、更新课程

### 1. 基本信息

| 项目 | 内容 |
|------|------|
| 接口名称 | 更新课程 |
| 接口路径 | /api/v1/courses/{course_id} |
| HTTP方法 | POST |
| 功能描述 | 更新课程基本信息，仅课程创建者可操作 |

### 2. 请求参数

#### Path参数

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| course_id | integer | 是 | 课程ID |

#### 请求体JSON

```json
{
  "title": "Python进阶教程",
  "cover_url": "https://example.com/new-cover.jpg",
  "summary": "深入学习Python编程",
  "description": "<p>更新后的课程描述...</p>",
  "category_id": 1,
  "tags": ["Python", "进阶"]
}
```

### 3. 响应结构

#### 成功响应

```json
{
  "code": 200,
  "message": "课程更新成功",
  "data": {
    "course_id": 1,
    "title": "Python进阶教程",
    "updated_at": "2026-03-17T11:00:00"
  }
}
```

#### 失败响应

| 场景 | code | message |
|------|------|---------|
| 权限不足 | 403 | 权限不足 |
| 课程不存在 | 404 | 课程不存在 |
| 非创建者 | 403 | 无权限修改此课程 |

### 4. 业务逻辑

```
开始
  ↓
验证讲师或管理员权限
  ├── 否 → 返回403错误
  └── 是 → 继续
  ↓
查询课程是否存在
  ├── 否 → 返回404错误
  └── 是 → 继续
  ↓
检查是否为课程创建者
  ├── 否 → 返回403错误
  └── 是 → 继续
  ↓
验证分类是否存在
  ├── 否 → 返回400错误
  └── 是 → 继续
  ↓
开启数据库事务
  ↓
更新课程基本信息
  ↓
更新标签关联
  ↓
提交事务
  ↓
清除课程缓存
  ↓
记录操作日志
  ↓
返回成功
结束
```

### 5. 数据库操作

- **涉及数据表**：courses, course_tags, tags
- **操作类型**：更新、删除、插入

```python
# SQLAlchemy示例
async with db.begin():
    # 更新课程
    await db.execute(
        update(Course)
        .where(Course.id == course_id)
        .values(
            title=request.title,
            cover_url=request.cover_url,
            summary=request.summary,
            description=request.description,
            category_id=request.category_id,
            updated_at=datetime.now()
        )
    )

    # 删除旧标签关联
    await db.execute(
        delete(CourseTag).where(CourseTag.course_id == course_id)
    )

    # 添加新标签关联
    for tag_name in request.tags:
        tag = await get_or_create_tag(db, tag_name)
        db.add(CourseTag(course_id=course_id, tag_id=tag.id))
```

### 6. 依赖项和工具

- **Python库**：
  - sqlalchemy（ORM）

### 7. 异常处理

| 异常场景 | 处理方式 |
|----------|----------|
| 课程不存在 | 查询后判断，返回404 |
| 非创建者 | 业务逻辑判断，返回403 |
| 数据库更新失败 | 捕获异常，回滚事务，返回500 |

### 8. 安全要求

- 仅课程创建者可修改
- 富文本内容XSS过滤
- 记录操作日志

### 9. 性能考虑

- 使用事务保证数据一致性
- 更新后清除缓存

### 10. 代码组织建议

```python
# api/v1/courses.py
@router.post("/{course_id}", response_model=ApiResponse[CourseUpdateResponse])
async def update_course(
    course_id: int,
    request: CourseUpdateRequest,
    current_user: User = Depends(require_teacher_or_admin),
    db: AsyncSession = Depends(get_db)
):
    # 检查课程存在性和权限
    course = await course_service.get_course_by_id(db, course_id)
    if not course:
        raise HTTPException(status_code=404, detail="课程不存在")

    if course.teacher_id != current_user.id and current_user.role != 'admin':
        raise HTTPException(status_code=403, detail="无权限修改此课程")

    updated_course = await course_service.update_course(db, course_id, request)
    return ApiResponse(message="课程更新成功", data=updated_course)
```

---

## 五、发布课程

### 1. 基本信息

| 项目 | 内容 |
|------|------|
| 接口名称 | 发布课程 |
| 接口路径 | /api/v1/courses/{course_id}/publish |
| HTTP方法 | POST |
| 功能描述 | 将草稿状态的课程发布，发布后对所有用户可见 |

### 2. 请求参数

#### Path参数

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| course_id | integer | 是 | 课程ID |

### 3. 响应结构

#### 成功响应

```json
{
  "code": 200,
  "message": "课程发布成功",
  "data": {
    "course_id": 1,
    "status": "published",
    "published_at": "2026-03-17T12:00:00"
  }
}
```

#### 失败响应

| 场景 | code | message |
|------|------|---------|
| 权限不足 | 403 | 权限不足 |
| 课程不存在 | 404 | 课程不存在 |
| 课程内容不完整 | 400 | 课程必须包含至少1个章节、1个小节、1个学习资源 |
| 课程信息不完整 | 400 | 课程基本信息不完整，请完善后再发布 |
| 已发布课程 | 400 | 课程已发布，无需重复操作 |

### 4. 业务逻辑

```
开始
  ↓
验证讲师或管理员权限
  ├── 否 → 返回403错误
  └── 是 → 继续
  ↓
查询课程是否存在
  ├── 否 → 返回404错误
  └── 是 → 继续
  ↓
检查是否为课程创建者
  ├── 否 → 返回403错误
  └── 是 → 继续
  ↓
检查课程状态
  ├── 已发布 → 返回400错误
  └── 草稿/已下架 → 继续
  ↓
验证课程信息完整性
  ├── 标题为空 → 返回400错误
  ├── 封面为空 → 返回400错误
  └── 简介为空 → 返回400错误
  ↓
验证课程内容完整性
  ├── 无章节 → 返回400错误
  ├── 章节无小节 → 返回400错误
  └── 小节无资源 → 返回400错误
  ↓
更新课程状态为published
  ↓
设置published_at时间
  ↓
更新搜索索引（Meilisearch）
  ↓
记录操作日志
  ↓
返回成功
结束
```

### 5. 数据库操作

- **涉及数据表**：courses, chapters, sections, resources
- **操作类型**：查询、更新

```python
# SQLAlchemy示例
async def publish_course(db: AsyncSession, course_id: int, teacher_id: int) -> Course:
    # 验证课程内容完整性
    chapters = await db.execute(
        select(Chapter).where(Chapter.course_id == course_id)
    )
    chapters = chapters.scalars().all()

    if not chapters:
        raise ValueError("课程必须包含至少1个章节")

    for chapter in chapters:
        sections = await db.execute(
            select(Section).where(Section.chapter_id == chapter.id)
        )
        sections = sections.scalars().all()

        if not sections:
            raise ValueError("每个章节必须包含至少1个小节")

        for section in sections:
            resources = await db.execute(
                select(Resource).where(Resource.section_id == section.id)
            )
            resources = resources.scalars().all()

            if not resources:
                raise ValueError("每个小节必须包含至少1个学习资源")

    # 更新课程状态
    await db.execute(
        update(Course)
        .where(Course.id == course_id)
        .values(
            status='published',
            published_at=datetime.now(),
            updated_at=datetime.now()
        )
    )
    await db.commit()

    return await get_course_by_id(db, course_id)
```

### 6. 依赖项和工具

- **Python库**：
  - sqlalchemy（ORM）
- **外部服务**：
  - Meilisearch（搜索索引）

### 7. 异常处理

| 异常场景 | 处理方式 |
|----------|----------|
| 课程不存在 | 查询后判断，返回404 |
| 非创建者 | 业务逻辑判断，返回403 |
| 内容不完整 | 验证后判断，返回400 |
| 数据库更新失败 | 捕获异常，回滚事务，返回500 |

### 8. 安全要求

- 仅课程创建者可发布
- 必须验证课程内容完整性
- 记录操作日志

### 9. 性能考虑

- 发布时同步更新搜索索引
- 可使用异步任务处理索引更新

### 10. 代码组织建议

```python
# api/v1/courses.py
@router.post("/{course_id}/publish", response_model=ApiResponse[CoursePublishResponse])
async def publish_course(
    course_id: int,
    current_user: User = Depends(require_teacher_or_admin),
    db: AsyncSession = Depends(get_db)
):
    try:
        course = await course_service.publish_course(db, course_id, current_user.id)

        # 异步更新搜索索引
        await search_service.index_course(course)

        return ApiResponse(
            message="课程发布成功",
            data=CoursePublishResponse(
                course_id=course.id,
                status=course.status,
                published_at=course.published_at
            )
        )
    except ValueError as e:
        raise HTTPException(status_code=400, detail=str(e))
```

---

## 六、下架课程

### 1. 基本信息

| 项目 | 内容 |
|------|------|
| 接口名称 | 下架课程 |
| 接口路径 | /api/v1/courses/{course_id}/archive |
| HTTP方法 | POST |
| 功能描述 | 将已发布的课程下架，下架后用户不可见 |

### 2. 请求参数

#### Path参数

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| course_id | integer | 是 | 课程ID |

#### 请求体JSON

```json
{
  "archive_reason": "课程内容需要更新"
}
```

#### 参数说明

| 参数 | 类型 | 必填 | 说明 | 验证规则 |
|------|------|------|------|----------|
| archive_reason | string | 是 | 下架原因 | 10-200字符 |

### 3. 响应结构

#### 成功响应

```json
{
  "code": 200,
  "message": "课程已下架",
  "data": {
    "course_id": 1,
    "status": "archived",
    "archived_at": "2026-03-17T13:00:00"
  }
}
```

#### 失败响应

| 场景 | code | message |
|------|------|---------|
| 权限不足 | 403 | 权限不足 |
| 课程不存在 | 404 | 课程不存在 |
| 课程未发布 | 400 | 只能下架已发布的课程 |
| 非创建者 | 403 | 无权限操作此课程 |

### 4. 业务逻辑

```
开始
  ↓
验证讲师或管理员权限
  ├── 否 → 返回403错误
  └── 是 → 继续
  ↓
查询课程是否存在
  ├── 否 → 返回404错误
  └── 是 → 继续
  ↓
检查是否为课程创建者
  ├── 否 → 返回403错误
  └── 是 → 继续
  ↓
检查课程状态是否为published
  ├── 否 → 返回400错误
  └── 是 → 继续
  ↓
更新课程状态为archived
  ↓
记录下架原因和时间
  ↓
从搜索索引中移除课程
  ↓
记录操作日志
  ↓
返回成功
结束
```

### 5. 数据库操作

- **涉及数据表**：courses
- **操作类型**：更新

```python
# SQLAlchemy示例
await db.execute(
    update(Course)
    .where(Course.id == course_id)
    .values(
        status='archived',
        archive_reason=request.archive_reason,
        archived_at=datetime.now(),
        updated_at=datetime.now()
    )
)
await db.commit()
```

### 6. 依赖项和工具

- **Python库**：
  - sqlalchemy（ORM）
- **外部服务**：
  - Meilisearch（搜索索引）

### 7. 异常处理

| 异常场景 | 处理方式 |
|----------|----------|
| 课程不存在 | 查询后判断，返回404 |
| 非创建者 | 业务逻辑判断，返回403 |
| 课程未发布 | 业务逻辑判断，返回400 |
| 数据库更新失败 | 捕获异常，返回500 |

### 8. 安全要求

- 仅课程创建者可下架
- 必须填写下架原因
- 记录操作日志

### 9. 性能考虑

- 从搜索索引中移除课程

### 10. 代码组织建议

```python
# schemas/course.py
class ArchiveCourseRequest(BaseModel):
    archive_reason: str = Field(..., min_length=10, max_length=200)

class ArchiveCourseResponse(BaseModel):
    course_id: int
    status: str
    archived_at: datetime

# api/v1/courses.py
@router.post("/{course_id}/archive", response_model=ApiResponse[ArchiveCourseResponse])
async def archive_course(
    course_id: int,
    request: ArchiveCourseRequest,
    current_user: User = Depends(require_teacher_or_admin),
    db: AsyncSession = Depends(get_db)
):
    course = await course_service.archive_course(
        db, course_id, request.archive_reason, current_user.id
    )

    # 从搜索索引中移除
    await search_service.remove_course(course_id)

    return ApiResponse(
        message="课程已下架",
        data=ArchiveCourseResponse(
            course_id=course.id,
            status=course.status,
            archived_at=course.archived_at
        )
    )
```

---

## 七、删除课程

### 1. 基本信息

| 项目 | 内容 |
|------|------|
| 接口名称 | 删除课程 |
| 接口路径 | /api/v1/courses/{course_id} |
| HTTP方法 | POST |
| 功能描述 | 永久删除课程及其所有关联数据 |

### 2. 请求参数

#### Path参数

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| course_id | integer | 是 | 课程ID |

### 3. 响应结构

#### 成功响应

```json
{
  "code": 200,
  "message": "课程删除成功",
  "data": null
}
```

#### 失败响应

| 场景 | code | message |
|------|------|---------|
| 权限不足 | 403 | 权限不足 |
| 课程不存在 | 404 | 课程不存在 |
| 课程已发布 | 400 | 已发布课程必须先下架才能删除 |
| 非创建者 | 403 | 无权限操作此课程 |

### 4. 业务逻辑

```
开始
  ↓
验证讲师或管理员权限
  ├── 否 → 返回403错误
  └── 是 → 继续
  ↓
查询课程是否存在
  ├── 否 → 返回404错误
  └── 是 → 继续
  ↓
检查是否为课程创建者
  ├── 否 → 返回403错误
  └── 是 → 继续
  ↓
检查课程状态
  ├── 已发布 → 返回400错误
  └── 草稿/已下架 → 继续
  ↓
开启数据库事务
  ↓
删除课程关联的所有数据
  ├── 删除学习进度记录
  ├── 删除资源文件记录
  ├── 删除小节记录
  ├── 删除章节记录
  ├── 删除配套资料记录
  ├── 删除标签关联
  └── 删除课程记录
  ↓
提交事务
  ↓
删除存储的文件资源
  ↓
记录操作日志
  ↓
返回成功
结束
```

### 5. 数据库操作

- **涉及数据表**：courses, chapters, sections, resources, course_materials, course_tags, learning_progress
- **操作类型**：删除

```python
# SQLAlchemy示例
async with db.begin():
    # 删除学习进度
    await db.execute(
        delete(LearningProgress)
        .where(LearningProgress.course_id == course_id)
    )

    # 获取所有资源ID用于删除文件
    resource_ids = await db.execute(
        select(Resource.id)
        .join(Section)
        .join(Chapter)
        .where(Chapter.course_id == course_id)
    )

    # 删除资源
    await db.execute(
        delete(Resource)
        .where(Resource.section_id.in_(
            select(Section.id)
            .join(Chapter)
            .where(Chapter.course_id == course_id)
        ))
    )

    # 删除小节
    await db.execute(
        delete(Section)
        .where(Section.chapter_id.in_(
            select(Chapter.id).where(Chapter.course_id == course_id)
        ))
    )

    # 删除章节
    await db.execute(
        delete(Chapter).where(Chapter.course_id == course_id)
    )

    # 删除配套资料
    await db.execute(
        delete(CourseMaterial).where(CourseMaterial.course_id == course_id)
    )

    # 删除标签关联
    await db.execute(
        delete(CourseTag).where(CourseTag.course_id == course_id)
    )

    # 删除课程
    await db.execute(
        delete(Course).where(Course.id == course_id)
    )
```

### 6. 依赖项和工具

- **Python库**：
  - sqlalchemy（ORM）
- **外部服务**：
  - 文件存储服务（删除文件）

### 7. 异常处理

| 异常场景 | 处理方式 |
|----------|----------|
| 课程不存在 | 查询后判断，返回404 |
| 非创建者 | 业务逻辑判断，返回403 |
| 课程已发布 | 业务逻辑判断，返回400 |
| 数据库删除失败 | 捕获异常，回滚事务，返回500 |

### 8. 安全要求

- 仅课程创建者可删除
- 已发布课程必须先下架
- 必须使用数据库事务
- 记录操作日志

### 9. 性能考虑

- 使用事务保证数据一致性
- 大量关联数据删除可能耗时，考虑使用异步任务

### 10. 代码组织建议

```python
# services/course_service.py
async def delete_course(
    self,
    db: AsyncSession,
    course_id: int,
    teacher_id: int
) -> None:
    """删除课程及其所有关联数据"""
    course = await self.get_course_by_id(db, course_id)
    if not course:
        raise HTTPException(status_code=404, detail="课程不存在")

    if course.teacher_id != teacher_id:
        raise HTTPException(status_code=403, detail="无权限操作此课程")

    if course.status == 'published':
        raise HTTPException(
            status_code=400,
            detail="已发布课程必须先下架才能删除"
        )

    # 获取需要删除的文件列表
    files_to_delete = await self.get_course_files(db, course_id)

    # 删除数据库记录
    async with db.begin():
        await self._delete_course_data(db, course_id)

    # 异步删除文件
    await file_service.delete_files_async(files_to_delete)
```

---

## 八、我的课程列表（讲师）

### 1. 基本信息

| 项目 | 内容 |
|------|------|
| 接口名称 | 我的课程列表 |
| 接口路径 | /api/v1/courses/my-courses |
| HTTP方法 | GET |
| 功能描述 | 讲师查看自己创建的所有课程 |

### 2. 请求参数

#### Query参数

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|------|------|------|--------|------|
| status | string | 否 | all | 状态筛选：all/draft/published/archived |
| page | integer | 否 | 1 | 页码 |
| page_size | integer | 否 | 20 | 每页数量 |

### 3. 响应结构

#### 成功响应

```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "items": [
      {
        "course_id": 1,
        "title": "Python入门教程",
        "cover_url": "https://example.com/cover.jpg",
        "status": "published",
        "view_count": 1000,
        "created_at": "2026-03-01T10:00:00",
        "published_at": "2026-03-01T12:00:00"
      }
    ],
    "total": 10,
    "page": 1,
    "page_size": 20,
    "total_pages": 1
  }
}
```

### 4. 业务逻辑

```
开始
  ↓
验证讲师或管理员权限
  ├── 否 → 返回403错误
  └── 是 → 继续
  ↓
解析状态筛选条件
  ↓
查询当前用户创建的课程
  ├── 条件：teacher_id = current_user.id
  └── 条件：status = ?（如有筛选）
  ↓
按创建时间倒序排序
  ↓
分页处理
  ↓
返回课程列表
结束
```

### 5. 数据库操作

- **涉及数据表**：courses
- **操作类型**：查询

```sql
-- SQL示例
SELECT
    id as course_id,
    title,
    cover_url,
    status,
    view_count,
    created_at,
    published_at
FROM courses
WHERE teacher_id = ?
  AND (? = 'all' OR status = ?)
ORDER BY created_at DESC
LIMIT ? OFFSET ?;
```

### 6. 依赖项和工具

- **Python库**：
  - sqlalchemy（ORM）

### 7. 异常处理

| 异常场景 | 处理方式 |
|----------|----------|
| 权限不足 | 中间件拦截，返回403 |
| 数据库查询失败 | 捕获异常，返回500 |

### 8. 安全要求

- 仅讲师和管理员可访问
- 只返回自己创建的课程

### 9. 性能考虑

- **数据库索引**：(teacher_id, created_at)复合索引

### 10. 代码组织建议

```python
# api/v1/courses.py
@router.get("/my-courses", response_model=ApiResponse[PaginatedResponse[MyCourseItem]])
async def get_my_courses(
    status: str = Query(default="all"),
    page: int = Query(default=1, ge=1),
    page_size: int = Query(default=20, ge=1, le=100),
    current_user: User = Depends(require_teacher_or_admin),
    db: AsyncSession = Depends(get_db)
):
    result = await course_service.get_my_courses(
        db, current_user.id, status, page, page_size
    )
    return ApiResponse(data=result)
```

---

## 九、上传配套资料

### 1. 基本信息

| 项目 | 内容 |
|------|------|
| 接口名称 | 上传配套资料 |
| 接口路径 | /api/v1/courses/{course_id}/materials |
| HTTP方法 | POST |
| 功能描述 | 讲师上传课程配套资料（PDF/DOC/ZIP） |

### 2. 请求参数

#### Path参数

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| course_id | integer | 是 | 课程ID |

#### 请求体（multipart/form-data）

```http
Content-Type: multipart/form-data

file: [文件二进制数据]
```

#### 文件限制

| 参数 | 说明 |
|------|------|
| 支持格式 | PDF/DOC/DOCX/ZIP |
| 单文件大小 | 最大1GB |

### 3. 响应结构

#### 成功响应

```json
{
  "code": 200,
  "message": "上传成功",
  "data": {
    "material_id": 1,
    "file_name": "课程资料.pdf",
    "file_url": "https://example.com/material.pdf",
    "file_size": 1048576
  }
}
```

#### 失败响应

| 场景 | code | message |
|------|------|---------|
| 权限不足 | 403 | 权限不足 |
| 课程不存在 | 404 | 课程不存在 |
| 文件格式不支持 | 400 | 仅支持PDF/DOC/ZIP格式 |
| 文件过大 | 400 | 文件大小不能超过1GB |
| 上传失败 | 500 | 文件上传失败，请重试 |

### 4. 业务逻辑

```
开始
  ↓
验证讲师或管理员权限
  ├── 否 → 返回403错误
  └── 是 → 继续
  ↓
检查课程存在性和权限
  ├── 不存在 → 返回404错误
  └── 非创建者 → 返回403错误
  ↓
验证文件格式
  ├── 不支持 → 返回400错误
  └── 支持 → 继续
  ↓
验证文件大小
  ├── 超过1GB → 返回400错误
  └── 符合要求 → 继续
  ↓
生成文件存储路径
  ↓
上传文件到存储服务
  ├── 失败 → 返回500错误
  └── 成功 → 继续
  ↓
创建资料记录
  ↓
返回资料信息
结束
```

### 5. 数据库操作

- **涉及数据表**：course_materials
- **操作类型**：插入

```python
# SQLAlchemy示例
material = CourseMaterial(
    course_id=course_id,
    file_name=file.filename,
    file_url=file_url,
    file_size=file_size
)
db.add(material)
await db.commit()
```

### 6. 依赖项和工具

- **Python库**：
  - python-multipart（文件上传）
  - aiofiles（异步文件处理）
- **外部服务**：
  - 对象存储服务（如OSS、S3）

### 7. 异常处理

| 异常场景 | 处理方式 |
|----------|----------|
| 文件格式不支持 | 验证后返回400 |
| 文件过大 | 验证后返回400 |
| 上传失败 | 捕获异常，返回500 |

### 8. 安全要求

- 仅课程创建者可上传
- 文件类型白名单验证
- 文件大小限制
- 病毒扫描（建议）

### 9. 性能考虑

- 大文件使用分片上传
- 使用异步上传避免阻塞

### 10. 代码组织建议

```python
# api/v1/courses.py
from fastapi import UploadFile, File

ALLOWED_EXTENSIONS = {'.pdf', '.doc', '.docx', '.zip'}
MAX_FILE_SIZE = 1024 * 1024 * 1024  # 1GB

@router.post("/{course_id}/materials", response_model=ApiResponse[MaterialResponse])
async def upload_material(
    course_id: int,
    file: UploadFile = File(...),
    current_user: User = Depends(require_teacher_or_admin),
    db: AsyncSession = Depends(get_db)
):
    # 检查课程权限
    course = await course_service.check_course_owner(db, course_id, current_user.id)

    # 验证文件格式
    file_ext = os.path.splitext(file.filename)[1].lower()
    if file_ext not in ALLOWED_EXTENSIONS:
        raise HTTPException(
            status_code=400,
            detail="仅支持PDF/DOC/ZIP格式"
        )

    # 上传文件
    file_url, file_size = await file_service.upload(file, f"materials/{course_id}/")

    # 创建记录
    material = await course_service.add_material(
        db, course_id, file.filename, file_url, file_size
    )

    return ApiResponse(
        message="上传成功",
        data=MaterialResponse(
            material_id=material.id,
            file_name=material.file_name,
            file_url=material.file_url,
            file_size=material.file_size
        )
    )
```

---

## 十、删除配套资料

### 1. 基本信息

| 项目 | 内容 |
|------|------|
| 接口名称 | 删除配套资料 |
| 接口路径 | /api/v1/courses/{course_id}/materials/{material_id} |
| HTTP方法 | POST |
| 功能描述 | 删除课程配套资料 |

### 2. 请求参数

#### Path参数

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| course_id | integer | 是 | 课程ID |
| material_id | integer | 是 | 资料ID |

### 3. 响应结构

#### 成功响应

```json
{
  "code": 200,
  "message": "删除成功",
  "data": null
}
```

#### 失败响应

| 场景 | code | message |
|------|------|---------|
| 权限不足 | 403 | 权限不足 |
| 资料不存在 | 404 | 资料不存在 |

### 4. 业务逻辑

```
开始
  ↓
验证讲师或管理员权限
  ├── 否 → 返回403错误
  └── 是 → 继续
  ↓
查询资料是否存在
  ├── 不存在 → 返回404错误
  └── 存在 → 继续
  ↓
检查课程所有权
  ├── 非创建者 → 返回403错误
  └── 创建者 → 继续
  ↓
删除数据库记录
  ↓
删除存储的文件
  ↓
返回成功
结束
```

### 5. 数据库操作

- **涉及数据表**：course_materials
- **操作类型**：删除

### 6. 代码组织建议

```python
# api/v1/courses.py
@router.post("/{course_id}/materials/{material_id}", response_model=ApiResponse)
async def delete_material(
    course_id: int,
    material_id: int,
    current_user: User = Depends(require_teacher_or_admin),
    db: AsyncSession = Depends(get_db)
):
    # 检查课程权限
    await course_service.check_course_owner(db, course_id, current_user.id)

    # 删除资料
    material = await course_service.get_material(db, material_id)
    if not material or material.course_id != course_id:
        raise HTTPException(status_code=404, detail="资料不存在")

    await course_service.delete_material(db, material_id)

    # 异步删除文件
    await file_service.delete_file_async(material.file_url)

    return ApiResponse(message="删除成功")
```

---

## 十一、课程搜索

### 1. 基本信息

| 项目 | 内容 |
|------|------|
| 接口名称 | 课程搜索 |
| 接口路径 | /api/v1/courses/search |
| HTTP方法 | GET |
| 功能描述 | 使用Meilisearch进行全文搜索，支持关键词高亮 |

### 2. 请求参数

#### Query参数

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|------|------|------|--------|------|
| q | string | 是 | - | 搜索关键词，最少2个字符 |
| page | integer | 否 | 1 | 页码 |
| page_size | integer | 否 | 20 | 每页数量 |

### 3. 响应结构

#### 成功响应

```json
{
  "code": 200,
  "message": "搜索成功",
  "data": {
    "items": [
      {
        "course_id": 1,
        "title": "Python入门教程",
        "cover_url": "https://example.com/cover.jpg",
        "summary": "从零开始学习Python编程",
        "teacher_name": "李老师",
        "highlight": {
          "title": "<em>Python</em>入门教程"
        }
      }
    ],
    "total": 5,
    "page": 1,
    "page_size": 20,
    "total_pages": 1
  }
}
```

#### 响应字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| highlight | object | 高亮显示结果 |
| highlight.title | string | 标题中匹配的关键词会被`<em>`标签包裹 |

### 4. 业务逻辑

```
开始
  ↓
验证搜索关键词
  ├── 为空或长度不足 → 返回400错误
  └── 符合要求 → 继续
  ↓
调用Meilisearch搜索
  ├── 搜索范围：title, summary, teacher_name
  └── 返回结果包含高亮信息
  ↓
格式化返回结果
  ↓
返回搜索结果
结束
```

### 5. 数据库操作

- **涉及数据表**：无直接数据库操作
- **外部服务**：Meilisearch

### 6. 依赖项和工具

- **Python库**：
  - meilisearch-python（搜索客户端）
- **外部服务**：
  - Meilisearch搜索引擎

### 7. 异常处理

| 异常场景 | 处理方式 |
|----------|----------|
| 搜索服务不可用 | 捕获异常，返回500，可降级到数据库模糊搜索 |
| 关键词过短 | 验证后返回400 |

### 8. 安全要求

- 公开接口
- 搜索关键词需转义防止注入

### 9. 性能考虑

- Meilisearch是内存搜索引擎，响应速度快
- 可配置搜索结果缓存

### 10. 代码组织建议

```python
# services/search_service.py
from meilisearch import Client

class SearchService:
    def __init__(self, host: str, api_key: str):
        self.client = Client(host, api_key)
        self.index = self.client.index('courses')

    async def search_courses(
        self,
        query: str,
        page: int,
        page_size: int
    ) -> dict:
        """搜索课程"""
        results = self.index.search(
            query,
            {
                'attributesToSearchOn': ['title', 'summary', 'teacher_name'],
                'attributesToHighlight': ['title', 'summary'],
                'page': page,
                'hitsPerPage': page_size
            }
        )

        return {
            'items': self._format_results(results['hits']),
            'total': results.get('totalHits', 0),
            'page': page,
            'page_size': page_size,
            'total_pages': (results.get('totalHits', 0) + page_size - 1) // page_size
        }

    def _format_results(self, hits: list) -> list:
        """格式化搜索结果"""
        return [
            {
                'course_id': hit['id'],
                'title': hit['title'],
                'cover_url': hit['cover_url'],
                'summary': hit['summary'],
                'teacher_name': hit['teacher_name'],
                'highlight': hit.get('_formatted', {})
            }
            for hit in hits
        ]
```

---

## 十二、首页课程列表

### 1. 基本信息

| 项目 | 内容 |
|------|------|
| 接口名称 | 首页课程列表 |
| 接口路径 | /api/v1/courses/homepage |
| HTTP方法 | GET |
| 功能描述 | 获取首页展示的课程列表，展示已发布且启用状态的课程 |

### 2. 请求参数

#### Query参数

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|------|------|------|--------|------|
| page | integer | 否 | 1 | 页码 |
| page_size | integer | 否 | 20 | 每页数量 |

### 3. 响应结构

#### 成功响应

```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "items": [
      {
        "course_id": 1,
        "title": "Python入门教程",
        "cover_url": "https://example.com/cover.jpg",
        "summary": "从零开始学习Python编程",
        "teacher_name": "李老师",
        "published_at": "2026-03-01T10:00:00"
      }
    ],
    "total": 30,
    "page": 1,
    "page_size": 20,
    "total_pages": 2
  }
}
```

### 4. 业务逻辑

```
开始
  ↓
查询已发布状态的课程
  ├── 条件：status = 'published'
  └── 条件：分类启用状态 = true
  ↓
按最新发布时间倒序排列
  ↓
分页处理
  ↓
返回课程列表
结束
```

### 5. 数据库操作

- **涉及数据表**：courses, users, categories
- **操作类型**：查询

```sql
-- SQL示例
SELECT
    c.id as course_id,
    c.title,
    c.cover_url,
    c.summary,
    u.nickname as teacher_name,
    c.published_at
FROM courses c
LEFT JOIN users u ON c.teacher_id = u.id
LEFT JOIN categories cat ON c.category_id = cat.id
WHERE c.status = 'published'
  AND cat.is_enabled = true
ORDER BY c.published_at DESC
LIMIT ? OFFSET ?;
```

### 6. 性能考虑

- **缓存策略**：首页课程列表缓存5分钟
- **数据库索引**：(status, published_at)复合索引

### 7. 代码组织建议

```python
# api/v1/courses.py
@router.get("/homepage", response_model=ApiResponse[PaginatedResponse[HomepageCourseItem]])
async def get_homepage_courses(
    page: int = Query(default=1, ge=1),
    page_size: int = Query(default=20, ge=1, le=100),
    db: AsyncSession = Depends(get_db)
):
    # 尝试从缓存获取
    cache_key = f"homepage_courses:{page}:{page_size}"
    cached = await cache_service.get(cache_key)
    if cached:
        return ApiResponse(data=cached)

    result = await course_service.get_homepage_courses(db, page, page_size)

    # 缓存5分钟
    await cache_service.set(cache_key, result, expire=300)

    return ApiResponse(data=result)
```

---

## 模块依赖汇总

### 数据库表

| 表名 | 说明 | 操作类型 |
|------|------|----------|
| courses | 课程表 | 查询、插入、更新、删除 |
| chapters | 章节表 | 查询、插入、更新、删除 |
| sections | 小节表 | 查询、插入、更新、删除 |
| resources | 学习资源表 | 查询、插入、更新、删除 |
| course_materials | 配套资料表 | 查询、插入、删除 |
| course_tags | 课程标签关联表 | 插入、删除 |
| tags | 标签表 | 查询、插入 |
| categories | 分类表 | 查询 |
| users | 用户表 | 查询 |
| learning_progress | 学习进度表 | 查询、删除 |

### 外部服务依赖

| 服务 | 说明 |
|------|------|
| 认证服务 | JWT令牌验证、权限验证 |
| 文件存储服务 | OSS/S3文件存储 |
| 搜索服务 | Meilisearch全文搜索 |
| 缓存服务 | Redis缓存 |

### 公共工具

| 工具 | 说明 |
|------|------|
| require_teacher_or_admin | 讲师或管理员权限验证 |
| file_service | 文件上传/删除服务 |
| cache_service | 缓存服务 |
| search_service | 搜索服务 |

---

## 文件组织建议

```
backend/app/
├── api/v1/
│   └── courses.py           # 课程管理路由
├── models/
│   ├── course.py            # 课程模型
│   ├── chapter.py           # 章节模型
│   ├── section.py           # 小节模型
│   ├── resource.py          # 学习资源模型
│   ├── course_material.py   # 配套资料模型
│   └── tag.py               # 标签模型
├── schemas/
│   └── course.py            # 课程相关Pydantic模型
├── services/
│   ├── course_service.py    # 课程业务逻辑
│   ├── file_service.py      # 文件服务
│   └── search_service.py    # 搜索服务
└── utils/
    └── validators.py        # 验证器
```

---

**文档版本**：v1.0
**生成时间**：2026-03-19
**维护人员**：开发团队